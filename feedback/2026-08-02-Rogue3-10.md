---
feedbackSchema: 2
date: 2026-08-02
workspace: Rogue3
cycle: item-34-verify-noop-merge-gate-steps
lane: none
toolVersion: 0.32.0
commit: 2798e71
---

# Rogue3 feedback — item #34, three merge-gate steps that checked nothing, and two things I published wrongly

## §1 Provenance and confidence

- **activation:** active
- **phases:** onboarding-first-build, lifecycle-authoring-or-not-used, implementation-test-evidence, verify-ship-pr
- **material events:** 6
- **zero-event reason:** n/a
- Checkpoints: `feedback/checkpoints/item-34-verify-noop-merge-gate-steps.jsonl`, 6 events, first at `2026-08-02T06:59:59Z`. Untracked while authoring; committed together with this report.
- Scaffold provenance `FS.GG.SDD.Artifacts/0.32.0`, provider `rendering`, provider contract 1.1.0, template `fs-gg-ui`, profile `game`, design system `wcag`, lifecycle `sdd`. Package pins: FS.GG.UI 0.21.1, FS.GG.Game 0.12.0, FS.GG.Audio 0.5.0, FS.GG.Contracts 7.5.2.
- Cycle boundary: board item #34, worker `godwit-d5f5`, claim marker `2026-08-02T06:54:13Z`, three concurrent item workers (`rook-f046` on #20, `wren-03e2` on #38). The `lane:` field is the SDD lifecycle lane, which is `none`. Branched from `origin/main` at `d8d0024` and rebased onto `2798e71` when PR #42 landed mid-wave; measurements labelled "at `d8d0024`" were taken before that rebase and are so marked.
- **What shipped.** `build.fsx` only. `TemplateDrift` and `GeneratedGuidanceCheck` are real checks that raise; `Dev` is removed from `Verify`'s step list; a new `SelfTest` target plants a violation per check. `TemplateDrift` carries the weight — `GeneratedGuidanceCheck` can only fail on a missing or empty guidance target, a missing agent inventory, or asymmetric generated guidance, and §5 records that its symmetry arm has nothing to exercise on this tree. 30 self-test cases pass, and 20 detection-only mutations — 14 of mine plus all 6 that survived an independent reviewer's sweep — are each killed by a named failing case, and the 228-test suite is unchanged and green.
- **The item was blocked for the first part of the cycle and became implementable mid-cycle.** #38 — the audit-binding gate's non-convergence — made any edit to `build.fsx` unmergeable. `wren-03e2` landed the fix in PR #42 during this wave. This report records the block as history (§4.5), not as the outcome.
- **Merge state at the time of writing: PR #45 is open and green** — both required checks pass, `verify-paths` reports OK against #34's declared touch-set, and `landable` returns `green`. Review, merge and the #34 done-stamp are pending, and §10 and §12 record that boundary rather than claiming a merge. `scripts/audit-binding-exceptions.json` is a shared token with #20 and #38; the agreed order was #42, then #20, then #34, and #42 merged at `07:32:49Z`. `rook-f046` had no PR open when I was ready, so I said on #34 that I would proceed and that whoever merges second re-runs `--grandfather` — which is now cheap, and my entries only ever name `build.fsx`.
- **Lifecycle authoring was not used.** One build script, no product behaviour, no `work/<id>/` artifacts. Recorded as exercised-and-declined. The board carries `Effort: M` for #34, which the fan-out brief routes to direct implementation, so the routing is on the record and not a judgement call.
- **The performance-first planning gate was not invoked, deliberately.** #34 changes a build script; nothing enters `update`/`view` and no frame-budget surface moves.
- **"Merge gate" is the issue's word, and it overstates what CI does — including after this fix.** `.github/workflows/` holds exactly two workflows: `verify.yml` runs `dotnet restore` + `dotnet test -c Release`, and `audit-bindings.yml` runs the binding checker. **Nothing on a PR invokes the FAKE `Verify` target, so nothing on a PR runs `TemplateDrift`, `GeneratedGuidanceCheck` or `SelfTest`.** They are real checks in the target a human runs before shipping, which is what #34 asked for, but a reader should not infer that CI now blocks kit drift. Surfaced by an independent reviewer; recorded rather than fixed, because wiring a workflow step is outside #34's declared touch-set.
- **Known defect #26 never entered this cycle's path.** The FAKE `Verify` target was not run. `readiness/logs/` is gitignored (`.gitignore:8`, `readiness/*/*`) and `git ls-files readiness/logs` is empty, so `writeLog` is not part of #26's tracked-artifact write surface — contrary to what #34's body suggests. `git status` showed only intended changes throughout.
- **Three independent fresh-context critics reviewed this cycle, and every one of them changed what shipped — including retracting things I had already published.** The technical critic falsified my central mechanism claim and found a hand route to green I had asserted did not exist. The actionability critic refuted an entire finding by checking an artifact I never checked, which cost a filed issue. The final critic caught a finding built on the wrong GitHub project, and produced the evasion battery that reshaped the code (§4.4). My own errors are recorded as a finding in their own right (§4.6), not buried here. Per-locator results are bound in `feedback/audits/2026-08-02-Rogue3-10.audit.json`.
- **Two published corrections were issued during this cycle, and one false finding was removed before publication.** A correction comment on #38 (the obsolete-exception mechanism was wrong), and #41 retracted and closed as invalid. Both sibling workers were notified directly, because both had received the wrong diagnosis from me first.
- Confidence limits. **First**, the two counting metrics for audit bindings are not interchangeable and this report labels every use: **204** raw `file:` locator occurrences over **112** distinct files, versus the checker's **188 bindings**, which is 169 evidence bindings deduplicated by `(audit, kind, locator, boundSha256)` plus 19 `report` bindings. `build.fsx` carries **7** raw occurrences and **4** deduplicated bindings. The draft of this report used the two interchangeably and a critic caught it. **Second**, §4.5's ledger digests from my own `--grandfather` probe are a function of the probe text and the `--reason` string; they are not reproducible constants and are quoted only to show that the sequence never repeats, never as values to match. **Third**, the 228-test count is reproducible but not artifact-backed — no TRX is retained for this cycle. **Fourth**, §4.3's claim that the provenance digests are stale "by design" is an inference from the fact that the drifted files are locally-edited driver skills and that the repository's own commit `d847456` edited them; no provider statement of intent exists in this workspace. **Fifth**, §4.4's coverage figures count files on disk under `.agents/skills` and `.claude/skills`; they say nothing about whether an unpinned file *has* drifted, only that nothing would report it if it did.

## §2 What worked

- **The planted violation, not the source reading, is what made the fix designable.** #34 argues from reading `build.fsx`. Planting a real violation first showed the same thing, then showed something the item did not know: the file I first planted into is one of only **7 of the 32 materialized** manifest-pinned kit files that an audit *also* binds, so the audit-binding gate caught it and the experiment proved less than it appeared to. Re-running on `.agents/skills/fs-gg-collision/SKILL.md` — one of the other 25 — gave the honest result: nothing in the repository noticed. That is the experiment the fix is built on.
- **A self-test that is itself mutation-tested.** `SelfTest` plants a violation per check (30 cases). The final sweep of 20 mutations removes *detection only*, never control flow, so every kill is attributable to a named case — 20 of 20, no survivors and no crash-kills. Six of those 20 are the reviewer's, not mine: my own 14-mutation sweep reported "no survivors" and an independent sweep immediately found six, including one that silently removed the digest check from 17 of 33 manifest entries while `SelfTest` still printed all-green. That discipline was learned mid-cycle: a first, cruder sweep killed four mutations by crashing, which proves the code ran and not that a case notices. A gate whose tests survive mutation is precisely the defect #34 describes, in a new costume.
- **Mutation testing found a real hole, not just weak tests.** Asking "what drift would still pass?" surfaced the mirrored kit copies: both manifests pin only `.agents/...` paths, so a drifted `.claude/skills/**` file is named by no manifest and no audit. All 95 mirrored files are byte-identical to their sources today; the check reaches the 32 that a manifest entry names, which is the part it can honestly pin. §4.4 records what remains.
- **`materializes-when` turned out to be the trap, and the fixture encodes it.** 32 of 33 manifest entries resolve; the 33rd (`fs-gg-samples`) is absent *correctly*, because its condition is `profile == sample-pack` and this product is `game`. A naive digest sweep would have shipped a gate that is red on a correct tree. `SelfTest` carries a case asserting an unmaterialized skill is *not* reported.
- **Three critics, and each one caught something the previous two had passed.** Exactly as the brief predicts, and more sharply than I expected. The technical critic falsified the mechanism behind my headline claim and found a green path I had called impossible; the actionability critic refuted a whole finding and the issue I had filed on it; the final critic caught the `Effort` finding built on the wrong project, and ran an evasion battery that found six ways to drift the tree past my new gate — four of which I closed before merge. Not one of the three was redundant.
- **The coordination primitives handled a genuine three-way contention without a human.** `widen` reported the collision and auto-notified both holders; `say` carried the diagnosis, the correction, and the retraction; the three workers converged on a merge order among themselves. `overlap` and `batch --explain` make the scheduler's reasoning auditable per item.
- **CI is reproducible locally in full**, which is how the block was diagnosed before anything was written: `audit-bindings.yml` reduces to the checker's self-test then the checker, both sub-second.

## §3 What did not

- Three of the merge gate's six steps reported success without reading anything, and the one named `TemplateDrift` was the only thing that would have guarded 25 kit files nothing else looks at (§4.1, §4.2).
- The obvious source of truth for a drift check — `.fsgg/scaffold-provenance.json` — is already stale for 12 files, so a gate built on it would have been red on a correct tree from the first run (§4.3).
- The fix narrows the hole rather than closing it: 64 of 190 kit files are covered, the gate's own oracle is pinned by nothing, and two evasion routes survive review (§4.4) — and no CI workflow runs any of it (§1).
- My own mutation sweep reported "no survivors" on checks an independent sweep then broke six ways, including one that disabled the digest check for 17 of 33 pinned entries while the self-test stayed green (§4.7).
- The item was unimplementable for the first part of the cycle, and my published diagnosis of *why* was wrong in mechanism and overstated in conclusion (§4.5, §4.6).
- I filed a defect against the scheduler that was refuted by one API call I did not make, warned two workers on that basis before retracting it, and separately drafted a finding accusing three merged reports of fabricating a board field that in fact exists — three claims, three critics, none of them caught by me (§4.6).

## §4 Findings

#### §4.1 `Dev`, `GeneratedGuidanceCheck` and `TemplateDrift` returned success without reading anything, and `Verify` counted all three

- **Kind:** defect
- **Impact:** Three of the merge gate's six steps provided no coverage while being counted as coverage. A reviewer seeing six green steps reasonably infers six checks. The one that mattered most, `TemplateDrift`, is the only step that would notice a materialized kit file drifting from the digest its manifest pins.
- **Expected:** A step named `TemplateDrift` fails on a tree whose template has drifted.
- **Observed:** At `d8d0024` the `run` dispatch routed all three to `writeLog`, and `Verify` ran the same three through `List.iter writeLog`; `writeLog` writes `readiness/logs/<target>.txt` and prints a success line. On a tree with a line appended to `.agents/skills/fs-gg-collision/SKILL.md` — a file whose sha256 is pinned in both skill manifests and which no audit binds — `TEMPLATEDRIFT_EXIT=0`, `GENERATEDGUIDANCECHECK_EXIT=0`, `DEV_EXIT=0`, the audit-binding gate exits 0, and `dotnet test` passes 228. Fixed in this cycle: the same tree now gives `TEMPLATEDRIFT_EXIT=1` naming both manifests and the pinned-versus-found digests. Note the scope this fixes and does not: the three steps are `Verify`'s, and CI does not invoke `Verify` (see §1), so this restores meaning to a target a human runs, not to the PR gate. `Dev` is removed from `Verify`'s step list rather than given a check, because the help banner has always documented it as "A completion-marker / log-writer only … a green `Dev` is not evidence the build passes", and inventing a check to justify its place in the gate would have restored the same false coverage in a different shape.
- **Evidence:** command:./fake.sh build -t TemplateDrift; command:./fake.sh build -t SelfTest; command:git show d8d0024:build.fsx; file:build.fsx; issue:EHotwagner/rogue3#34
- **Version:** `build.fsx` at `d8d0024`, fixed on branch `item/34-verify-noop-merge-gate-steps`.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `run` dispatch and the `Verify` step list.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-7.md` §4.6 first recorded it by reading the dispatch, and it was filed as issue:EHotwagner/rogue3#34. New here: the property is established by planted violation rather than by inspection, and the choice of planted file turns out to decide whether the experiment proves anything (§4.2).
- **Avoidable cost:** none to this cycle beyond the blocked period in §4.5; the durable cost is however long the three steps were counted.
- **Disposition:** product fix

#### §4.2 25 of 32 materialized kit files were guarded by nothing, and the 7 that were guarded were guarded by accident

- **Kind:** quality-gap
- **Impact:** This is why §4.1 mattered rather than merely read badly. With `TemplateDrift` a no-op, the only thing that ever noticed a kit file changing was an audit that happened to cite it as `file:` evidence — a side effect of one cycle's evidence choices, not a guard. It also nearly invalidated this report: the first planted violation I ran was on one of the 7 covered files, so the experiment "proved" a no-op on a tree the merge gate would in fact have rejected.
- **Expected:** A product that pins digests for 33 kit files in two manifests has some gate that reads them.
- **Observed:** `.agents/skills/skill-manifest.json` and `.claude/skills/skill-manifest.json` each pin 33 entries; 32 resolve on disk (the 33rd is correctly absent, §2). Exactly **7** of those paths are also bound by some audit under `feedback/audits/`: the `fs-gg-feedback-report`, `fs-gg-game-core`, `fs-gg-grids`, `fs-gg-line-drawing`, `fs-gg-model-swap`, `fs-gg-testing` and `work-roadmap` skill files. The other **25** are bound by nothing. No governance test reads either manifest — `tests/Rogue3.Tests/GovernanceTests.fs` scans `build.fsx` and generated source but never the manifests. Mutating one of the 25 leaves the audit-binding gate at exit 0 and the 228-test suite green. **The mirrored copies are worse off still:** both manifests pin only `.agents/...` paths, so all 95 files under `.claude/skills/` that mirror an `.agents/` source are named by no manifest at all, and `mirroredPaths` in provenance cannot serve as the oracle for them (§4.3). Closed by the `TemplateDrift` implemented in this cycle, which reads both manifests directly and additionally holds every mirrored copy to the digest pinned for its source — all 95 are byte-identical today, so the check is green where it ships.
- **Evidence:** command:./fake.sh build -t TemplateDrift; file:tests/Rogue3.Tests/GovernanceTests.fs; file:.agents/skills/skill-manifest.json; file:build.fsx
- **Version:** manifests as at `d8d0024`; FS.GG.SDD.Artifacts 0.32.0 emitted them.
- **Owner:** EHotwagner/rogue3 for the gate (fixed here); FS-GG scaffold provider for shipping pinned digests with no consuming check.
- **Recurrence:** new — no prior report measures manifest coverage. Adjacent to `feedback/2026-08-02-Rogue3-5.md` §4.1 and issue:EHotwagner/rogue3#32, which are about unreferenced *compile items*; this is about unread *pinned digests*, a different surface with the same shape.
- **Avoidable cost:** one discarded experiment, and one near-miss where this report would have shipped an unreproducible reproduction.
- **Disposition:** product fix

#### §4.3 The obvious source of truth for a drift gate is already stale for 12 files, so building on it would have shipped a permanently red gate

- **Kind:** capability-gap
- **Impact:** `.fsgg/scaffold-provenance.json` is the file that looks like the drift oracle: it carries sha256 pins for 24 `mirroredPaths`, 42 `driverPaths` and 20 `gameSkillPaths`. Building `TemplateDrift` on it — the obvious implementation, and the one the item's own wording about provenance nudges toward — produces a gate that fails on a correct tree at the first run. Nothing in the file distinguishes a digest that must hold from one that records history.
- **Expected:** A provenance file that pins digests either states which pins are invariants, or is not the place a gate reads.
- **Observed:** At `d8d0024`, recomputing every sha256-bearing entry (raw bytes) gives: `gameSkillPaths` 20/20 match; `mirroredPaths` 23/24, the exception being `.claude/skills/skill-manifest.json`; `driverPaths` 32/42, the ten mismatches all `work-board`, `work-board-best`, `work-board-normal`, `work-board/references/deep-detail.md` and `work-board/references/host-loop.md`, in both `.agents/` and `.claude/`; `producedPaths` has 145 entries of which only 24 carry a digest, and one of those mismatches. The driver-skill drift is legitimate — this repository edits those skills locally, most recently in `d847456` ("Fix the board concurrency model at two waves of three plus two reviewers"). Separately, `producedPaths` still lists `Grids.fs`, `LineDrawing.fs` and `Visibility.fs`, all deliberately deleted, which is the ambiguity #34's body already names. So the implemented `TemplateDrift` deliberately reads the two skill manifests and not provenance, and says so in a comment at the point of decision.
- **Evidence:** file:.fsgg/scaffold-provenance.json; command:git log --format=%h%x20%s -5; file:build.fsx; issue:EHotwagner/rogue3#34
- **Version:** provenance emitted by FS.GG.SDD.Artifacts 0.32.0; measured at `d8d0024`.
- **Owner:** FS-GG scaffold provider — `scaffold-provenance.json` schema; it needs a way to say "this pin is an invariant" versus "this pin is a record".
- **Recurrence:** new as a measurement. `feedback/2026-08-02-Rogue3-7.md` §4.6 and issue:EHotwagner/rogue3#34 both note that provenance does not distinguish "produced then deleted" from "produced and still present"; neither measures how many pins are already stale, which is what makes it unusable as a gate input rather than merely imprecise.
- **Avoidable cost:** one discarded implementation direction, caught before any code was written because the digests were recomputed first.
- **Disposition:** issue — worth filing against the scaffold provider; recorded here with the measurement rather than filed blind, since #16 already asks how upstream findings should leave this repository

#### §4.4 The new gate's own oracle is unpinned, and 126 of 190 kit files are still pinned by nothing

- **Kind:** quality-gap
- **Impact:** §4.1's fix narrows the hole in §4.2; it does not close it, and the honest number matters because the next person to read a green `TemplateDrift` will infer more coverage than exists. Two routes still put arbitrary drift into the tree with every gate green. The sharper one is that the file the gate *trusts* is itself pinned by nothing: `.fsgg/scaffold-provenance.json` does pin both manifests, but its pins for them are already stale (§4.3), and no audit under `feedback/audits/` binds either — so the oracle is as editable as the thing it guards.
- **Expected:** A digest gate either covers the kit or reports what it does not cover.
- **Observed:** `.agents/skills` and `.claude/skills` hold 95 files each, 190 in total. The manifests pin 33 entries, 32 materialized, and this cycle's check covers those 32 plus their 32 mirrors: **64 of 190**. Of 52 `SKILL.md` files under `.agents/skills`, **20 are named by no manifest entry at all** — every `fs-gg-sdd-*` skill, plus `check-board`, `cross-repo-coordination`, `intra-repo-parallel-work` and `pnext-item` — and with their mirrors that is 40 files pinned nowhere in the repository. An independent reviewer's evasion battery found nine routes; the fix closed four of them during review (a deleted mirror, three deleted mirror directories, flipping `materializes-when` to park a present file outside the digest check, and editing one manifest alone), and **two remain green**: appending to `.agents/skills/work-board/references/deep-detail.md`, which no manifest names, and mutating a kit file while deleting its entry from *both* manifests. The second is why the two-manifest agreement check is a weak guarantee rather than a strong one, and it is stated as such in the code.
- **Evidence:** command:./fake.sh build -t SelfTest; command:./fake.sh build -t TemplateDrift; file:.agents/skills/skill-manifest.json; file:build.fsx; issue:EHotwagner/rogue3#46
- **Version:** measured at `d8d0024`; the check as it ships on `item/34-verify-noop-merge-gate-steps`.
- **Owner:** FS-GG scaffold provider — the manifest should pin the whole kit it materializes, and should be pinned itself; EHotwagner/rogue3 for the residual routes.
- **Recurrence:** new — this is §4.2 measured *after* the fix rather than before, which no prior report has had occasion to do.
- **Avoidable cost:** none charged here; the cost is the drift that remains invisible until the manifest covers the kit.
- **Disposition:** issue — filed as issue:EHotwagner/rogue3#46 with the two surviving evasion routes and the 64/190 figure, rather than left implicit in a green gate

#### §4.5 The audit-binding gate blocked this item until a sibling worker's fix landed mid-wave

- **Kind:** defect
- **Impact:** For the first part of this cycle no edit to `build.fsx` could be merged, so #34's entire declared touch-set was unreachable. Diagnosis cost roughly 40 minutes and two full-tree reproductions.
- **Expected:** A board item declaring `Paths: build.fsx` is implementable by editing `build.fsx`.
- **Observed:** Editing `build.fsx` produces four stale bindings across four audits (`2026-08-01-Rogue3-8`, `2026-08-02-Rogue3-4`, `2026-08-02-Rogue3-7`, `2026-08-02-Rogue3`), and **zero obsolete exceptions** — a correction to what I first published, see §4.6. Excusing them runs `--grandfather`, which writes `scripts/audit-binding-exceptions.json`, which `2026-08-02-Rogue3-7.audit.json` binds; five consecutive `--grandfather` + `check` rounds gave exit 1 every round with a different ledger digest each time. Rebinding instead fails differently: hand-rebinding all seven `file:build.fsx` occurrences without touching the ledger leaves two obsolete exceptions *and* a new stale binding, because `2026-08-02-Rogue3-6.audit.json` binds `file:feedback/audits/2026-08-02-Rogue3-7.audit.json`. A hand route to green does exist — rebind, hand-prune the ledger, rebind the two audit-on-audit pins — but every route writes the ledger, which was outside this item's declared touch-set and held by two live workers. `wren-03e2` fixed the root cause in PR #42 during this wave by exempting both remedy surfaces from the scan.
- **Evidence:** command:python3 scripts/check-audit-bindings.py; command:git show d8d0024:build.fsx; issue:EHotwagner/rogue3#38; issue:EHotwagner/rogue3#34
- **Version:** `scripts/check-audit-bindings.py` at `d8d0024`; fixed by PR #42.
- **Recurrence:** seen again, and substantially a duplicate — issue:EHotwagner/rogue3#38 records the non-convergence, and a comment on it at `2026-08-02T06:42:10Z`, twelve minutes before I claimed #34, had already reproduced the audit-cites-audit shape with checker output and proposed exempting `feedback/audits/*.audit.json`. My later comment framed that as new evidence; it was not, and the credit is `wren-03e2`'s. What this cycle adds is only that `build.fsx` reaches the same loop, which makes it an instance of the general form rather than a stronger case.
- **Owner:** EHotwagner/rogue3 — `scripts/check-audit-bindings.py`.
- **Avoidable cost:** ~40 minutes and two reproductions, plus one incorrect published diagnosis (§4.6).
- **Disposition:** existing issue

#### §4.6 Every claim of mine that no independent critic had checked turned out to be wrong — three of them, each caught by a different critic

- **Kind:** orchestration
- **Impact:** I posted a diagnosis on #38, filed issue #41 against the coordination engine, and drafted a finding accusing three merged reports of fabricating a board field — and warned two sibling workers on the strength of the first two. All three were wrong. Each was caught by a different critic, and none by me. The common cause is that I treated "I ran a command and read its output" as sufficient, without asking what the command's output does *not* cover. The brief's two-critic rule is written for the report; nothing applies it to what a worker publishes to the board mid-cycle, which is where the damage is immediate and where two of these three went.
- **Expected:** A claim published to the board is at least as well checked as a claim in the report, since other workers act on it; and a claim that something does not exist is checked against the thing itself, not against a tool's projection of it.
- **Observed:** Three errors. **First**, I claimed editing `build.fsx` makes two ledger exceptions *obsolete*. It does not — it produces four stale bindings and no obsolete entries, because `evaluate()` adds the binding key to `used_keys` before comparing `observedSha256` (`scripts/check-audit-bindings.py:395-397`), so a re-edited excused binding is a violation and never obsolete. I had read the output of my *rebind* experiment back into my *grandfather* experiment. My own first probe output already showed no obsolete section, and I did not notice. **Second**, and worse, #41 claimed `take` admitted two colliding items. It did not: `#20`'s `Paths:` contained no `scripts/audit-binding-exceptions.json` at `03:34:41Z`, the last revision before the `06:53:18Z` and `06:53:47Z` claims; it first appears at `06:59:44Z`, six minutes *after*, when `rook-f046` widened and announced it in the same comment trail I had already read. One GraphQL call on the body edit history refutes the whole issue, and I ran `overlap --active` — which reports *current* state — instead. #41 is retracted and closed; a correction is posted on #38; both workers were told directly. **Third**, a draft finding of this report asserted the board has no `Effort` field and that three merged reports had therefore recorded reading something that does not exist. False. `gh project field-list 7 --owner EHotwagner` shows Rogue3 (project **#7**) carrying an `Effort` single-select with options `S`/`M`/`L`/`XL`; **#34 itself is `Effort: M`**, #14 and #28 are `S`, #15 is `M` — every accused report was right. I had run `gh project field-list 1`, which is a different project (`@Training outline`), and treated `fsgg-coord ready --json` — which does not project the field — as corroboration. The finding was replaced before publication, but it would have defamed three cycles and their critics, and it is the same error shape as the first two: an absence inferred from a partial view.
- **Evidence:** command:python3 scripts/check-audit-bindings.py; command:gh project field-list 7 --owner EHotwagner; issue:EHotwagner/rogue3#41; issue:EHotwagner/rogue3#38; file:feedback/checkpoints/item-34-verify-noop-merge-gate-steps.jsonl
- **Version:** this cycle; `scripts/check-audit-bindings.py` at `d8d0024`.
- **Owner:** FS-GG `.github` — the fan-out brief and `pnext-item`, which require independent critique of the report and say nothing about board comments or issues filed mid-cycle; and `fs.gg.coord.cli`, whose `ready --json` omits the very field the brief routes on (§11.2).
- **Recurrence:** new in this form. `feedback/2026-08-02-Rogue3-7.md` §1 records critics correcting six past-tense claims about an unperformed merge, and the brief records a host publishing a false verification from a piped exit code; both are the same family — a claim published before it was checked — but neither is about mid-cycle board writes.
- **Avoidable cost:** one invalid issue filed and closed, one incorrect comment corrected, three misleading messages to two workers, roughly 20 minutes of two workers' attention, and one false finding that reached a late draft and was removed only because a third critic checked the right project.
- **Disposition:** accepted — recorded rather than filed; the remedy is a wording change in a brief owned upstream, and #16 already holds the question of how upstream findings leave this repository

#### §4.7 A self-test that is mutation-tested is the only version of this fix that is worth anything

- **Kind:** positive-pattern
- **Impact:** #34's acceptance asks for "a test proving each fails on a planted violation, the way `scripts/check-audit-bindings.py --selftest` proves itself". Following that literally — a `SelfTest` target inside `build.fsx` rather than unit tests — kept the whole fix inside the item's one declared path, avoided `tests/Rogue3.Tests/` which a sibling worker held, and produced a gate that demonstrates its own liveness on every run.
- **Expected:** n/a — recorded as a pattern worth keeping.
- **Observed:** `SelfTest` runs 30 cases: both checks clean on this repository, a faithful synthetic fixture clean, an unmaterialized skill *not* reported, a mutated kit file reported by both manifests, a drifted mirrored copy reported exactly once, a deleted file reported, an unreadable `materializes-when` expression reported rather than skipped, a missing manifest reported, a malformed pinned digest reported rather than crashing the gate, and five guidance cases including the asymmetric-generated-guidance case. The final mutation sweep removes detection only — never control flow — and killed **20 of 20**, each by a named case, with no survivors and no crash-kills. That design was forced by a first, cruder sweep in which four mutations died by crashing; a crash proves the code ran, not that a case notices the loss, so those four were rewritten. Asking the same question of the checks themselves is what surfaced the mirrored-copy hole (§4.2), so mutation testing paid for itself twice: once in test quality, once in coverage.
- **Evidence:** command:./fake.sh build -t SelfTest; command:python3 scripts/check-audit-bindings.py --selftest; file:build.fsx
- **Version:** branch `item/34-verify-noop-merge-gate-steps`.
- **Owner:** EHotwagner/rogue3 — keep; and `fs-gg-testing`, where the in-script self-test pattern for build-time gates is not described.
- **Recurrence:** new as a stated pattern in this repository's reports; `feedback/2026-08-02-Rogue3-7.md` §4.5 records the audit checker's self-test giving "immediate, well-founded confidence", which is the same pattern observed on someone else's gate.
- **Avoidable cost:** none — this is the avoided cost.
- **Disposition:** accepted

## §5 Did not exercise

- **The FAKE `Verify` merge gate**, deliberately: known defect #26 rewrites tracked `readiness/` artifacts, CI does not invoke it, and its two new steps were exercised directly instead.
- **The SDD lifecycle** (`charter` → `ship`). Routed to direct implementation; no `work/<id>/` artifacts.
- **Runtime and playtest.** No runtime behaviour changed; `-t Run` launches a persistent window.
- **Performance evidence and the `pnext-item` planning gate.** No simulation or frame-budget surface touched.
- **Packaging and upgrade.** No package pin changed.
- **`GeneratedGuidanceCheck`'s symmetry arm against real generated guidance.** No `readiness/*/agent-commands/` directory exists in this tree, so that arm is exercised only by the synthetic fixture, where it is planted both ways.

## §6 Doc-versus-behavior contradictions

- **`scripts/check-audit-bindings.py` versus reachable remedies.** Its docstring gives remedies "in preference order", rebinding first, and says "expect (2) to be the common path until a rebind command exists. That is a known weakness, not the intended design." At `d8d0024` neither remedy terminates under the tool alone; only a hand route does. Owner: `scripts/check-audit-bindings.py`, addressed by PR #42.
- **`scripts/fsgg-coord ready --json` versus the board.** The board has an `Effort` single-select (`S`/`M`/`L`/`XL`) and #34 carries `M`, but `ready --json` projects only `number, repo, title, status, class, severity, state`, so the field the fan-out brief routes on is invisible to the command a worker is told to use. I mistook that projection for the board and drew a false conclusion from it — see §4.6. Owner: FS-GG `.github`, `fs.gg.coord.cli`.
- **Resolved this cycle:** `build.fsx`'s help banner documented `Dev` as evidence-free while `Verify` counted it as a gate step. The banner now states that `Verify` does not run `Dev`, and the step list matches.

## §7 Workarounds still in the tree

- **`scripts/audit-binding-exceptions.json`, 61 entries at `d8d0024`, 65 after this cycle.** The standing workaround for evidence digests rotting. Removal condition: a rebind command, plus the scan-set change PR #42 makes. Risk of permanence: monotone growth (45 → 47 → 48 → 51 → 61 → 63 → 65 across six commits; 63 is PR #42's two entries), and it is a declared path on three concurrent items, none of which is about it. **The in-place-rewrite problem recurred here and is worth naming precisely.** This cycle's `--grandfather` reported "65 exception(s); pruned 0", but only **two** entries are new (`2026-08-02-Rogue3-4`, `2026-08-02-Rogue3-7`); the other two `file:build.fsx` bindings already had entries, and their `observedSha256` *and* `reason` were overwritten in place — discarding the rationale item #14 recorded about the `Run`-target drain rewrite, which is now reachable only through git history. So four bindings were excused and the count rose by two. `feedback/2026-08-02-Rogue3-7.md` §4.4 recorded exactly this ("four bindings broke and only three entries appeared, which means the entry count structurally understates how many distinct events have excused a binding"); one cycle later the ledger still has no way to record two reasons for one binding, and the tool still silently replaces the first.
- **`Dev` as a target that writes a success log and checks nothing.** Retained deliberately, no longer counted by `Verify`. Removal condition: none — it is an honest dev-loop marker, and both the help banner and `GovernanceTests.assertBuildHelpBanner` pin that description. Risk: a future reader re-adds it to a gate list; the banner now says explicitly that `Verify` does not run it, and names the issue.
- **`TemplateDrift`'s deliberate blindness to `.fsgg/scaffold-provenance.json`.** Justified in §4.3 and commented at the decision point in `build.fsx`. Removal condition: a provenance schema that marks which pins are invariants. Risk: someone "completes" the check by adding provenance and turns the gate permanently red.

## §8 Friction and avoidable cost

- **~40 minutes diagnosing a blocker that a sibling worker fixed in the same wave.** Two full-tree reproductions plus a binding census, before any code. Unavoidable given the state at claim time, but the second reproduction was run only because the checker's docstring recommends the route that does not work.
- **One invalid issue filed, retracted and closed; one published comment corrected; three messages to two workers on wrong premises** (§4.6). The most expensive mistake of the cycle, and entirely mine.
- **One discarded implementation direction** (provenance-based drift), caught before code by recomputing the digests first.
- **Four mutations re-run** because the first pass killed them by crash rather than by a failing case — cheap, and it added two cases.
- **Command durations, kept separate from elapsed time:** `dotnet test` 228 tests in 4 s; `check-audit-bindings.py` sub-second; `./fake.sh build -t SelfTest` a few seconds; the 11-mutation sweep a few minutes, dominated by `dotnet fsi` startup per tree.
- **Three tool invocations refused by the worktree-isolation guard** for combining a command with a pipe or redirect, each re-run as a script file. This recurs — `feedback/2026-08-02-Rogue3-3.md` §4.3 and `-4.md` §8 record the same guard and the same remedy — and it interacts badly with the brief's own instruction to capture true exit codes rather than `$?` after a `| tail`, since the safe idiom is the one the guard rejects.

## §9 Skill value and gaps

- **`fs-gg-feedback-report` — invoked, and load-bearing.** `checkpoint`, `validate-checkpoint-state`, `digest` and `validate` all behaved as documented. Two gaps: its audit vocabulary has no locator kind for board or tool state, which is the structural half of §4.4; and it is silent on the change-cost of choosing a `file:` locator, which is why `build.fsx` accumulated seven citations before anyone noticed it was expensive to edit.
- **`fs-gg-testing` — relevant, not invoked as a document.** The in-script self-test pattern this cycle used for a build-time gate (§4.7) is not described there; it is materially different from the product-test guidance the skill covers.
- **`intra-repo-parallel-work` — exercised through `fsgg-coord` rather than as a document.** The claim/lease/widen/overlap/say model is what §2 records working; the engine's `--help` was more current than any prose.
- **`fs-gg-sdd-*` and `pnext-item` — relevant, not invoked.** #34 is `Effort: M`, which routes to direct implementation.
- **No skill covers the audit-binding gate**, which was the largest source of friction in this cycle and the previous one. A worker meets it when CI goes red, and its docstring's remedy ordering was wrong for the tree at `d8d0024`. Recorded as a gap rather than filed, since PR #42 changes the rules.
- **Not listed:** globally available tools and connectors unrelated to this workspace.

## §10 Outcome markers

| Marker | Value | Basis |
|---|---|---|
| Claim | `2026-08-02T06:54:13Z` on #34 | issue comment timestamp |
| Time to first material observation | ~6 min (first checkpoint `06:59:59Z`) | checkpoint file |
| Time to first build | n/a — no compiled product change | — |
| First meaningful test | 228 passed, 0 failed, 4 s, baseline before any edit | test output |
| Blocked period | claim → PR #42 green (~20 min of the cycle spent proving the item unimplementable) | §4.5 |
| First green verification | `SelfTest` 30/30, `TemplateDrift` and `GeneratedGuidanceCheck` exit 0 on this tree, 228 tests unchanged | §4.7 |
| Mutation result | 20 of 20 killed, each by a named failing case; 0 survivors, 0 crash-kills. My own sweep of 14 claimed "no survivors"; a reviewer's independent sweep found 6 | §4.7 |
| Ship readiness | PR #45 open and green: both required checks pass, `verify-paths` OK, `landable` green | §1 |
| Merge | **not merged** at the time of writing; review and the #34 done-stamp pending | §1 |
| Product change | `build.fsx` only | `git status` |
| Filed / corrected | #46 filed (residual gate coverage); #41 filed then retracted and closed as invalid; correction posted on #38; new evidence on #34 | board |

Estimates: elapsed times are the author's contemporaneous record. Test counts, mutation counts and self-test case counts are command output.

## §11 Falsifiable improvements

1. **Wire `SelfTest` and the two checks into a CI workflow.** Prevents the gap in §1: the checks are real but nothing on a PR runs them, so kit drift is still not blocked at the merge boundary. Owner: EHotwagner/rogue3, `.github/workflows/verify.yml`. Acceptance: a PR that drifts a manifest-pinned kit file fails CI; today it passes. Not done here because the workflow is outside #34's declared touch-set.
2. **Give `scaffold-provenance.json` a way to mark a pin as an invariant rather than a record.** Prevents §4.3 — the gate direction that would have shipped permanently red. Owner: FS-GG scaffold provider. Acceptance: a consumer can partition the pinned paths into "must still match" and "historical" without heuristics, and 12 legitimately-edited driver skills at `d8d0024` fall in the second set.
3. **Project `Effort` (and `Blocked by`) in `fsgg-coord ready --json`.** Prevents the mistake in §4.6: the board has the field, the tool a worker is told to use does not show it, and I concluded the field did not exist. Owner: FS-GG `.github`, `fs.gg.coord.cli`. Acceptance: `ready --json` rows carry every board field the fan-out brief routes on, so a worker never has to query the project API to learn how to route the item they are holding.
4. **Require independent checking of a mid-cycle board write, not only of the report.** Prevents §4.6. Owner: FS-GG `.github`, the fan-out brief and `pnext-item`. Acceptance: the brief names the class — issues filed, diagnoses posted to a sibling's item — and states the minimum check, e.g. that a claim about when a touch-set changed is checked against the body edit history rather than current state.
5. **State the change-cost of a `file:` locator where authors choose locators.** Owner: FS-GG scaffold provider, `fs-gg-feedback-report`. Acceptance: the locator section says a `file:` binding pins the file until the audit is rebound or excused, and names `command:` as the deliberate alternative for files under active development.
6. **Have the skill manifest name the mirrored paths it pins, not only the `.agents/` source.** Prevents the hole in §4.2 at its origin rather than in each consumer. Owner: FS-GG scaffold provider, `skill-manifest.json` schema. Acceptance: a consumer can enumerate every materialized path a pin covers — today all 95 `.claude/skills/**` mirrors have to be inferred by string surgery on the `.agents/` path, which this cycle's `TemplateDrift` does and which will break the first time the mirror layout changes.
7. **Report per-file binding counts in the checker's summary.** Makes the §7 concentration visible before it bites. Owner: EHotwagner/rogue3, `scripts/check-audit-bindings.py`. Acceptance: the summary prints the bindings-per-file distribution and the ledger entry count, using the checker's own deduplicated metric and labelling it as such, so `build.fsx` reads as 4 and not as this report's raw 7.

## §12 Development-surface coverage

| Surface | Status | Evidence and result |
|---|---|---|
| scaffolding | exercised | Both skill manifests and all four sha256-bearing provenance sets recomputed; 12 already-stale driver pins found and the drift gate scoped around them (§4.3). A materialized kit file was mutated to plant §4.1's violation. |
| onboarding-guidance | partial | The brief's warnings were correct and load-bearing: #38 blocked exactly as flagged, #26 was avoided by not running `Verify`, and widening before finishing prevented a third DRIFT. One gap: nothing in the brief governs mid-cycle board writes, which is where both of my published errors went (§4.6). |
| skills | partial | `fs-gg-feedback-report` invoked end to end. `fs-gg-testing`, `fs-gg-sdd-*`, `pnext-item` relevant but not invoked (§9). No skill covers the audit-binding gate. |
| sdd-authoring | not-exercised | Routed to direct implementation on the board's `Effort: M`. Declined, not skipped. |
| implementation-apis | not-exercised | No product source touched; the item's surface is a build script. |
| dependencies-build | exercised | `build.fsx` rewritten: two real checks, `Dev` dropped from the gate list, a new `SelfTest` target. All targets run; help banner updated and still satisfies the five phrases `GovernanceTests` pins. Neither new target is wired into a CI workflow (§1). |
| testing | exercised | 30 self-test cases; 20 detection-only mutations, all killed by a named failing case with no survivors, after a first sweep whose four crash-kills were rewritten (§4.7); 228 product tests unchanged and green. |
| evidence | exercised | 204 raw `file:` occurrences over 112 files reconciled against the checker's 188 bindings; the excuse ledger's non-convergence reproduced two ways; one published claim falsified and corrected, one filed issue retracted (§4.5, §4.6). |
| runtime-playtest | not-exercised | No runtime behaviour changed. |
| performance | not-exercised | No simulation or frame-budget surface touched; planning gate deliberately not invoked. |
| documentation | exercised | Three doc-versus-behaviour contradictions recorded, one of them resolved in this change (§6). |
| packaging-upgrade | not-exercised | No package pin changed; pins read for provenance only. |
| worker-git-pr | exercised | Claim, two widens (the second reporting a live collision and auto-notifying both holders), `overlap`, `batch --explain`, six `say` messages including two retractions, one issue filed and closed, merge order agreed with two sibling workers (§2, §4.6). |

---
feedbackSchema: 2
date: 2026-08-02
workspace: Rogue3
cycle: item-46-templatedrift-kit-coverage
lane: none
toolVersion: 0.32.0
commit: 2487d41
---

# Rogue3 feedback — item #46, a drift gate that covered a third of the kit, and two facts the item itself got wrong

## §1 Provenance and confidence

- **activation:** active
- **phases:** onboarding-first-build, lifecycle-authoring-or-not-used, implementation-test-evidence, verify-ship-pr
- **material events:** 10
- **zero-event reason:** n/a
- Checkpoints: `feedback/checkpoints/item-46-templatedrift-kit-coverage.jsonl`, 10 events, first at `2026-08-02T09:28:34Z`. Untracked while authoring; committed with this report.
- Scaffold provenance `FS.GG.SDD.Artifacts/0.32.0`, provider `rendering`, provider contract 1.1.0, template `fs-gg-ui`, profile `game`, design system `wcag`, lifecycle `sdd`. Package pins: FS.GG.UI 0.21.1, FS.GG.Game 0.12.0, FS.GG.Audio 0.5.0, FS.GG.Contracts 7.5.2.
- Cycle boundary: board item #46, worker `wren-bbe8`, claimed at `09:26Z` with `take` exit 0. Three concurrent item workers (`rook-c2de` on #47, `tern-85a0` on #52). `lane:` is the SDD lifecycle lane, which is `none`. Branched from `origin/main` at `09895b9`.
- **What shipped.** `build.fsx` and a new `scripts/kit-pins.json`. `TemplateDrift` enumerates both kit trees and reports every file no pin covers; the ledger pins the 63 sources the generated manifest does not name; a `KitPins` target regenerates it. Coverage went **64 of 190 → 190 of 190** and `SelfTest` **77 → 111** cases. The `dotnet test` suite is **unchanged by this cycle** and green: 235 at the `09895b9` baseline, 253 after rebasing onto #47's merge (`95b1170`), with none of that delta mine — this cycle adds no `dotnet test` case, only `SelfTest` cases.
- **Two facts this item asserts are false, and I repeated both in code before a critic caught them.** #46 says no audit binds either skill manifest and that its evasion route 2 is green on every gate. Both went stale when #45's and #40's own audits merged. §4.2 has the measurement; a correction is published on #46 as `issuecomment-5156999321`. This is recorded as the cycle's headline finding rather than a footnote, because the false claim was the *premise of the design argument* and it survived a green `SelfTest`, a green `TemplateDrift` and a green test suite. No gate in this repository can detect a false claim in a comment, which is the durable lesson.
- **Lifecycle authoring was not used.** One build script and one data ledger, no product behaviour, no `work/<id>/` artifacts. Recorded as exercised-and-declined.
- **The performance-first planning gate was not invoked, deliberately.** Nothing enters `update`/`view`; no frame-budget surface moves.
- **Scope of a green `TemplateDrift`, stated so it is not over-read.** It is a target a human runs. `.github/workflows/` holds two workflows; neither invokes the FAKE `Verify` target, so `TemplateDrift` does not itself run on a pull request. #52 is landing a step that runs `SelfTest` in CI, and `SelfTest` asserts `TemplateDrift` is clean on this repository — so after #52 merges, this check reaches CI *through* `SelfTest`, not on its own.
- **The 34 new `SelfTest` cases are cases, not gates.** `rogue3#57`, filed this session by the #52 worker, shows that deleting one line (`failures <- failures + 1`) makes the whole target print `FAIL` and exit 0, and that the target-dispatch layer — including my new `runTemplateDrift` and `KitPins` arms — is exercised by nothing. §4.5. Not re-reported, not fixed here, and explicitly not claimed as strength.
- Confidence limits. **First**, "190" is files on disk under the two kit trees; `.codex/skills/` and `.specify/` do not exist in this product, so the total really is two roots and not the three `docs/scaffold-map.md` describes. **Second**, the coverage figures say nothing about whether an unpinned file *had* drifted, only that nothing would have reported it — the same limit `2026-08-02-Rogue3-10.md` §4.4 records. **Third**, the "18 stale provenance pins" in §4.2 is my own recomputation at `09895b9` and, unlike the 12 it replaces, is deliberately not written into the code as a constant, because it grows with every local driver-skill edit. **Fourth**, my brief asserted the feedback validator corpus is 3 pass / 20 fail; an independent run measured **3 pass / 21 fail over 24** reports. I did not re-run it myself and cite it only as a corrected second-hand figure. **Fifth**, `SelfTest` timing (1.67 s warm) is a single measurement on this host, not a benchmark. **Sixth**, three numbers in the first draft of this report were wrong and were corrected by critics, not by me: the dropped-input count in §4.6 (10, actually 6), the passing M7 route name in §4.8 (`menu-root`, actually `main-menu`, published into two issues before correction), and the new-case count (19, actually 34 by the end). Every one was transcribed rather than re-read off the artifact. Treat any number here that is not accompanied by a command as weaker than one that is. **Seventh**, §4.8's latency comparison was not like-for-like in the first draft and the surviving argument is the budget comparison, which is host-independent; the re-measured deltas are single-host. **Eighth**, the mutation results in §4.9 are 35 mutants from one critic plus 24 of mine, not an exhaustive sweep, and two of mine were equivalent mutants I initially misread as survivors — so "0 survivors" means "none among 59 attempted", not "none exist".

## §2 What worked

- **Planting the evasion routes against the REAL tree, not only fixtures, is what made the claims falsifiable.** A six-route battery (`route1`, `route1-mirror`, `route2`, inject, oracle-edit, delete) plus a pristine control runs in one script against the working tree and restores it with `git checkout`. Every route exits 1 with a named file; the control exits 0. That script is also what proved #46's route 2 was *already* guarded at `09895b9`, which no amount of reading `build.fsx` would have shown.
- **The coverage rule closed a route it was not designed for.** It was written for route 1 (files nothing names). It closes route 2 as a side effect and more cleanly than a second digest would: deleting an entry from both manifests does not hide a mutated file, it *strips the file of its only pin* and lands it in the enumeration. Two mechanisms fire on that tree, independently.
- **The check earned itself on `main` within hours, on a divergence nothing else read.** PR #66 changed a kit source without mirroring it; the two trees sat seven lines apart with every gate green, and the new mirror pass reported it on first contact after a rebase (§4.11). That is a measured recurrence of the exact fault #46 describes, not a constructed one.
- **Byte-identical mirrors made the second half of the kit free.** All 95 pairs are byte-identical, so pinning 63 sources and holding each mirror to its source's digest covers 126 files with 63 new pins. Verified adversarially rather than assumed: appending one byte to all 95 `.claude` files produces 95 distinct `mirror:` violations.
- **`#26`'s evidence-graph publication rule earned itself on an unrelated cycle.** `Verify` sensed **6** fewer inputs than the committed graph, refused to publish, restored the previous bytes and named every one. Before #26 this cycle would have committed a graph asserting that evidence disappeared. §4.6 — including the fact that I first recorded that number as 10 and a critic caught it.
- **`KitPins` is deterministic and idempotent**, so the remedy the gate prints leaves a clean `git status` when nothing changed — which is what makes "run `KitPins`" safe advice rather than a source of spurious diffs.
- **The coordination primitives again handled a three-way contention without a human**, and the two siblings' `say` messages carried information no artifact could: `tern-85a0` announced that #52 wires `SelfTest` into CI, which is why the one case reading the real kit total counts its denominator independently instead of asserting a literal `190` that a re-materialized kit would red.

## §3 What did not

- The item's own body carries two claims that are false at `09895b9`, and I built a design argument on one of them and wrote it into three code comments before an independent critic checked it (§4.2).
- The gate's **count** and the gate's **checks** were computed from different sets, and it took four rounds to find all four directions of that one fault: a counted-but-unnamed mirror (§4.7), a named-but-counted-as-covered file (§4.9), a manifest name treated as a pin, and then the same thing again for the ledger — found by a second reviewer, in the fix for the first (§4.10). Every round was on a candidate that was green.
- A governance scan intended to stop the evidence workflow *shelling* decommissioned tools is a whole-file substring test, so naming one of those tools in a **code comment** failed `dotnet test` after `SelfTest` and `TemplateDrift` were both green (§4.4).
- `scripts/audit-binding-exceptions.json` forced a mid-flight `widen` that collided with **both** live siblings at once — `rogue3#53`, third wave running (§4.3). I first wrote that up as a novel degree of collision; a sibling report already in the tree records the same shape from the previous wave, which is the more useful fact.
- The gate this cycle strengthens still does not run on a pull request under its own name, and the 34 cases that prove it are hosted by a harness with a known single-line kill (§4.5).
- A hostile-code critic left 8 of 35 mutants alive against a green 98-case suite, including two that passed an assertion the fixture's own filename made unfalsifiable (§4.9).
- I published two wrong numbers — a dropped-input count and a route name — one of them into an issue I had filed myself an hour earlier (§4.6, §4.8).
- Rebasing onto `main` turned `Verify` red for reasons that are not this cycle's, and the same gap that hid it — nothing on a pull request runs `Verify` — is the gap that made this whole item necessary (§4.8).

## §4 Findings

#### §4.1 `TemplateDrift` pinned 64 of 190 kit files and enumerated none of them, so an unpinned kit file could only be found by looking for it

- **Kind:** quality-gap
- **Impact:** 63 of the 95 files under `.agents/skills` — every `fs-gg-sdd-*` skill, `check-board`, `cross-repo-coordination`, `intra-repo-parallel-work`, `pnext-item`, and 7 of `work-board`'s 8 files — were pinned by nothing in the repository, and so were their 63 mirrors. The gate's output said nothing about them, so a green `TemplateDrift` read as "the kit is intact" when it meant "the 32 files a generated manifest happens to name are intact". A file *added* to either tree was invisible to every check, because nothing enumerated the trees at all.
- **Expected:** A digest gate either covers the kit it guards or reports what it does not cover.
- **Observed:** At `09895b9` the check reached only manifest `resolvablePath` entries: 32 materialized sources, plus the 32 mirrors the mirror pass derives from them — 64 of 190. Measured on a `git archive 09895b9` export: appending one line to `.agents/skills/work-board/references/deep-detail.md` leaves `check-audit-bindings.py` at exit 0 and every other gate green. Closed here by taking #46's second acceptance option in its strong form: `TemplateDrift` now enumerates both trees and emits a `coverage:` line naming any file no pin covers, and `scripts/kit-pins.json` pins the 63-file complement, so the uncovered set is both *reported* and *empty*. The gate now prints `TemplateDrift: 190 of 190 kit files pinned — 32 source(s) by the generated manifest, 63 by scripts/kit-pins.json, plus 95 mirror(s); 0 uncovered` on every run, green or red. Verified adversarially: appending a byte to all 95 mirrors yields 95 distinct `mirror:` violations; to all 95 sources yields 32 + 32 + 63 lines naming every source. The upstream fix — widening the generated manifest — is not available here, because the manifest is generated and a local widening would not survive re-emission.
- **Evidence:** command:./fake.sh build -t TemplateDrift; command:./fake.sh build -t SelfTest; file:scripts/kit-pins.json; file:.agents/skills/skill-manifest.json; file:build.fsx; issue:EHotwagner/rogue3#46
- **Version:** `build.fsx` at `09895b9`, fixed on branch `item/46-templatedrift-kit-coverage`; manifest emitted by FS.GG.SDD.Artifacts 0.32.0.
- **Owner:** FS-GG scaffold provider — `skill-manifest.json` should pin the whole kit it materializes; EHotwagner/rogue3 `build.fsx` for the local enumeration, which is what shipped.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-10.md` §4.2 measured the pre-#45 state (25 of 32 guarded by nothing) and §4.4 measured the post-#45 state (64 of 190), filing issue:EHotwagner/rogue3#46. New here: the uncovered set is enumerated by the gate rather than counted in a report, and injection into either tree is detected for the first time.
- **Avoidable cost:** none to this cycle; the durable cost is however long 126 kit files were unguarded.
- **Disposition:** product fix

#### §4.2 Two facts stated in issue #46 are false at `09895b9`, and both survived into code comments behind a fully green gate

- **Kind:** defect
- **Impact:** #46's body says nothing pins either skill manifest and that its evasion route 2 is green on every gate. I used the first as the premise of the design argument for how the new ledger is pinned, and wrote both into three comments in `build.fsx`. `SelfTest` (95 cases), `TemplateDrift` and `dotnet test` (235 at the time) were all green on that candidate — no gate in this repository can detect a false claim in a comment. A reviewer reading the merged file would have inherited both.
- **Expected:** A measurement quoted in an issue body is either true at the commit the work branches from, or marked as of a commit.
- **Observed:** `git grep -l skill-manifest.json 09895b9 -- feedback/audits/` returns **two** files, not empty: `feedback/audits/2026-08-02-Rogue3-10.audit.json` and `-11.audit.json`, both citing `file:.agents/skills/skill-manifest.json` with a `sha256`. The `-11` binding is fresh and enforced today. Consequently route 2 is **not** green: on a `git archive 09895b9` export, `check-audit-bindings.py` exits 0 on the pristine tree, **0** for route 1 (append to `work-board/references/deep-detail.md`) and **1** for route 2 (mutate `fs-gg-collision/SKILL.md` and delete its entry from both manifests). #46 was correct when filed and went stale when #45's and #40's own audits merged — `feedback/2026-08-02-Rogue3-11.md` records #40 re-pinning the manifest and staling `-10.audit.json`, which is the same event from the other side. Separately, the "12 stale `.fsgg/scaffold-provenance.json` pins" figure, which my worker brief told me was established and not to re-prove, is **18** of the 110 sha-bearing entries at `09895b9` (`producedPaths` 4, `mirroredPaths` 4, `driverPaths` 10; every pinned file present, so all 18 are content drift). Route 2's existing guard is *incidental* — a by-product of two cycles citing the manifest as evidence, the same "guarded by accident" shape §4.2 of `-10` records for 7 of 32 files — it lives only in CI, `--grandfather` clears it, and **no audit binds `.claude/skills/skill-manifest.json`**. Corrected in three comments, published on the issue, and the design argument restated as the condition it actually is. **A third absence claim in the same area, this one mine, was also wrong**: I wrote that this cycle adds the first pin on `.claude/skills/skill-manifest.json`. `.fsgg/scaffold-provenance.json` pins both manifests (`producedPaths`, `mirroredPaths`) at `7bf2f301c0a1` while both files are `a9e86b4ec1b6` — stale, and read by nothing, but present. The defensible claim is "the first pin that anything checks", and that is what the comment now says. Three claims of absence in one area, three wrong on inspection, is the finding.
- **Evidence:** command:git grep -l skill-manifest.json 09895b9 -- feedback/audits/; command:python3 scripts/check-audit-bindings.py; file:.fsgg/scaffold-provenance.json; file:.claude/skills/skill-manifest.json; file:feedback/audits/2026-08-02-Rogue3-11.audit.json; issue:EHotwagner/rogue3#46
- **Version:** measured at `09895b9`; #46 filed against the tree as it stood before #45 and #40 merged.
- **Owner:** EHotwagner/rogue3 — issue bodies and worker briefs that restate a dated measurement as a durable fact; and the filer convention, which has no "as of `<commit>`" marker for a measured claim.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-10.md` §4.6 records the same author-side failure three times in one cycle (a refuted scheduler defect, a false absence claim, a finding built on the wrong project). New here: the false claims originate in the *issue*, not the worker, and my brief propagated one of them a third time.
- **Avoidable cost:** three code comments rewritten and one design justification restated after the code was already green; roughly one review round.
- **Disposition:** existing issue — correction published on issue:EHotwagner/rogue3#46 (`issuecomment-5156999321`) rather than filed separately, since the item is the artifact carrying the error

#### §4.3 `scripts/audit-binding-exceptions.json` collided with BOTH live siblings at once — #53, third consecutive wave

- **Kind:** orchestration
- **Impact:** `widen` returned exit 6 naming two collisions in one call. All three concurrent workers again had to sequence a shared file by hand-negotiated folklore, and none of the three items declares it.
- **Expected:** The scheduler refuses a wave whose members will contend, or the contended path is declarable.
- **Observed:** Editing `build.fsx` staled 5 `file:build.fsx` bindings across `feedback/audits/2026-08-02-Rogue3{,-4,-7,-11,-12}.audit.json` (7 by the time the branch was rebased), whose only non-destructive remedy writes the one shared ledger. `scripts/fsgg-coord widen rogue3#46 --paths scripts/audit-binding-exceptions.json …` reported `OVERLAP` against `rogue3#47` (`rook-c2de`) *and* `rogue3#52` (`tern-85a0`) and notified both. **I first wrote this up as the first simultaneous three-way collision; that is false and a critic caught it against an artifact in my own tree.** `feedback/2026-08-02-Rogue3-13.md` §4.5 records #47's single `widen` returning `overlap` against **both** #26 and #40 in the previous wave, with 20 stale bindings across 10 audits — a larger instance of the same event. A fourth worker (`curlew-cd9c` on #55) also widened onto the ledger at `10:11Z`, so the session's contention is four-way, not three-way. Nothing about this instance is novel; that is the point. The agreed workaround held again — entries appended last, immediately before pushing, rebase and re-append behind whoever merges first — and both siblings independently volunteered the same protocol before being asked. It remains folklore the protocol does not enforce, and it costs one serialization point per wave.
- **Evidence:** command:python3 scripts/check-audit-bindings.py; issue:EHotwagner/rogue3#46; issue:EHotwagner/rogue3#53; issue:EHotwagner/rogue3#41
- **Version:** `fsgg-coord` as at `09895b9`; ledger at 72+ entries and growing monotonically.
- **Owner:** EHotwagner/rogue3 — `scripts/check-audit-bindings.py` ledger shape (#53's option 1, one file per cycle) or the board's `Paths:` derivation (#53's option 2).
- **Note on evidence:** the `widen` invocation itself is deliberately not cited. It is state-changing, notifies live workers and is not safely re-runnable, so it cannot be verified by a reader — the same choice `feedback/2026-08-02-Rogue3-11.md` records for the same command. The durable evidence is the coordination trail on issue #46 and the binding state the checker reports.
- **Recurrence:** seen again — issue:EHotwagner/rogue3#53 filed for the second wave, itself distinguishing this from the closed-invalid issue:EHotwagner/rogue3#41, and `feedback/2026-08-02-Rogue3-13.md` §4.5 records the same contention from the other side in the previous wave. **Nothing new in kind or degree**; the value of this instance is that it is the third consecutive unremarkable recurrence of a filed, unfixed defect.
- **Avoidable cost:** one mid-flight `widen`, two sibling notifications, two coordination messages, and a merge order that cannot be parallelised.
- **Disposition:** existing issue

#### §4.4 A governance scan meant to stop the evidence workflow *shelling* a decommissioned tool fails on a code comment that merely names it

- **Kind:** quality-gap
- **Impact:** `dotnet test` went red for a documentation change, after `SelfTest` and `TemplateDrift` were both green. The practical effect is worse than the delay: the comment that had to be reworded was a *verification instruction* telling the reader how to check a claim the comment makes, and it had to be shipped with its interpreter prefix removed and a parenthetical explaining why — so a scan protecting against shell-outs degraded a runnable instruction into an almost-runnable one.
- **Expected:** A check named "generated evidence graph and audit do not shell the decommissioned scripts" fails when the evidence workflow shells one, not when any line of the file contains its name.
- **Observed:** Reproduce by inserting the token `python3` into any comment anywhere in `build.fsx` and running `dotnet test -c Release`; the tree as it ships is green, because the token was removed, so the failure is not reachable from the shipped state. `tests/Rogue3.Tests/GovernanceTests.fs:438` scans the whole of `build.fsx` for the substrings `run-audit.sh`, `compute-task-graph.py`, `python3` and `ProcessStartInfo("bash"`. Writing `python3 scripts/check-audit-bindings.py --json | grep kit-pins` inside a comment — advice about a *different* tool that is not shelled anywhere — fails it with `generated evidence workflow excludes the decommissioned python3. Actual value was true but had expected it to be false.` The token cannot appear in prose, a filename, or a URL. A narrower scan over `runProcess`/`ProcessStartInfo` call sites, or over the evidence-workflow region the test names, would keep the guarantee and lose the false positive. Worked around by dropping the prefix; the gate was not weakened, and this is deliberately not proposed as a change to the test inside this item's touch-set.
- **Evidence:** file:tests/Rogue3.Tests/GovernanceTests.fs; command:inserting the token python3 into a COMMENT in build.fsx makes dotnet test -c Release fail `generated evidence graph and audit do not shell the decommissioned scripts`; file:build.fsx
- **Version:** `GovernanceTests.fs` at `09895b9`, unchanged by this cycle.
- **Owner:** EHotwagner/rogue3 — `tests/Rogue3.Tests/GovernanceTests.fs`, the forbidden-token scan's scope.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-12.md` §8 records the identical class at `GovernanceTests.fs:453`, where `File.WriteAllBytes` in the evidence-graph restore tripped a scan "whose stated subject is the *redirected log writer*", and calls it "a blunt whole-file substring ban". That cycle recorded it as friction because its forced redesign was an improvement; here the forced redesign degrades a verification instruction, so the same mechanism is worth a finding rather than a line. I originally wrote "new"; a critic falsified it against a report already in this tree.
- **Avoidable cost:** one failed `dotnet test` run and one reworded comment; roughly one build cycle.
- **Disposition:** issue

#### §4.5 The 19 new `SelfTest` cases are hosted by a harness one deleted line defeats, and they never reach the target dispatch they protect

- **Kind:** quality-gap
- **Impact:** This item's whole claim to strength is "`SelfTest` plants each violation and requires it to be reported". `rogue3#57` — filed this session by the #52 worker while wiring `SelfTest` into CI — shows 7 of 23 mutants surviving it, including deleting `failures <- failures + 1`, after which the target prints every `FAIL` and exits 0. So the correct reading of "98 cases, 0 failures" is weaker than it looks, and this cycle grows the number of cases resting on that accounting by 34.
- **Expected:** A self-test whose purpose is to prove a gate cannot silently stop checking cannot itself silently stop failing.
- **Observed:** Confirmed by reading #57 rather than re-deriving. Two consequences specific to this item. First, the new cases inherit the single-line kill unchanged. Second, #57 §3 records that the target-dispatch layer is exercised by nothing — and this cycle *added* to that layer: `runTemplateDrift` (which prints the coverage line) and the new `| "KitPins" ->` arm are reached by no case, because `SelfTest` calls the check functions directly. `templateDriftCoverage` itself is covered by four cases, so the line's *content* is asserted; its *installation* in the `TemplateDrift` target is not. Not fixed here: #57 is a filed item with a named owner, its fix changes `runSelfTest`'s failure accounting for every case in the file, and doing that inside #46 would be a second item.
- **Evidence:** issue:EHotwagner/rogue3#57; command:./fake.sh build -t SelfTest; file:build.fsx
- **Version:** `build.fsx` on branch `item/46-templatedrift-kit-coverage`; #57 measured at `bfd1288`.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `runSelfTest` failure accounting and the untested dispatch arms.
- **Recurrence:** first seen issue:EHotwagner/rogue3#57 (this session). New here: the observation that #46 enlarges the exposure, and that its own two new dispatch arms join the untested set.
- **Avoidable cost:** none charged — reading a sibling's issue before claiming strength cost nothing and prevented a false claim in §1.
- **Disposition:** existing issue

#### §4.6 The #26 evidence-graph publication rule prevented a falsified artifact on a cycle that had nothing to do with it

- **Kind:** positive-pattern
- **Impact:** `Verify` on a worktree that does not hold the whole readiness tree would previously have rewritten the tracked `readiness/evidence-graph.md` with a smaller graph and exited 0, and a worker following the documented instructions would have committed an artifact asserting that evidence disappeared.
- **Expected:** n/a — reusable pattern, evaluated for what it prevented.
- **Observed:** On this worktree at the branch point, `./fake.sh build -t Verify` exited 0 and reported `could not sense 6 input(s) that readiness/evidence-graph.md already records`, listed all 6 — every one `(absent from this checkout)`, none `(PRESENT but not sensed)` — restored the previous bytes and left the graph untouched. `git status --porcelain` afterwards showed exactly two modified files, `readiness/performance-evidence.json` and `readiness/m7-ui-performance.json`, restored with one `git checkout`. **I first recorded that count as 10 and a critic falsified it**; both of my runs say 6, an independent set-difference of the graph's 102 sensed entries against disk says 6, and so did the critic's own run. The number was never read off the artifact — it was transcribed from memory of the terminal — which is the same failure mode as §4.2 and §4.8 and is why §3 lists it separately. The generalisable part is the shape, and it survives the correction: the rule compares *sets*, not counts; it refuses rather than fails, so it cannot train anyone to ignore it; and its diagnostic wording is asserted by tests rather than merely printed. Note the `(PRESENT but not sensed)` arm is *tested* but was **not exercised** by this run, so this cycle observed only half of the distinction it credits. The two dirtied artifacts are called "declared exclusions" here because `build.fsx` documents them as such; `feedback/2026-08-02-Rogue3-13.md` §4.6 files that same churn as friction under open issue:EHotwagner/rogue3#56, and that framing is at least as defensible as mine.
- **Evidence:** command:./fake.sh build -t Verify; command:git status --porcelain; file:readiness/evidence-graph.md; file:build.fsx
- **Version:** `build.fsx` at `09895b9` (#26 merged as PR #54).
- **Owner:** EHotwagner/rogue3 — `build.fsx` evidence-graph publication rule.
- **Recurrence:** new as an independent confirmation; the rule itself is issue:EHotwagner/rogue3#26, merged the same session.
- **Avoidable cost:** none — it removed cost.
- **Disposition:** accepted

#### §4.7 The first candidate's coverage counter and coverage enumeration disagreed, understating by omission inside the fix for understating

- **Kind:** defect
- **Impact:** On a tree where an uncovered source had an equally uncovered mirror, the summary line said `2 uncovered` while only **one** `coverage:` line was emitted — the source was named, the mirror only counted. #46's acceptance is that the uncovered set is *visible in the gate's own output*; a file the denominator calls uncovered but no line names is visible only as arithmetic. Found by an independent critic, in the fix for exactly this class of overstatement.
- **Expected:** Every file the coverage counter treats as uncovered is named by a coverage line.
- **Observed:** The mirror pass walks *pinned sources*, so an uncovered source is skipped and its mirror is never reached by any name-emitting pass, while `templateDriftCoverage` correctly declined to count that mirror as covered. Fixed by adding a second arm to the `.claude` enumeration: a mirror whose source exists but is pinned by nothing is now reported as `coverage: <mirror> is pinned by nothing, because its source <source> is pinned by nothing`. Guarded by a case that asserts the *agreement* rather than either side — it counts the emitted `coverage:` lines and requires the summary to report that same number, so the two cannot drift apart again.
- **Evidence:** command:./fake.sh build -t SelfTest; command:./fake.sh build -t TemplateDrift; file:build.fsx
- **Version:** introduced and fixed on branch `item/46-templatedrift-kit-coverage`; the pre-rebase SHAs were `728898d` → `e779533`, which after the rebase onto `95b1170` are `55e0c2a` → `9691659`. Never merged in the defective form.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `templateDriftViolations` mirror enumeration.
- **Recurrence:** new.
- **Avoidable cost:** one critic round; fixed before ship, so no durable cost.
- **Disposition:** product fix

#### §4.8 `Verify` has been red on `main` since `95b1170`, and nothing on a pull request could have noticed

- **Kind:** defect
- **Impact:** The target the help banner tells a developer to run before shipping — and the one that runs `TemplateDrift`, `EvidenceAudit` and the performance evidence — fails on `main`. Every worker who rebases onto it inherits a red gate for a reason unrelated to their work, which is exactly how a gate stops being run. It also cost this cycle its clean ship-readiness claim: §10 can no longer say "green `Verify`" without qualification.
- **Expected:** `./fake.sh build -t Verify` exits 0 on `main`.
- **Observed:** `95b1170`'s sole parent is `09895b9`, so these are adjacent commits and the attribution to PR #59 is a measurement, not the coarse bisect I first called it. Tracked files only, true exit codes: `git archive 09895b9` → `PerformanceEvidence` exit **0**; `git archive 95b1170` → exit **1**, `status=failed m7-ui-performance routes=4`, and `Verify` then dies at `build.fsx:498`. Three of the four M7 routes — `hud-playing`, `run-result`, `stats-charts` — compute a `definitionDigest` that no longer matches the `declaredDefinitionDigest` committed in `readiness/m7-ui-performance.json`, so `authorshipPassed` is false. Only **`main-menu`** still matches; I first published this as `menu-root`, a route that does not exist, in the issue and on #47, and corrected both. Nothing is slow, and the clean evidence for that is the budget rather than a delta: worst observed p95 is `stats-charts` at 0.5219 ms against a `p95BudgetMs` of 16.67, and worst p99 is 0.7353 against 25 — every route roughly 30x inside, and every route `scalePassed`. My first argument here compared a `p95Ms` *committed in the artifact* against a value I measured fresh, which is not like-for-like; re-measured at both commits on one host, only two of the three improved and `hud-playing` was flat. The conclusion holds, the original evidence for it did not. So the failure is purely the authorship acknowledgement the target exists to force. PR #59 was merged with both required checks green, because `verify.yml` runs `dotnet restore` and `dotnet test` and nothing on a pull request invokes the FAKE `Verify` target. Filed at root cause as issue:EHotwagner/rogue3#61 rather than fixed here: re-declaring those digests is an acknowledgement that belongs to whoever owns #47's route changes, and doing it blind inside #46 would be the precise abuse the check is designed to prevent.
- **Evidence:** command:./fake.sh build -t PerformanceEvidence on a clean `git archive` export exits 0 at 09895b9 and 1 at 95b1170; file:readiness/m7-ui-performance.json; issue:EHotwagner/rogue3#52
- **Version:** green at `09895b9`, red at `95b1170` (PR #59, item #47); measured on clean `git archive` exports of both.
- **Owner:** EHotwagner/rogue3 — `readiness/m7-ui-performance.json` declared digests for #47's route changes; and, at root, the pull-request gate that does not run `Verify`.
- **Recurrence:** new as a `main`-is-red event. The *coverage gap* that let it through is not new: `feedback/2026-08-02-Rogue3-10.md` §1 records that nothing on a PR invokes `Verify`, and issue:EHotwagner/rogue3#52 is closing part of it by wiring `SelfTest` — but deliberately not `Verify`, so this class keeps landing.
- **Avoidable cost:** one failed `Verify`, one two-point bisect, one filed issue, and the loss of an unqualified ship-readiness claim; roughly one build cycle plus two clean-export runs.
- **Disposition:** issue

#### §4.9 8 of 35 mutants survived a green 98-case self-test, and two of them survived an assertion the fixture's own filename had made unfalsifiable

- **Kind:** quality-gap
- **Impact:** This is the cycle's sharpest result, because the item's whole deliverable is a *gate*. At the point the sweep ran, `SelfTest` reported 98 cases and 0 failures, `TemplateDrift` was green at 190/190, the suite was green, and a claim-verification critic had already been round. A hostile-code critic then removed detection — never control flow — from the new logic 35 times and 8 of those mutants left `SelfTest` at exit 0. A gate that survives that is not yet a gate.
- **Expected:** Every piece of detection the gate relies on is pinned by a named failing case.
- **Observed:** The eight: a source-coverage predicate excluding `/references/` (every existing source case also had a mirror, so the mirror arm added an hour earlier was *masking* the source arm); dropping dot-prefixed files from the enumeration; widening `kitSourcePrefix` to `.agents/`; dropping its trailing slash; skipping ledger entries the manifest also names; halving the breakdown attribution; never printing the coverage line from the target; and making `KitPins` write an empty ledger. Two causes were structural rather than missing cases — `templateDriftCoverage` recomputed coverage from its own name-membership test instead of deriving it from the enumeration (so a `sha256`-less ledger entry made it print `0 uncovered` while the enumeration named two files), and the `KitPins` target and the test fixture were parallel implementations of the same write, so only the fixture was ever exercised. **The worst single finding is a test that could not fail:** three refusal assertions checked `v.Contains "outside"` while the fixture pinned a file named `outside-the-kit.md`, so the *drift* message satisfied them and two boundary mutants passed. All 8 are now killed by a named case; `SelfTest` is 106. Residual routes the same critic found and this item does not close — symlinked kit roots overstating the 190, the manifest having no path boundary while the ledger does, an unreadable file crashing the gate, and no `.gitattributes` — are filed as issue:EHotwagner/rogue3#62.
- **Evidence:** command:./fake.sh build -t SelfTest; file:build.fsx; issue:EHotwagner/rogue3#62; issue:EHotwagner/rogue3#57
- **Version:** measured against the branch at the pre-rebase equivalent of `e779533`; closed at `9617cd1`.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `TemplateDrift` and its `SelfTest` cases.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-10.md` §2 records the identical experience on the same target one cycle earlier: a self-run 14-mutation sweep reported "no survivors" and an independent sweep immediately found six. Mine repeated it exactly, including the part where my own sweep was the weaker one: two of my twenty mutations were equivalent mutants that still returned `Error`, so they read as survivors and were not.
- **Avoidable cost:** one restructure of `templateDriftCoverage`, one extracted write path, 8 new cases and 3 rewritten assertions — all after the code was green and reviewed once.
- **Disposition:** product fix

#### §4.10 A PR reviewer found the gate treating a manifest NAME as a manifest PIN, so the number lied on a red tree

- **Kind:** defect
- **Impact:** A manifest entry that keeps its `resolvablePath` but loses its `sha256` is named and not pinned. The coverage rule keyed on the *name*, so on such a tree `TemplateDrift` printed `190 of 190 kit files pinned … 0 uncovered` while that source and its mirror were digest-checked by nothing — and a **drifted mirror was never mentioned at all**. That is the overstatement this whole item exists to remove, surviving inside the fix for it, and it is the third distinct instance of it in this one cycle (§4.7 and §4.9 are the other two).
- **Expected:** A file the gate counts as pinned is a file some digest is actually compared against.
- **Observed:** Reproduced on a copy: strip `sha256` from the `fs-gg-collision` entry in both manifests and append a line to `.claude/skills/fs-gg-collision/SKILL.md`. Before: exit 1 with only `skill \`fs-gg-collision\` pins no resolvablePath/sha256` from each manifest, the coverage line reading `190 of 190 … 0 uncovered`, and no mention of the drifted mirror. After: `188 of 190 … 2 uncovered`, with both `.agents/skills/fs-gg-collision/SKILL.md` and its mirror named. **The tree was red in both cases** — the manifest pass reports the malformed entry — so this was never a route to hiding drift behind a green gate; it made the count wrong on a tree that was already failing, which matters because the count is the thing #46 asked to be made trustworthy. Fixed by keying coverage, the mirror arm and the summary's attribution on the digest-bearing set rather than the name set, and by making `computeKitPins` filter on the same set so `KitPins` can remedy what the gate now reports. Three regression cases; all 8 of §4.9's mutants re-checked afterwards and still killed.
- **Evidence:** command:./fake.sh build -t SelfTest; command:./fake.sh build -t TemplateDrift; file:build.fsx
- **Version:** introduced with the coverage rule at the start of this branch; found in review of PR #64 and fixed at `c2cc06d`. Never merged in the defective form.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `templateDriftViolations` coverage predicate and `templateDriftCoverage`.
- **A SECOND reviewer then found the mirror image of it, in the fix.** `byLedger` still keyed on the raw ledger, so a digestless *ledger* entry was attributed to `scripts/kit-pins.json` on the same line that a `coverage:` violation named that file as pinned by nothing. The existing `ledgerNoSha` and `counterVsNames` fixtures build exactly that tree and assert only the COUNT, never the ATTRIBUTION, which is why both passed. Fixed the same way and guarded by two cases that assert the attribution; the same reviewer also found `named` had become dead code while a comment I had just written claimed it had a live consumer, and a doc comment merged onto the wrong binding. Four rounds, four directions of one fault.
- **Recurrence:** new as a specific defect; the third and fourth instances in this cycle of one general fault — the gate's *count* and the gate's *checks* being computed from different sets (§4.7 counted an uncovered mirror without naming it; §4.9 named files the counter called covered; this one counted files nothing checked). §4.7's fix and §4.9's fix each closed one direction and left this one.
- **Avoidable cost:** one review round after two critic rounds and green CI; three cases and a four-line predicate change.
- **Disposition:** product fix

#### §4.11 The fault this item closes recurred on `main` the same day, and the new gate caught it on first contact

- **Kind:** defect
- **Impact:** PR #66 changed `.agents/skills/work-board/references/deep-detail.md` and did not mirror the change into `.claude/`, leaving the two kit trees diverged by seven lines while the PR body asserted they were identical. Every gate passed. That file is one of the 63 sources the generated manifest does not name, so before this cycle **nothing in the repository read it**, and the divergence would have persisted silently until someone happened to diff the trees by hand.
- **Expected:** A tree whose two kit roots disagree does not pass a merge gate that claims to check kit integrity.
- **Observed:** Measured, not argued. Rebasing this branch onto `4fc6993` (post-#66) and running the new check gives exit 1 with `mirror: .claude/skills/work-board/references/deep-detail.md differs from the digest pinned for .agents/skills/work-board/references/deep-detail.md — pinned 609790a062af, found ce84f9bb7c8b`, and independently `diff -rq .agents/skills .claude/skills` names exactly that one file, 388 lines against 381. `SelfTest` fails 1 of 111 on `TemplateDrift is clean on this repository`, so — because #52 wired `SelfTest` into CI — the required check goes red too. Two further facts make this a sharper result than a demonstration. **First, `KitPins` cannot paper over it:** re-pinning fixes the `.agents` side and the mirror pass then holds `.claude` to that pin, which still differs, so the remedy the gate advertises correctly refuses to launder a real divergence. **Second, it dictated a merge order.** This branch could not merge first without landing a red gate on `main`; the mirroring fix (PR #68) had to go first. The author of #66 reported the miss themselves and filed the fix before the gate existed to catch them, which is the honest framing — this cycle did not catch a hidden defect, it turned a defect that had already been noticed by hand into one that a command reports.
- **Evidence:** command:./fake.sh build -t TemplateDrift; command:diff -rq .agents/skills .claude/skills; file:scripts/kit-pins.json
- **Version:** `main` at `4fc6993`; this branch rebased onto it.
- **Owner:** EHotwagner/rogue3 — the two kit trees and whatever mirrors between them; the underlying gap is the one §6 records, that `fsgg-sdd` is the documented "mirror authority" and is invoked by nothing.
- **Recurrence:** new as an instance; it is the concrete realisation of the risk `feedback/2026-08-02-Rogue3-10.md` §4.4 states abstractly — "all 95 files under `.claude/skills/` that mirror an `.agents/` source are named by no manifest at all".
- **Avoidable cost:** none to this cycle beyond one rebase and a merge-order negotiation; the durable cost is however long a silent divergence would otherwise have stood.
- **Disposition:** product fix

## §5 Did not exercise

- **The runtime.** No product code changed; `./fake.sh build -t Run`, rendering, playtest and symbology were not exercised and had no bearing on the item.
- **The SDD lifecycle stages.** `charter`/`specify`/`clarify`/`checklist`/`plan`/`tasks`/`analyze`/`evidence`/`verify`/`ship` were not run — declined for a build-script item, recorded in §1.
- **`GeneratedGuidanceCheck`'s symmetry arm.** As `2026-08-02-Rogue3-10.md` §5 records, it has nothing to exercise on this tree; unchanged here.
- **A profile other than `game`.** `materializes-when` is exercised for `always`, `profile == game` and `profile == sample-pack` in fixtures, but no real re-scaffold under another profile was performed, so the claim that the ledger survives a profile change is untested on a real tree.
- **A kit re-materialization.** The `KitPins` remedy is exercised on fixtures and on deliberate local edits, never against a genuinely regenerated kit from the provider — which is the case that will exercise it in anger.

## §6 Doc-versus-behavior contradictions

- **`docs/scaffold-map.md:225-249` describes a mirror verification this product does not perform.** The doc says the first build "verifies all three roots against the manifest (presence ∧ byte-identity ∧ digest match)", then carves out SDD products: "Products scaffolded through the SDD orchestrator (`lifecycle=sdd`) do not carry the materialize step: there the `fsgg-sdd` CLI is the mirror authority for the non-`.agents` roots". This product is `lifecycle=sdd`; `.specify/` and `.codex/` do not exist, and the `FsGgMaterializeSkillRoots` target in `Directory.Build.props` is conditioned on a script that is absent. But nothing in `build.fsx`, `build.sh`, `fake.sh` or either workflow invokes `fsgg-sdd`, so the named "mirror authority" is never run by any automated path in this repository. The measured consequence is §4.1: before this cycle, 63 of 95 mirrors were verified by nothing at all. The doc is not wrong about ownership; it is silent about the fact that the authority is never invoked, and a reader reasonably concludes the mirrors are checked. Owner: FS-GG scaffold provider — `docs/scaffold-map.md`, the SDD carve-out paragraph.
- **`build.fsx`'s own `#34` scope comment contradicted the code beneath it after this change.** It read "The manifests ARE the oracle, and nothing else in the tree pins them: … no audit binds either" — false when written (§4.2) and doubly false once `scripts/kit-pins.json` pins them. Corrected in this cycle rather than left, and recorded here because the contradiction was *introduced by a merged cycle's comment surviving a later cycle's change*, which no gate detects.

## §7 Workarounds still in the tree

- **`scripts/kit-pins.json` is itself the workaround.** The right fix is upstream: the generated manifest should pin the whole kit it materializes. The ledger duplicates, in a repository-owned file, information the provider already has. Removal condition: `skill-manifest.json` names every file under `.agents/skills/**`, after which `KitPins` produces an empty `pins` array and the file can be deleted. Risk of permanence: the ledger must be re-pinned by hand after every deliberate kit edit, so it accrues a small tax on every cycle that touches a skill — and two of the files it pins (`fs-gg-feedback-report/scripts/feedback-tool.fsx` and `FeedbackReportTool.fs`) are edited by feedback-tooling cycles specifically, which is exactly what `feedback/2026-08-02-Rogue3-11.md` records #40 doing.
- **The ledger's pin lives outside the gate that reads it.** `scripts/kit-pins.json` is pinned only by this cycle's audit citing it, enforced by `audit-bindings.yml`. If a future cycle rebinds or removes that citation, the oracle silently reverts to unpinned and `TemplateDrift` stays green. Removal condition: a check that asserts *some* audit binds the ledger. Deliberately not added here — it would be a fixed point of the same shape the checker already exempts `scripts/audit-binding-exceptions.json` from.
- **The `--grandfather` append-last protocol** (§4.3) remains folklore agreed over messages, in the tree only as ledger entries whose `reason` strings narrate it. Removal condition: #53's option 1 or 2.
- **The interpreter prefix removed from a verification instruction** in `build.fsx`'s `#46` comment and in `scripts/kit-pins.json`'s `note`, to pass the scan in §4.4. Removal condition: that scan is narrowed to call sites. Risk: low, but the instruction is now one token short of runnable, which is a poor thing for a comment whose entire purpose is "verify rather than believe".

## §8 Friction and avoidable cost

- **One mid-flight `widen` and a two-way collision** (§4.3): `widen` exit 6, two auto-notifications, two `say` messages, and a merge order negotiated rather than scheduled. Not retried; the protocol worked, it just is not a protocol.
- **One failed `dotnet test`** caused entirely by a code comment (§4.4). One build cycle, roughly 30 s of command time.
- **One critic round spent on claims rather than code** (§4.2, §4.7): three comments rewritten, one design argument restated, one issue correction published, one enumeration arm added. Zero source-logic defects were found by either critic — every correction was to something *asserted*.
- **Two compile-error retries** on `Seq.ofSeq`, which does not exist; ~1 min. Trivial and recorded only for completeness.
- **No lifecycle reruns, no generated files replaced, no worker restarts, no reverts.** The two `readiness/*.json` measurement artifacts were restored once with a single `git checkout` after `Verify`, as designed.

## §9 Skill value and gaps

Inventory: `.agents/skills/skill-manifest.json`, 33 rows against 52 `SKILL.md` files present — the coverage gap that is this item's subject, and itself a §4.1 finding rather than only an inventory note.

- **Invoked: `fs-gg-feedback-report`.** Drove all seven checkpoints, the report structure, and the audit contract. `validate-checkpoint-state` and `selftest` (39/39) both exercised; the `selftest`-before-trusting-a-verdict instruction is the reason the validator's verdict here is worth quoting.
- **Invoked: `work-board` / `intra-repo-parallel-work` (as protocol, via `fsgg-coord`).** `take`, `widen`, `overlap`, `say`, `inbox`, `batch` all used. `overlap … --active` reporting DISJOINT *before* the widen and OVERLAP after is what made the collision legible as #53 rather than as bad luck.
- **Relevant, not invoked: the ten `fs-gg-sdd-*` stage skills.** Declined for a build-script item (§1). Worth noting that all sixteen of them are among the 63 files this cycle pins — the skills least likely to be invoked by a product cycle were exactly the ones nothing guarded.
- **Wanted and absent: a skill for the merge-gate/governance surface itself — and this is a RE-REQUEST, not a new one.** `feedback/2026-08-02-Rogue3-12.md` §9 asked for exactly this and named the exact cost it would have avoided: "governance scans read `build.fsx` as text and ban substrings whole-file … A short skill would have prevented the `File.WriteAllBytes` collision outright." One cycle later the same absence cost me the same collision on a different token (§4.4). `build.fsx` carries `TemplateDrift`, `GeneratedGuidanceCheck`, `SelfTest`, `EvidenceGraph`, `EvidenceAudit`, `KitPins` and the audit-binding contract, and five items this session (#34, #26, #46, #52, #57) have landed in it. The doctrine is still transmitted by comment archaeology.
- **Misleading guidance: none from a skill.** The misleading inputs this cycle were an *issue body* and a *worker brief* (§4.2), neither of which is a skill.
- **Overlap: none observed** between the skills actually invoked.

## §10 Outcome markers

- **Time to first build / baseline:** first command of the cycle; `./fake.sh build -t SelfTest` exit 0, 77 cases, on `origin/main` at `09895b9`, with no retry and no setup step. The gate this item extends was provably alive before any edit.
- **First meaningful test:** `SelfTest` 95 cases, 0 failures, first run after the implementation compiled — the first commit's 18 new cases passed without iteration. The final count is 106; the 11 cases beyond that first commit are all critic-driven and are the honest measure of how much the first green run was worth. Two compile errors preceded it (§8).
- **First green verification:** `./fake.sh build -t Verify` exit 0 **at the branch point**, including `TemplateDrift 190 of 190 … 0 uncovered`, `EvidenceAudit`, the full test suite and the performance evidence workloads. **After rebasing onto `95b1170` it is red**, in `PerformanceEvidence` and for reasons that are not this cycle's — see §4.8. So the honest marker is: green `Verify` on `09895b9` + this branch's commits; red `Verify` on `95b1170` + the same commits, with the identical failure on a clean export of `95b1170` alone.
- **First render / playable state:** not applicable; no product code changed.
- **Ship readiness:** `SelfTest` 111 cases 0 failures; `TemplateDrift` exit 0 at 190/190; `dotnet test -c Release` 253/253 after rebase (235/235 before it, the delta being #47's); six planted evasion routes each exit 1 with a named file and a pristine control exits 0; `check-audit-bindings.py --selftest` and `feedback-tool.fsx -- selftest` (39/39) both green.
- **Merge:** recorded at §12/PR rather than asserted here.
- **Supporting measures, not substitutes for elapsed time:** `SelfTest` 1.67 s warm (single measurement, this host) — relevant because #52 is making it a per-pull-request cost. Test suite 4 s. Coverage: 64/190 → 190/190. Cases: 77 → 111. Ledger: 63 pins, deterministic and idempotent across repeated `KitPins` runs.

## §11 Falsifiable improvements

1. **Pin the whole kit upstream.** Owner: FS-GG scaffold provider, `skill-manifest.json` emission. Would have prevented §4.1 entirely and made §7's ledger unnecessary. Acceptance: on a freshly scaffolded `lifecycle=sdd` product, every file under `.agents/skills/**` appears in `skill-manifest.json`, and `./fake.sh build -t KitPins` produces an empty `pins` array.
2. **Give a measured claim in an issue body an "as of `<commit>`" marker, and have the filer emit one.** Owner: EHotwagner/rogue3 filer conventions / `padd-item`. Would have prevented §4.2 — both stale facts were undated measurements read as durable. Acceptance: an issue whose body states a measurement carries the commit it was measured at, and a worker can re-run it; #46's two claims would have been marked "as of `d8d0024`" and re-checked rather than repeated.
3. **Narrow the forbidden-token scan to call sites.** Owner: EHotwagner/rogue3, `tests/Rogue3.Tests/GovernanceTests.fs:438`. Would have prevented §4.4. Acceptance: `build.fsx` containing `python3` inside a comment passes, while a `runProcess`/`ProcessStartInfo` invoking it fails; both directions asserted by a case.
4. **Make `SelfTest`'s failure count underivable from one line.** Owner: EHotwagner/rogue3, `build.fsx` `runSelfTest` — already filed as issue:EHotwagner/rogue3#57, restated here only because §4.5 shows this cycle enlarging the exposure. Acceptance: #57's own — no single deletion leaves the target printing `FAIL` and exiting 0.
5. **Make the audit-binding excuse ledger per-cycle.** Owner: EHotwagner/rogue3, `scripts/check-audit-bindings.py` — issue:EHotwagner/rogue3#53 option 1. Would have prevented §4.3 for all three workers. Acceptance: two concurrent items that both edit audit-bound source each land their excuse without touching a path the other declares.
6. **Require every `expect` in `SelfTest` to be falsifiable by construction, or sweep for unfalsifiable assertions.** Owner: EHotwagner/rogue3, `build.fsx` `runSelfTest`. Would have prevented the worst item in §4.9 — three assertions that checked `v.Contains "outside"` against a fixture named `outside-the-kit.md`, so the *drift* message satisfied them and two boundary mutants passed a test that could not fail. Acceptance: a check (or a documented convention plus a sweep) under which no `expect` passes on a fixture whose own paths supply the asserted substring; concretely, renaming any fixture file must not be able to turn a passing case green-by-accident.

7. **Compute a gate's reported coverage from its own violation list, never from a parallel recomputation.** Owner: EHotwagner/rogue3, `build.fsx` `templateDriftCoverage` — done here, but stated as a general rule because it took four rounds to apply it completely (§4.7, §4.9, §4.10 twice). Acceptance: no summary figure in a gate is derived from a set that the gate's own checks do not use; concretely, deleting any validation from the check path must change the reported number in the same direction.

8. **Assert that some audit binds `scripts/kit-pins.json`.** Owner: EHotwagner/rogue3, `build.fsx`. Would close §7's second workaround — today the oracle's pin can be removed silently by a later cycle rebinding its audit. Acceptance: removing every `file:scripts/kit-pins.json` citation from `feedback/audits/**` makes some check fail; deliberately not attempted here because a naive version reintroduces the fixed point `check-audit-bindings.py` already exempts one path from.

## §12 Development-surface coverage

| Surface | Status | Evidence and result |
|---|---|---|
| scaffolding | partial | No scaffold or regeneration ran. `.fsgg/scaffold-provenance.json` read for §1 provenance and recomputed in full for §4.2 (110 sha-bearing entries, 18 stale, 0 missing files); `docs/scaffold-map.md` read and found to describe a three-root materialize step this `lifecycle=sdd` product does not carry (§6). The generated `skill-manifest.json` is the item's subject: it names 32 of the 95 files it materializes. |
| onboarding-guidance | partial | `CLAUDE.md` is a one-line pointer to generated guidance; the load-bearing knowledge for this item was in `build.fsx` comments and two prior reports, not in onboarding. §9 records the absent merge-gate/governance skill. |
| skills | exercised | `fs-gg-feedback-report` drove 7 checkpoints, the report and the audit; `fsgg-coord` drove take/widen/overlap/say/inbox/batch through a real three-way collision. Manifest inventoried in §9; 16 `fs-gg-sdd-*` skills declined with reasons. |
| sdd-authoring | not-exercised | Exercised-and-declined: one build script and one data ledger, no product behaviour, no `work/<id>/` artifacts. Recorded as a routing decision in §1, not an omission. |
| implementation-apis | not-exercised | No product API touched. The implementation is `System.IO`/`System.Text.Json` inside `build.fsx`; no FS.GG.UI, Game, Audio or Contracts surface entered the change. |
| dependencies-build | exercised | `./fake.sh build` targets `SelfTest`, `TemplateDrift`, `KitPins`, `Verify` all driven; new `KitPins` target added and proved deterministic and idempotent; two `Seq.ofSeq` compile errors and their fix (§8). |
| testing | exercised | `SelfTest` 77 → 111 cases, 0 failures, of which 8 exist only because an independent mutation sweep found the corresponding detection removable from a suite that was already green (§4.9) and 5 because two PR reviewers found the count and the checks keyed on different sets, first for the manifest and then for the ledger (§4.10); 34 new cases covering both evasion routes, injection into either tree, deletion, the oracle edit, four malformed-ledger states, path escape, the `KitPins` remedy, and counter/enumeration agreement. `dotnet test` suite unchanged by this cycle and green (235 at baseline, 253 after rebasing onto #47), with one genuine red from a governance scan (§4.4). Limitation stated rather than hidden: §4.5. |
| evidence | exercised | Six-route evasion battery against the real tree with a pristine control; independent recomputation of the mirror arithmetic and the provenance staleness; `check-audit-bindings.py --json` parsed directly; `feedback-tool.fsx -- selftest` 39/39 run before trusting the validator's verdict; #26's publication rule observed refusing a smaller graph (§4.6). |
| runtime-playtest | not-exercised | No product code changed; no frame, scene or input path touched. Correctly out of scope for a build-gate item. |
| performance | partial | At the branch point `Verify` ran the Release performance-evidence workloads (7, exit 0) and the two measurement artifacts were dirtied and restored as designed. **After rebasing onto `95b1170` that step exits 1** for reasons that are not this cycle's, which is §4.8 and issue #61 — so the row records a green run at `09895b9` and a red one on current `main`, not a green gate. No performance-first planning gate invoked, deliberately (§1). `SelfTest` timed at 1.67 s warm because #52 is making it a per-PR cost. |
| documentation | exercised | Three false code comments corrected against measurement (§4.2); one stale inherited figure replaced with a re-measurement and deliberately de-constantised; a doc-vs-behaviour contradiction recorded (§6); a correction published on issue #46; the help banner extended for `KitPins` and the coverage line. |
| packaging-upgrade | not-exercised | No package pin moved; no upgrade or template refresh attempted. `Directory.Packages.props` read for §1 only. |
| worker-git-pr | exercised | `take` exit 0, `widen` exit 6 with a two-way collision, `overlap --active` before and after, two `say` messages and an `inbox` read that changed the implementation (the independent denominator, §2). Branch, four commits, PR, review and merge; ledger entries appended last per the agreed protocol (§4.3). |

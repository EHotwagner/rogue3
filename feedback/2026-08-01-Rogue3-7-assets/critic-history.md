# M6 fresh-critic history

## Candidate 4fc6e2c — round 1

- Correctness critic: supported. Non-blocking note that expiry was not asserted exactly; repair added the lifetime-boundary assertion.
- Visual critic: rejected. Blocking findings were incomplete/legacy inventory with side-projected false observation, non-faithful neutral grammar galleries, incorrect/default-east facing, and Maggot/Fly identity relying only on overloaded size.
- Performance critic: rejected. Blocking findings were incomplete visual/cost coverage, direct 600-particle fixture seeding that bypassed the cap, unrelated SDD evidence artifacts, and an enemy-symbol scale counter derived from model kinds rather than rendered tokens.

## Repair disposition

- Replaced the legacy subject set with 30 production dungeon elements. The audited observations now originate in `Render.renderedElements`, the same production composition consumed by `View.view`.
- Added bosses, shop offers, all obstacles, all visible pickups, both projectile sides, shadows, player, particle, background, and HUD bindings/cost drivers.
- Mapped live directional state to north-zero `Heading`; added a four-level `Speed` channel so all eight required enemy tuples are unique without relying on overloaded Size.
- Replaced neutral galleries with identical hard-content Token/Badge/Ring production frames and a 3840×720 same-frame contact sheet.
- The measured maximum route now sends `SpawnM6Particles(650, ...)` through production update on every frame and exact-gates the retained 600; enemy-symbol scale derives from `Render.enemyTokens`.
- Rebound SDD obligations to subject-specific code, TRX, raster, performance, contract, migration, and generated-view artifacts.

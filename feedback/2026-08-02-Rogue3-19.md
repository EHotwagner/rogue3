---
feedbackSchema: 2
date: 2026-08-02
workspace: Rogue3
cycle: item-56-readiness-rollups-derived-tree
lane: none
toolVersion: 0.32.0
commit: c4c1b57
---

# Rogue3 feedback — item #56, the roll-ups that were tracked but could not be reproduced

## §1 Provenance and confidence

- **activation:** active
- **phases:** onboarding-first-build, lifecycle-authoring-or-not-used, implementation-test-evidence, verify-ship-pr
- **material events:** 13
- **zero-event reason:** n/a
- Checkpoints: `feedback/checkpoints/item-56-readiness-rollups-derived-tree.jsonl`, 13 events. Untracked while authoring; committed with this report.
- Scaffold provenance `FS.GG.SDD.Artifacts/0.32.0`, provider `rendering`, provider contract 1.1.0, template `fs-gg-ui`, profile `game`. Package pins: FS.GG.UI 0.21.1, FS.GG.Game 0.12.0, FS.GG.Audio 0.5.0, FS.GG.Contracts 7.5.2.
- Cycle boundary: one board item under the bounded fan-out protocol, worker `tern-2727`, two siblings live throughout (`rogue3#53`/`smew-a1e0`, `rogue3#63`/`curlew-39d8`). Branched from `origin/main` at `715bef9`. Product change: `.gitignore`, `build.fsx`, `scripts/check-audit-bindings.py`, `.github/workflows/verify.yml`, `docs/product.md`, three artifacts removed from the index, and one `--grandfather` pass.
- **Lifecycle authoring was not used.** The item changes the gate's plumbing and what the repository tracks; it authors no product behaviour and has no `work/<id>/` item, so charter-through-ship would have produced empty artifacts. Recorded as exercised-and-declined, not skipped silently.
- **Both critics returned do-not-ship on a candidate that was green, pushed, and described as finished**, and each found defects the other did not. Every finding below that is marked as caught by a critic was caught after the author had already committed the opposite claim.
- Confidence limits. **First**, everything was measured on this Linux host; no Windows path was exercised. **Second**, `22` stale bindings is the figure for `db5ee2e` — the commit that untracked the artifacts and left the checker unchanged — not for the finished candidate, which reports **22** stale before `--grandfather` and 0 after. An earlier draft of this report and of a source comment attributed 22 to "the candidate"; a claims critic caught it and both are corrected. **Third**, this report deliberately does not cite `scripts/audit-binding-exceptions.json` as a digest-bound `file:` locator: #40/#49 record that any later `--grandfather` invalidates such a citation, and `rogue3#53` was rewriting that file's whole layout during this cycle. **Fourth**, §4.6's CI half was derived from the checker's code path before it was measured; it is now measured on a `git archive` export and the derivation is retired. **Fifth**, elapsed times in §10 are the author's contemporaneous record; only command durations are artifact-backed.

## §2 What worked

- **A control case already in the tree decided the design.** `readiness/evidence-audit.md` is tracked, rewritten by every run, and byte-identical every time, because it records a verdict and a node count and enumerates nothing. It falsifies "tracked roll-ups are the problem" and pins the real claim — "roll-ups that enumerate irreproducible things are" — which is what kept the reproducible artifacts in git instead of untracking `readiness/` wholesale.
- **Exporting the tree with `git archive` to measure the other checkout state.** The whole design rests on a claim about two checkouts that cannot both exist at once. `git archive HEAD` into a scratch directory produces the one the author is not sitting in, and it turned a derived argument into a measurement in about a second.
- **Re-deriving the item's numbers against current `main` before implementing**, as the predecessor's report advised. It confirmed 102 sensed entries, 12 in no clean checkout, and exactly the four omitted tracked files, while correcting the per-run drop to 10.
- **The `--selftest` and `SelfTest` conventions.** Both gates carry their own suites in the same file as the code, so 22 new cases needed no new harness — and the existing cases immediately rejected a design error (§3).

## §3 What did not

- The first `.gitignore` coupling raised its violation for every declared path unconditionally and **failed 25 existing selftest cases at once**, because the checker's fixture trees carry no `.gitignore`. A gate red in every workspace is a gate nobody runs; it is now raised only for a path some audit actually cites.
- **The fix had no enforcing gate at all** until a critic re-tracked the artifacts and watched every gate stay green (§4.1).
- The author fixed a false generalisation in two files and **left it live in the third**, which was the operator-facing one (§4.5).
- The first attempt at closing the predicate-versus-installation gap **did not close it** (§4.2).
- The item's declared touch-set was unfulfillable as written, for the second cycle running and on the same file (§4.4).

## §4 Findings

#### §4.1 The change removed a defect and shipped no way to notice its return; `.gitignore` cannot see the index

- **Kind:** defect
- **Impact:** The entire item could be undone by one command, with every gate in the repository still green and the checker's output byte-identical. The next gate run would then re-dirty the tree exactly as before, and the audit-binding checker would go on refusing to check the digests pinned over those artifacts — so the repository would be in a strictly worse state than before the item: tracked, churning, and unchecked.
- **Expected:** A change whose acceptance is a global property of the repository leaves something that fails when the property stops holding.
- **Observed:** A code critic with mutation licence ran `git add -f readiness/evidence-graph.md readiness/performance-evidence.json readiness/m7-ui-performance.json`. `SelfTest` 116/0, `check-audit-bindings.py --selftest` 90 ok, `dotnet test -c Release` 272/0, and the checker's summary line unchanged. `./fake.sh build -t PerformanceEvidence` then left three staged, modified files. The author's own text asserted the opposite in two places — `.gitignore` said "re-tracking a path here without removing it there fails the checker loudly instead of exempting it silently" and the checker's docstring said the same. Both were false for the same reason: a `.gitignore` rule has no effect on a path already in the index, and `derived_exemptions` keys on the ignore declaration, never on the index. The refusal to shell out to git is right — a checker that asked git what is tracked would answer "nothing" in an export with no repository and exempt every binding in the gate — but the claim made about what that refusal costs was not.
- **Evidence:** command:git add -f readiness/evidence-graph.md readiness/performance-evidence.json readiness/m7-ui-performance.json; file:.github/workflows/verify.yml; file:scripts/check-audit-bindings.py
- **Version:** `715bef9` + this cycle's commits, FS.GG.UI 0.21.1.
- **Owner:** EHotwagner/rogue3 — `.github/workflows/verify.yml`, and the two files that made the claim.
- **Recurrence:** seen again, narrowed. `feedback/2026-08-02-Rogue3-6.md` §4.2/§7 already records `git add -f` past `.gitignore` as an unenforced standing practice, and `feedback/2026-08-02-Rogue3-12.md` §4.6 already records a false claim about `.gitignore` semantics in this same operator-facing surface — so neither half is new, and an earlier draft called this finding "new". Same class as `issue:EHotwagner/rogue3#34` (a gate step that checked nothing) and `-12` §4.4 (the gates CI does not run), one level out: an acceptance with no gate at all.
- **Avoidable cost:** one CI step written and verified against the mutant, after the candidate had been declared green and pushed.
- **Disposition:** product fix — a new `verify.yml` step, "Derived roll-ups stay out of the index", asserts each path is absent from `git ls-files` and ignored by `git check-ignore`, and that its own list agrees with `DERIVED_ROLLUP_RELPATHS`. Verified: exit 1 naming all three under the mutant, exit 0 without it. The false claims are replaced with what the `.gitignore` keying actually buys — a two-file change a reviewer can see, and withdrawal of the exemption when a rule is deleted or negated.

#### §4.2 A test that proves a predicate does not prove it is installed — and the fix for that repeated the mistake one level up

- **Kind:** quality-gap
- **Impact:** The engine-drift check that the whole publication rule depends on could be uninstalled twice over with a fully green suite, including by the exact reordering its own comment called load-bearing.
- **Expected:** `feedback/2026-08-02-Rogue3-12.md` §4.6 records this repository paying a full re-implementation round to learn that "the rule being INSTALLED is exactly as load-bearing as the rule being correct", and `build.fsx` carries that sentence in a comment eight lines from the new code.
- **Observed:** The first version hung `emittedGraphSectionWarning` off an unparameterised call site inside `runEvidenceGraph`. A code critic killed the predicate mutants (returning `[]` unconditionally fails three cases) but showed two installation mutants surviving: deleting the call, and moving it after the publication rule. The author then factored `emitWithHeadingCheck` to take the emitter and the warn sink and drove it from `SelfTest` — and **the second mutant still survived**, because the new cases proved the composition while the production call site could still be rewritten to bypass it. Only a source scan asserting that `runEvidenceGraph` reaches the check solely through that wrapper killed it. The ordering case is now built so it can only pass one way: a headingless emission over a published graph that has the heading, so a check before the rule warns and a check after it sees the restored heading and stays silent.
- **Evidence:** command:./fake.sh build -t SelfTest; file:build.fsx; file:feedback/2026-08-02-Rogue3-12.md
- **Version:** `715bef9`, FS.GG.UI 0.21.1.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `SelfTest`.
- **Recurrence:** seen again, and NOT for the first time in `-12`. `feedback/2026-08-02-Rogue3-15.md` §4.5 records the identical distinction in this identical file one cycle earlier ("the line's content is asserted; its installation in the `TemplateDrift` target is not"), and `feedback/2026-08-02-Rogue3-14.md` §4.5 filed the dispatch-layer version as `issue:EHotwagner/rogue3#57`. `-12` §4.6 is the instance this cycle's code comment quotes, not the first.
- **Avoidable cost:** two mutation rounds and two rounds of new cases; the first fix for the finding did not close the finding.
- **Disposition:** product fix — `emitWithHeadingCheck` plus six cases, four behavioural and two structural. The structural pair is a text scan and says so: it proves the wiring is written, not that it runs.

#### §4.3 The merge gate's evidence roll-up was frozen and wrong at the same time

- **Kind:** defect
- **Impact:** The artifact recording what evidence exists was, at `715bef9`, both un-updatable and inaccurate — the state a green gate reported as success. Every cycle adding readiness evidence widened the gap, and the previous cycle's fix correctly made an ordinary run refuse to narrow it, so the gap could only grow.
- **Expected:** A tracked roll-up either tracks its inputs or is not tracked.
- **Observed:** The committed `readiness/evidence-graph.md` recorded 102 sensed files. **Twelve** exist in no clean checkout (five fsgg-sdd outputs under `readiness/014-m13-…/`, seven `readiness/logs/*.txt`), so every ordinary run refused: a full `Verify` sensed ten fewer, the two survivors being `TemplateDrift.txt` and `GeneratedGuidanceCheck.txt`, which #34 made real steps that run before the graph. Simultaneously the committed graph **omitted four tracked files that do exist**. The same run left `readiness/performance-evidence.json` and `readiness/m7-ui-performance.json` dirty, as every run had. A field-level diff over one gate run shows the two are not the same artifact: `performance-evidence.json` moves in 39 leaves (`p50Ms`/`p95Ms`/`p99Ms`, `allocatedBytes`, three digests, a `compositionAuthority` MVID), `m7-ui-performance.json` in 8, all `p95Ms`/`p99Ms`, and it carries no `p50Ms`, no `allocatedBytes` and no `compositionAuthority` anywhere.
- **Evidence:** command:./fake.sh build -t Verify; command:git ls-files readiness/; issue:EHotwagner/rogue3#26
- **Version:** `715bef9`, FS.GG.UI 0.21.1. The EvidenceGraph emitter ships in the FS.GG.UI.Build package and was not inspected.
- **Owner:** FS.GG.UI.Build EvidenceGraph emitter for the enumeration; EHotwagner/rogue3 `.gitignore` for the decision to track the result.
- **Recurrence:** seen again — `issue:EHotwagner/rogue3#26` (merged, consumer-side half), `feedback/2026-08-02-Rogue3-12.md` §4.3, `feedback/2026-08-02-Rogue3-5.md` §4.3.
- **Avoidable cost:** four full `Verify` runs (~4 min each): baseline, acceptance, idempotence, and one `EvidenceGraph` re-run to exercise the recovery route.
- **Disposition:** product fix — the three are gitignored and out of the index. A full `Verify` exits 0 leaving `git status --porcelain` empty, twice consecutively. `readiness/performance-critic-request.json` was untracked mid-cycle and **restored**: it digests one of the artifacts and is irreproducible for the same reason, but only `-t PerformanceCriticRequest` writes it and `Verify` never calls that target, so untracking it deleted a record and bought no part of the acceptance.

#### §4.4 The item's touch-set was unfulfillable, and the path it omits is the one an open item exists to fix

- **Kind:** orchestration
- **Impact:** The fix cannot land without editing two files the item does not name, both held by a live sibling claim. Widening collided immediately, so sequencing was negotiated by message for at least the fifth cycle running.
- **Expected:** Either the scheduler sees the contention before admitting a wave, or the remedy does not funnel every worker onto one file.
- **Observed:** As filed, `#56` declared `.gitignore`, three `readiness/` paths and `build.fsx`. Untracking artifacts that twelve merged audits cite requires `scripts/check-audit-bindings.py` and `scripts/audit-binding-exceptions.json`, and `git diff --name-only origin/main...HEAD` confirms the landed fix edits both. `widen` reported `OVERLAP — now collides with rogue3#53 (worker smew-a1e0)` on exactly those two tokens and wrote them into the item's declaration, which is why `#56`'s `Paths:` line now contains them — the board record of a touch-set corrected mid-flight rather than at filing. (That `widen` output itself is transient board state and is preserved in no artifact; the durable half is the diff and the amended `Paths:` line.) `#53` is the item filed to stop workers colliding on the ledger and was itself the collision. The exchange that followed was the most valuable coordination event of the cycle: `#53`'s holder answered with its whole design (the ledger becomes a per-cycle directory, `--grandfather` requires `--cycle`, the legacy path stays a read-only archive), asked this cycle to rebase rather than take the shared edit, and flagged an asymmetry between the two evidence gates that it had hit from the other side. That flag became §4.6 and it went the other way from what either worker expected.
- **Evidence:** command:git diff --name-only origin/main...HEAD; issue:EHotwagner/rogue3#56; issue:EHotwagner/rogue3#53
- **Version:** `715bef9`.
- **Owner:** EHotwagner/rogue3 — the board's path-declaration protocol and the single-file ledger.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-12.md` §4.5 records the same collision from the other side; `issue:EHotwagner/rogue3#53` records one bounded fan-out of three concurrent workers plus closed `issue:EHotwagner/rogue3#41` — *not* four prior cycles, as an earlier draft of this line claimed. The "fifth cycle running" count is supportable from the reports (`-13` §4.5, `-14` §4.3, `-15` §4.3, `-16` §4.6, `-17` §4.5), not from the issue.
- **Avoidable cost:** one `widen`, one overlap collision, three coordination messages, and a re-sequencing that held the shared edits until last. No `Blocked by` edge: the contention is transient.
- **Disposition:** existing issue — `issue:EHotwagner/rogue3#53`, which merged during this cycle and removes the ledger half of the contention; this cycle rebased onto it as agreed. The new evidence it carries forward is that the collision now also reaches `scripts/check-audit-bindings.py` and not only the ledger, so a per-cycle ledger alone does not end it.

#### §4.5 Correcting a false generalisation in three of its four locations is how it survives

- **Kind:** documentation
- **Impact:** The surviving copy was the help banner — operator-facing output, the same surface on which this board has now shipped a false generalisation three times.
- **Expected:** A correction enumerates every surface carrying the sentence.
- **Observed:** Mid-cycle the author self-audited against the predecessor's known defect and found the same one: a single field list bracketed against both performance artifacts, and "all four are written by Verify" when `Verify` never calls `PerformanceCriticRequest`. Both were corrected in `.gitignore`, `docs/product.md` **and** `build.fsx`'s rationale comment, and committed as a self-correction — three of the four surfaces carrying the sentence. A claims critic then found the identical conflation still live in the fourth, `build.fsx`'s help banner, which is the operator-facing one. (This finding's own first draft said "two of three"; the report critic counted four. A finding about a miscounted enumeration containing a miscounted enumeration is the joke writing itself, and it is left visible rather than quietly fixed.) Separately it found the checker docstring attributing `22` stale bindings to "the candidate" when 22 belongs to the first commit with the pre-fix checker, and an unenforceable forward promise ("the second exemption, and the last") colliding with an existing comment that called a different thing "the second".
- **Evidence:** file:build.fsx; file:.gitignore; file:feedback/2026-08-02-Rogue3-12.md
- **Version:** `715bef9`.
- **Owner:** EHotwagner/rogue3 — `build.fsx` help banner and in-source rationale.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-12.md` §4.6 records a claims critic and then a third reviewer finding false assertions in this same operator-facing string.
- **Avoidable cost:** one further correction pass. The in-cycle self-audit that caught the first two copies did not enumerate the surfaces carrying the sentence, which is the whole lesson.
- **Disposition:** accepted — corrected, with measured per-artifact field lists and the commit each measurement was taken at.

#### §4.6 The repository's two evidence gates disagree about a citation onto a file that does not exist

- **Kind:** quality-gap
- **Impact:** An exemption added to one gate cannot be relied on to hold in the other, so a change that makes a cited artifact absent is green under `check-audit-bindings.py` and red under the feedback validator. A worker reading only the first would ship believing the citation was handled.
- **Expected:** Two gates over the same citations agree on what an unbindable citation means, or the difference is documented.
- **Observed:** `check-audit-bindings.py` applies `exemption()` **before** the existence check, so an exempt citation onto an absent file is reported and not checked. `FeedbackReportTool.resolveEvidencePath` checks existence **upstream** of its digest exemption, and the skill states the rule: "the locator still has to resolve inside the workspace and the file still has to exist, only the digest is unbindable." `rogue3#53`'s holder hit this from the other side and reverted a migration because of it. Measured both ways on `feedback/2026-08-01-Rogue3-8.md`, which cites both performance artifacts: on `origin/main` it already fails with `evidence digest is stale` for both, alongside seven stale source digests; on the candidate the same report fails with `evidence file is missing` for the same two and the same seven. **No green validation turns red** — a pre-existing failure changes its message, because the validator has no excuse ledger and historical reports in this repository do not re-validate against a moved tree anyway.
- **Evidence:** command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- validate feedback/2026-08-01-Rogue3-8.md --audit feedback/audits/2026-08-01-Rogue3-8.audit.json; file:scripts/check-audit-bindings.py; issue:EHotwagner/rogue3#53
- **Version:** `715bef9`, feedback tool 0.32.0.
- **Owner:** FS.GG feedback-tool `FeedbackReportTool.resolveEvidencePath`; secondarily EHotwagner/rogue3 `scripts/check-audit-bindings.py` for the ordering it chose.
- **Recurrence:** **duplicate.** `issue:EHotwagner/rogue3#77` was filed at 14:23:29Z — before this cycle's own checkpoint of the same observation — records the identical two-row disagreement, quotes the same `SKILL.md` rule, cites the same reverted `#53` migration, and names `rogue3#56` in its body as heading for the same wall. This cycle read its inbox at claim time, did not re-check open issues before filing, and filed `issue:EHotwagner/rogue3#83` as a duplicate; a report critic caught it. #83 is closed as duplicate and the measurement below was moved onto #77 as a comment. **The dedupe failure is the more useful finding here than the asymmetry.**
- **Avoidable cost:** none — the measurement was cheap and it retired a worry that had already cost `rogue3#53` a written-and-reverted migration.
- **Disposition:** existing issue — `issue:EHotwagner/rogue3#77`, with this cycle's measurement added as a comment. Not blocking: the outcome is unchanged for every report in the tree, which is the part #77 reasons about but does not measure, and it matters because the fear of this asymmetry had already cost `rogue3#53` a written-and-reverted migration.

#### §4.7 A digest pinned over a run output cannot be settled by the excuse ledger

- **Kind:** defect
- **Impact:** Fourteen `file:` bindings across twelve merged audits pin a sha256 over an artifact the merge gate rewrites (sixteen raw citations; the checker counts one binding per audit and locator, so three duplicates in one audit collapse to one). While the artifacts were tracked, workers kept those bindings alive by reverting the files by hand after every gate run. The moment the artifacts stop being tracked the bindings become unsatisfiable in a way the documented remedy cannot reach.
- **Expected:** `--grandfather` settles any stale binding in one pass and keeps it settled; the module docstring says so and proves it for the audit-to-audit case.
- **Observed:** An exception's identity includes the observed digest, so it excuses exactly one observed value, and a run output does not have one. At `db5ee2e` the checker reported 22 stale bindings in a worktree that had just run the gate — and **none** of the four bindings onto `readiness/evidence-graph.md` was reported stale — two were fresh, because the publication rule had restored the exact bytes the file carried while tracked, and two were already ledger-excused at those same bytes. Not stale in the very checkout where CI was about to read `<missing>`. (An earlier draft of this finding, and of the docstring it cites, said all four were *fresh*; a report critic corrected it.) An entry written from a developer's tree therefore fails in CI and one written from CI fails on the developer's tree. This is `rogue3#38`'s shape arriving through a different door.
- **Evidence:** command:python3 scripts/check-audit-bindings.py; file:scripts/check-audit-bindings.py; issue:EHotwagner/rogue3#38
- **Version:** `db5ee2e`/`715bef9`, FS.GG.UI 0.21.1.
- **Owner:** EHotwagner/rogue3 — `scripts/check-audit-bindings.py`, the exemption surface.
- **Recurrence:** seen again in part. The pin-one-digest mechanism is prior art — `feedback/2026-08-02-Rogue3-17.md` §4.8 states it outright, `feedback/2026-08-02-Rogue3-16.md` §4.6 and `issue:EHotwagner/rogue3#49` carry it too. What is new here is only the **local-vs-CI divergence** of a run output: a path whose observed digest differs by who is looking, which no prior instance describes. Adjacent to `issue:EHotwagner/rogue3#40` and `issue:EHotwagner/rogue3#38`.
- **Avoidable cost:** none incurred — the checker was run before the PR rather than after CI. The counterfactual is fourteen ledger entries that go stale on the next gate run.
- **Disposition:** product fix — `DERIVED_ROLLUP_RELPATHS`, an enumerated set intersected with `.gitignore`, reported through the `notBound` channel. Verified to give an identical verdict in a worktree holding all three artifacts and in a `git archive` export holding none: 257 bindings, 164 fresh, 18 not bound in both.

#### §4.8 A vacuous test fixture and a self-inflicted excuse loop: two costs this cycle recorded as events and nearly did not report

- **Kind:** quality-gap
- **Impact:** Two measurable costs were captured as checkpoints and had no finding until a report critic noticed. Both are cheap to state and both recur by design, so losing them loses the only evidence that they are structural rather than one-off clumsiness.
- **Expected:** Every material checkpoint event either becomes a finding or is explicitly dispositioned as not worth one.
- **Observed:** **(a) The vacuous fixture.** #26 deliberately made two `SelfTest` heading cases unconditional so they could not disappear; untracking the graph made the *subject* they read absent on every fresh checkout, so they run and can only announce that they proved nothing. That is the observation §11.4 exists to prevent, and it was recorded only in §7 and §9. **(b) The excuse loop.** After `--grandfather` made the checker green, one further docstring edit to `scripts/check-audit-bindings.py` — made in response to review — re-staled **5** of the excuses just written for that same file and returned the gate to exit 1, requiring a second pass. Then answering the PR review edited the same file again, requiring a third. An excuse pins one observed digest, so a cycle that responds to review feedback on an audit-bound file pays one `--grandfather` per round, and the remedy must be run last — which is in direct tension with revising after review.
- **Evidence:** command:python3 scripts/check-audit-bindings.py; command:./fake.sh build -t SelfTest; file:feedback/checkpoints/item-56-readiness-rollups-derived-tree.jsonl
- **Version:** `715bef9`..`c4c1b57`, FS.GG.UI 0.21.1.
- **Owner:** EHotwagner/rogue3 — `build.fsx` `SelfTest` for (a); `scripts/check-audit-bindings.py` and the ledger design for (b).
- **Recurrence:** seen again — (b) is `feedback/2026-08-02-Rogue3-17.md` §4.8 and `issue:EHotwagner/rogue3#49` exactly, now with an in-cycle instance; (a) is the fixture half of the §4.2 class.
- **Avoidable cost:** two extra `--grandfather` passes; (a) cost nothing this cycle but leaves two cases that assert nothing in CI.
- **Disposition:** accepted as an observation, NOT handed off. For (a) the real pin moved to `emittedGraphSectionWarning` and §11.4 proposes the convention. For (b) `issue:EHotwagner/rogue3#49` and `issue:EHotwagner/rogue3#53` already carry the mechanism — #53's per-cycle ledger landed mid-cycle and removes the *collision* but not the *re-staling* — so nothing new is filed. This finding was authored in response to the actionability critic and so was never graded by it; the audit records that as a confidence limit rather than letting it pass unmarked.

## §5 Did not exercise

- **The runtime product.** No gameplay, rendering, audio or input path was touched or run; `-t Run` was never invoked and no window was opened.
- **The SDD lifecycle.** No charter/spec/clarify/checklist/plan/tasks/analyze/evidence/verify/ship stage was run.
- **Windows.** Path rendering and `.gitignore` case sensitivity there are unexercised.
- **The engine emitter.** `FS.GG.UI.Build`'s `GeneratedRunner` is a package binary; this cycle changes what the repository does with its output and never inspects it. Emission ORDER — the issue's root cause 2, and the reason the graph is still not reproducible — belongs to it and is untaken.
- **The new CI step's FAILING path on a GitHub runner.** The step has now run there — job "Release tests" on PR #79 printed "All three derived roll-ups are untracked, ignored, and agree with DERIVED_ROLLUP_RELPATHS", which also confirms `git check-ignore` answers correctly for paths that do not exist in a fresh checkout. Its red path was exercised only locally, against the `git add -f` mutant and an `!` negation.

## §6 Doc-versus-behavior contradictions

- `.github/workflows/verify.yml` declared its exclusion list exhaustive and gave "Verify rewrites TRACKED readiness artifacts … a job that ran Verify and then checked for a dirty tree would fail for reasons unrelated to the PR" as the reason the full `Verify` is unwired. That premise is exactly what this item removed. Owner: `.github/workflows/`; rewritten — the remaining reason is the measured performance gate, whose pass depends on runner contention rather than on the PR.
- `docs/roguelike-dungeon-crawler-roadmap.md:171` and `work/013-m12-audio-assets/plan.md:72` instruct workers to read values "from the **committed** `readiness/performance-evidence.json`", and `plan.md`'s GV-001 requires those artifacts be "regenerated at the exact candidate and asserted unchanged". No checkout can do either now. Owner: the merged SDD evidence; disclosed in `.gitignore` and `docs/product.md` rather than rewritten, because rewriting merged evidence is what this repository's audit-binding gate exists to notice.
- `scripts/check-audit-bindings.py`'s docstring said the exemption was "Deliberately ONE path, not a class", arguing from convergence — an argument that does not hold for a path whose observed digest depends on the checkout. Owner: the docstring; extended with a second enumerated set and its own section.

## §7 Workarounds still in the tree

- **`FSGG_EVIDENCE_GRAPH_PUBLISH`** (`build.fsx`). Now second-choice: deleting the graph and re-running is the ordinary local route. Removable once the emitter declares what it could not sense.
- **The publication rule's restored graph is deliberately stale** (`build.fsx`). Unchanged in mechanism, changed in stakes: the in-checkout copy is now the only copy. The trade is named in-source — under #26 a stale graph was a tracked-file diff a reviewer could see; now the only signal is stderr inside a run that exits 0.
- **`DERIVED_ROLLUP_RELPATHS`** (`scripts/check-audit-bindings.py`) and the duplicated list in `verify.yml`. Three statements that must agree; the CI step asserts two of them do. Retired only when no merged audit cites the paths, which is never, since audits are immutable.
- **The two committed-graph `SelfTest` cases** (`build.fsx`). They can only announce that they proved nothing where the graph is absent, which is every fresh checkout. Kept as a free extra assertion; the real pin is `emittedGraphSectionWarning`.

## §8 Friction and avoidable cost

- **Four full `Verify` runs** and one `EvidenceGraph` run (~4 min each): baseline, acceptance, idempotence, recovery route.
- **Three critic rounds** — a code critic and a claims critic (both do-not-ship), then a PR reviewer on the post-fix diff (request-changes, two blocking mutants), plus a report actionability critic that corrected nine factual claims and found a duplicate issue. One further round of mutation work because the first fix for §4.2 did not close §4.2.
- **Three `--grandfather` passes**, not one: each round of review edited an audit-bound file and re-staled the excuses written for the previous round (§4.8b).
- **One merge of `origin/main`** after `rogue3#53` landed mid-review, resolving two conflicts in `scripts/check-audit-bindings.py` — the rebase side this cycle had agreed to take.
- **One design round lost to over-broad enforcement**: the `.gitignore` coupling raised unconditionally failed 25 existing selftest cases.
- **One `widen`, one overlap collision and three coordination messages** (§4.4).
- **Zero product rework.** `dotnet test -c Release` was 272/272 green at every checkpoint; the change adds no product behaviour.

## §9 Skill value and gaps

- **`fs-gg-feedback-report` — invoked.** Drove the checkpoint discipline; 13 events across four phases, `validate-checkpoint-state` exit 0. Its `validate` step is exposed to the known #40/#49 defect, which is why this report cites a command rather than the ledger, and its existence-before-exemption ordering is §4.6.
- **Relevant skills not invoked, and why.** The `fs-gg-sdd-*` lifecycle skills: no `work/<id>/` item and no product behaviour. `fs-gg-testing`, `fs-gg-playtest`, `fs-gg-project` and the gameplay skills are product-facing and nothing product-facing was touched.
- **Wanted and absent: a skill for the gate surface.** `build.fsx` is 3,029 lines carrying template drift, the guidance check, the evidence gates, the publication rule and a 122-case selftest; `scripts/check-audit-bindings.py` is a second gate with a 94-check selftest; `.github/workflows/verify.yml` is a third statement of the same policy. Nothing covers them, and the conventions that mattered here — that a `SelfTest` case must have a subject and not merely run, that the checker's fixture trees are bare, that a claim in one of the three must be restated in the other two — were all learned by reading source, by one failed selftest run, and by two critics. `feedback/2026-08-02-Rogue3-12.md` §9 asked for this skill for one file; the ask now covers three.
- **No misleading skill guidance encountered.** Drafting outside `feedback/` and writing the durable report only after critic dispositions was the right sequence: the draft changed substantially after both critic reports.

## §10 Outcome markers

- Time to first build: n/a — an existing repository. First command to green on the unmodified tree: `./fake.sh build -t SelfTest`, 111 cases.
- Time to first reproduction: one `Verify` run, ~4 minutes, first attempt; it dirtied exactly the two artifacts the item names and refused the graph with ten inputs named.
- First green verification of the acceptance: `./fake.sh build -t Verify` exit 0 with `git status --porcelain` **empty**, first attempt on the candidate; confirmed on a second consecutive run.
- Recovery route proved: `rm readiness/evidence-graph.md && ./fake.sh build -t EvidenceGraph` exits 0, publishes a graph derived from this checkout, prints no refusal, and leaves `git status --porcelain` empty.
- Final: `SelfTest` 125 cases / 0 failures; `check-audit-bindings.py --selftest` 157 checks; `check-audit-bindings.py` exit 0; `dotnet test -c Release` 272/272; `TemplateDrift` 190 of 190.
- Test-count delta: `SelfTest` 111 → 125 (+14); `--selftest` 72 → 157 after merging `rogue3#53`'s own additions, of which +27 are this cycle's; `dotnet test` unchanged at 272 — the change adds no product behaviour.
- Binding-count delta: 271 checked bindings → 257, `not bound` 4 → 18. The fourteen that moved are the citations onto the three untracked roll-ups.
- Mutation score: **six** mutants found alive across two review rounds, all now killed and each re-applied after its fix and observed red — re-tracking via `git add -f`; `!` negation of an ignore rule; deleting the heading check; moving it after the publication rule; neutering the warn sink with `ignore`; and binding the correct wiring to a dead `let _checked`. The last two were found by the PR reviewer *in the fix for the first four*, which is the second time this cycle a remedy failed to close the finding it answered.
- Ship readiness: PR `EHotwagner/rogue3#79`, opened against `715bef9` and merged onto `rogue3#53`'s landing. Estimated ~7 h wall clock across claim, re-derivation, implementation, four critic/review rounds, three mutation rounds, one merge and the report.

## §11 Falsifiable improvements

1. **Give the EvidenceGraph emitter a way to declare what it could not sense.** Would make the graph correctable from any checkout, would make it genuinely reproducible, and would have made this item unnecessary. Owner: FS.GG.UI.Build `GeneratedRunner`. Acceptance: the emitted graph distinguishes "absent" from "not sensed", and two runs in the same checkout sense the same set.
2. **Require an item that untracks, moves or deletes a file to declare the gate surfaces that bind it.** Prevents §4.4's specific shape. Owner: EHotwagner/rogue3 issue conventions. Acceptance: an item whose acceptance requires editing a gate's own remedy surface names that surface in `Paths:`.
3. **Re-check open issues, not only the inbox, before filing.** Prevents §4.6's real finding — this cycle filed a duplicate of an issue opened 25 minutes earlier by the worker it had been corresponding with. Owner: EHotwagner/rogue3 worker protocol. Acceptance: a cycle that files an issue records the `gh issue list` query it deduped against. (The underlying gate disagreement is already tracked as `issue:EHotwagner/rogue3#77`.)
4. **Add a `SelfTest` convention that a case names its subject and fails when the subject is absent.** Prevents the vacuous-fixture problem recorded in §4.8 and retires the two vacuous cases in §7. (§4.2 is about installation, not fixtures; an earlier draft mis-attributed this to it.) Owner: EHotwagner/rogue3 `build.fsx`. Acceptance: a case whose fixture disappears is reported as failed or skipped, never as `ok`.
5. **When correcting a claim, grep for every copy of it before committing.** Prevents §4.5. Owner: EHotwagner/rogue3 worker protocol. Acceptance: a correction commit's diff touches every file containing the corrected sentence, or says why one is exempt.
6. **An item whose acceptance is a repository-wide property must ship the check that fails when the property stops holding, and prove it by breaking it.** Prevents §4.1, which this cycle shipped and pushed without. Owner: EHotwagner/rogue3 worker protocol and `.github/workflows/`. Acceptance: for each such item, one command that makes the property false and one CI job observed red under it, both recorded in the cycle report. Note this would have caught §4.1 but not §4.2, §4.5 or §4.7 — it is a remedy for one of this report's findings, not for the class.

## §12 Development-surface coverage

| Surface | Status | Evidence and result |
|---|---|---|
| scaffolding | not-exercised | Existing repository at `715bef9`; provenance read from `.fsgg/scaffold-provenance.json` only. |
| onboarding-guidance | partial | `docs/product.md` and `build.fsx`'s help banner were the only guidance for the gate surface, were silent on what to do with the artifacts a run rewrites, and one carried a false generalisation (§4.5). Both extended. |
| skills | partial | `fs-gg-feedback-report` invoked and drove the checkpoint discipline; every product-facing skill was correctly inapplicable. A gate skill is absent and now wanted for three files (§9). |
| sdd-authoring | not-exercised | Deliberately declined — no `work/<id>/` item, no product behaviour. Recorded in §1. |
| implementation-apis | not-exercised | No FS.GG product API used; the change is BCL file IO in an F# script, Python, and one CI step. |
| dependencies-build | exercised | `dotnet restore`/`build`/`test` and four `./fake.sh build -t Verify` runs, all green. |
| testing | exercised | `SelfTest` 111 → 125, `--selftest` 72 → 157, `dotnet test -c Release` 272/272. Six surviving mutants found across two review rounds and killed, two of them inside the fix for the other four; one design error caught by existing cases (§3). |
| evidence | exercised | The cycle's subject. Three roll-ups untracked and one restored; acceptance measured as an empty `git status --porcelain` after a green `Verify`, twice; recovery route exercised; the checker verified identical in both checkout states. |
| runtime-playtest | not-exercised | No product code touched; `-t Run` not invoked and no window opened. |
| performance | partial | `PerformanceIntent`/`PerformanceEvidence` ran as part of `Verify` (7 workloads, 4 UI routes, `status=ok`) and their artifacts were the subject, but no workload changed and no performance work was done. |
| documentation | partial | `.gitignore`, `build.fsx`, `docs/product.md` and `verify.yml` all carry the new rule — but this cycle SHIPPED three false statements into that documentation and removed them only after a third review pass: the field-list conflation (§4.5), a `17 stale / 19 not bound` pair true of neither tree it could describe, and a leftover "the other three" from the four-path draft. On this report's own §4.5 standard that is `partial`, not `exercised`. |
| packaging-upgrade | not-exercised | No package pin changed; FS.GG.UI stayed at 0.21.1. |
| worker-git-pr | exercised | Claimed under the fan-out protocol (`take` exit 0), isolated worktree, one `widen`, one overlap with `rogue3#53` resolved by message and by sequencing the shared edits last, checkpoints pushed as they were made. See §4.4. |

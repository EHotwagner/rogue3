---
feedbackSchema: 2
date: 2026-08-02
workspace: Rogue3
cycle: item-38-audit-binding-gate-no-green-path
lane: none
toolVersion: 0.32.0
commit: c6ceb60
---

# Rogue3 feedback — item #38, the gate that demanded a fixed point it rewrote itself

## §1 Provenance and confidence

- **activation:** active
- **phases:** onboarding-first-build, lifecycle-authoring-or-not-used, implementation-test-evidence, verify-ship-pr
- **material events:** 5
- **zero-event reason:** n/a
- Checkpoints: `feedback/checkpoints/item-38-audit-binding-gate-no-green-path.jsonl`, 5 events. Committed with the change rather than with this report, because two of them are cited as evidence against the change itself.
- Scaffold provenance `FS.GG.SDD.Artifacts/0.32.0`, provider `rendering`, provider contract 1.1.0, template `fs-gg-ui`, profile `game`. Package pins: FS.GG.UI 0.21.1, FS.GG.Game 0.12.0, FS.GG.Audio 0.5.0, FS.GG.Contracts 7.5.2.
- Cycle boundary: branched from `d8d0024`; two commits, `71e3a18` then `c6ceb60`; merged as PR #42. The second commit exists because two critics rejected the first, and the difference between them is most of §4.
- Board item `EHotwagner/rogue3#38`, class `defect`, severity `High`, claimed by worker `wren-03e2`, `take` exit 0.
- **Confidence limits.** This cycle changed one Python script and the exceptions ledger; it compiled and ran no F# it did not already have, so "228 tests still pass" is a regression guard, not evidence about the change. Claims about the F# validator (§4.3) rest on running one command against one merged report and on one critic's read of `FeedbackReportTool.fs:574`; the tool's source was not inspected by the author. The pre-fix non-convergence claim is empirical over four passes plus a mechanism argument, not a proof of divergence for all inputs. The macOS half of §4.7's blocking bug was **not** reproduced on macOS — it was reproduced on Linux by pointing `$TMPDIR` at a symlink, and the macOS consequence is inferred from `/var → /private/var`.

## §2 What worked

- **The gate proves itself before it judges, and that is what made a change *to the gate* safe to make.** `.github/workflows/audit-bindings.yml` runs `--selftest` before the verdict step. This cycle rewrote the checker's core scan twice, and the 53 pre-existing self-test cases were the entire regression suite for both rewrites — the repository has no other test over this script. All 53 passed unchanged throughout.
- **`--selftest` builds fixtures in a temporary tree**, so a case needing a whole seeded repository — an audit citing the ledger, two audits citing each other, a symlinked root — costs about ten lines. Nineteen new cases were added across two commits with no fixture scaffolding.
- **`scripts/fsgg-coord widen` accepted a touch-set widening while the claim was held**, which kept this report and the checkpoint file inside the declared `Paths:` rather than becoming the DRIFT the item brief warns about. `verify-paths --pr 42` returned OK.
- **The ledger's carry-forward rule is honest under repetition.** Entries still excusing a live violation are re-emitted verbatim, so re-running `--grandfather` with a throwaway `--reason` did not overwrite the deliberate rationale. Checked against the diff, not assumed.
- **Worker-to-worker messaging did real work.** Two siblings were blocked on this item and both had planned the hand-prune-and-rebind sequence this fix removes; a reply on each told them to skip it. That is coordination changing what another worker will do, not status broadcasting.

## §3 What did not

- The first reproduction was scoped to copies of `feedback/` and `scripts/` only, mirroring the issue's own method. Every binding onto `src/`, `tests/` and `docs/` then resolved to a missing file, so the excused count read 142 instead of 61 and the tree's real shape sat behind 81 spurious `<missing>` violations. Redone over a full tree copy. A partial-tree copy is not a faithful fixture for a gate that scans the whole workspace.
- **The first candidate shipped a false justification and a blocking bug, and was green on CI.** It exempted every `feedback/audits/*.audit.json` in addition to the ledger, on the stated ground that such a binding has "no fixed point at all". It does have one. That candidate passed both CI checks, `verify-paths`, a 66-case self-test and a two-mutant matrix. §4.7 is the finding; the cost is a whole second implementation pass.
- One self-test case, `two audits citing each other converge`, was written, passed, and would have shipped as coverage. Mutation testing showed it passing with the fix fully reverted. It was rewritten — and then deleted entirely in the second commit, because the behaviour it was rewritten to assert turned out to be the behaviour that should not exist.

## §4 Findings

#### §4.1 The gate's documented remedy was a fixed-point equation with no fixed point

- **Kind:** defect
- **Impact:** The audit-bindings gate fails any PR touching any bound file, and the tool its failure message named could not clear it. Every worker touching a bound file had to hand-prune a merged cycle's ledger entry and hand-rebind another cycle's merged audit — the practice the `7e71d71` "excuse rather than rebind" precedent exists to prevent, which silently rewrites what an audit records its critic as having verified. M13 performed that surgery three times in one cycle. Two sibling workers were blocked on it during this cycle.
- **Expected:** The documented remedy converges. `--grandfather` "rewrites the ledger from the current violations and prunes obsolete entries", after which `check` is green.
- **Observed:** Excusing writes the ledger, and `feedback/audits/2026-08-02-Rogue3-7.audit.json` §4.4 cites the ledger — reasonably, since the finding is *about* the ledger. Over a full-tree copy of `d8d0024`, baseline green, after one edit to a bound file, four consecutive `--grandfather` runs produced four distinct ledger digests (`7def642719…`, `a7fa955193…`, `8bcdf01270…`, `9f3597b666…`) and `check` still exited 1 on the ledger's own binding. Post-fix on the same tree, four runs produce one digest (`e291ff3ddfda…`) and `check` exits 0.
- **Evidence:** command:python3 scripts/check-audit-bindings.py --root COPY --grandfather --reason "repro pass N" over four passes yields four distinct ledger digests, after which the same script exits 1 naming the ledger citation; command:python3 scripts/check-audit-bindings.py --grandfather --reason "convergence probe" run four times post-fix yields one ledger digest e291ff3ddfdaa897c312e58b00a8787ce02bdcecf06a55fcc8f0f2c71ccb7ad7, after which the same script exits 0; file:scripts/check-audit-bindings.py; issue:EHotwagner/rogue3#38
- **Version:** n/a — repository-local script. Reproduced at `d8d0024`; fixed at `c6ceb60`.
- **Owner:** EHotwagner/rogue3 `scripts/check-audit-bindings.py`, the binding scan in `collect_bindings`.
- **Recurrence:** seen again, now diagnosed and fixed. `feedback/2026-08-02-Rogue3-7.md` §4.4 recorded the *cost* of this shape — that excusing grows the ledger and rewrites a prior entry's rationale in place — and `feedback/2026-08-02-Rogue3-3.md` §7 recorded the ledger as a standing workaround at 45 entries. Neither recorded that the remedy cannot terminate; M13 found that and filed issue:EHotwagner/rogue3#38, which this cycle closes.
- **Avoidable cost:** historical — three hand-edits of merged audits in M13 alone, plus a red gate on `main` for any change touching a bound file, plus two workers blocked this wave.
- **Disposition:** product fix — merged in PR #42. A `file:` citation onto the ledger is reported rather than checked; the exemption is that one path and nothing else.

#### §4.2 `--grandfather` exits 0 while leaving the gate red, so the remedy reports a success it did not achieve

- **Kind:** defect
- **Impact:** This is why §4.1 was hard to read from the tool. A worker running the prescribed remedy sees `wrote scripts/audit-binding-exceptions.json with N exception(s)` and exit 0, with no signal that the gate is still red. In the reproduction it reported success four times running while `check` stayed at exit 1. The documented contract is "0 clean", so a worker trusting the exit code would commit and push a red tree.
- **Expected:** A remedy command that exits 0 has remedied the thing. `main()` already models this for one case, returning 1 when a malformed audit cannot be absorbed, precisely so the rewrite "does NOT report success while such a violation stands". The same reasoning applies to a violation the rewrite itself creates.
- **Observed:** `grandfather()` returns non-zero only for `notExcusable` entries and never re-evaluates after writing, so a violation caused by its own write is invisible to it. Pre-fix, four passes each printed a success line and exited 0 with the tree red.
- **Evidence:** command:python3 scripts/check-audit-bindings.py --root COPY --grandfather --reason "repro pass N" printed grandfather-exit=0 on all four passes while the following check printed CHECK_EXIT=1; file:scripts/check-audit-bindings.py
- **Version:** n/a — repository-local script. Observed at `d8d0024`.
- **Owner:** EHotwagner/rogue3 `scripts/check-audit-bindings.py`, the `--grandfather` exit-code contract in `main()`.
- **Recurrence:** new. No prior report examines what the remedy returns; `feedback/2026-08-02-Rogue3-7.md` §4.5 praised the checker's self-test without touching this.
- **Avoidable cost:** none this cycle — the reproduction captured both exit codes deliberately. The historical cost is shared with §4.1.
- **Disposition:** product fix, as an invariant rather than a code change, and recorded here as **only partly discharged**. With the ledger exempt, the sole file `--grandfather` writes is unbound, so exit 0 now implies `check` exits 0, and `--selftest` asserts exactly that. But the invariant is a *consequence* of the exemption, not enforced independently: `grandfather()` still does not re-evaluate. The stronger form is §11.3.

#### §4.3 The same no-fixed-point rule lives in the F# validator, where the gate fix cannot reach it

- **Kind:** defect
- **Impact:** Fixing the CI gate does not fix the evidence rule. `feedback-tool.fsx -- validate` applies the same digest rule to every `checkedEvidence[].locator` with no exemption, so a merged report whose finding is *about* the excuse ledger — and therefore reasonably cites it — is invalidated by the documented remedy for any unrelated stale binding. No careful worker can avoid this: every `--grandfather` run writes the ledger. **Merging PR #42 takes `main` from green to red on a merged report**, and every future cycle that excuses anything will do the same.
- **Expected:** A merged, audited report stays revalidatable. The checker's docstring explicitly claims alignment: "a file this checker calls fresh is a file the validator calls fresh."
- **Observed:** `feedback/audits/2026-08-02-Rogue3-7.audit.json` §4.4 pins the ledger at `c2ad54bb862d3af6cce58beb82e7e0bae63bc921e5f87cfe65a29a6f5133c9ed`, exactly the LF-normalized digest of the ledger at `origin/main` — green there only because M13 hand-rebound it. After this cycle's ledger write, `validate` on that report exits 1 with `audit: §4.4 evidence digest is stale`. The gate now calls that citation exempt; the validator still calls it stale.
- **Evidence:** command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- validate feedback/2026-08-02-Rogue3-7.md --audit feedback/audits/2026-08-02-Rogue3-7.audit.json exits 1 on a stale ledger evidence digest, and exits 0 against a clean origin/main copy; command:git show origin/main:scripts/audit-binding-exceptions.json digested under the LF-normalized sha256 rule yields c2ad54bb862d3af6cce58beb82e7e0bae63bc921e5f87cfe65a29a6f5133c9ed, the value that audit pins; issue:EHotwagner/rogue3#40
- **Version:** scaffolded skill `fs-gg-feedback-report` from `FS.GG.SDD.Artifacts/0.32.0`; the tool reports no version of its own and re-verification against a newer skill release was not possible from this workspace.
- **Owner:** FS-GG `fs-gg-feedback-report` skill, `scripts/feedback-tool.fsx` evidence-digest validation. **Not** "outside this repository" — a critic corrected that framing, and it is corrected in issue #40: `feedback-tool.fsx` and `FeedbackReportTool.fs` are tracked here among 95 files under `.agents/`. The objection is that patching them forks a vendored skill the repo consumes, which is a governance argument, not a physical one.
- **Recurrence:** new as a validator defect. The underlying shape is `feedback/2026-08-02-Rogue3-7.md` §4.4 and issue #38, both about the Python gate only.
- **Avoidable cost:** none to the implementation. The standing cost is that a merged report cannot be revalidated on `main` after this merge.
- **Disposition:** issue — filed at root cause as issue:EHotwagner/rogue3#40, on the board as `defect`/`High`, stated in PR #42's body rather than only in a comment, and flagged to the sibling worker who will hit it next. The one alternative that would have kept `main` green — rebinding `-3`/`-6` instead of excusing them — was rejected under `7e71d71` and is recorded on #40 rather than left unmentioned.

#### §4.4 Mutation testing was the only thing that distinguished real coverage from a green suite

- **Kind:** positive-pattern
- **Impact:** A test passing with the fix reverted is worse than no test: it is a green claim that a shape is covered. This cycle produced two such cases and caught both by mutation, not by review. One covered the second of the two shapes issue #38 names, which is the shape the owner's follow-up comment specifically asked the fix to handle.
- **Expected:** New tests for a defect fail on the defective code. The item brief requires this and requires the code critic be given licence to revert changes one at a time.
- **Observed:** In the first candidate, with `exemption()` forced to `None`, 8 of the then-12 new cases failed and `two audits citing each other converge` passed — because `--grandfather` writes the ledger and never rewrites an audit, so a mutual citation settles by excusing even when unfixed. That single surviving case was the thread that eventually unravelled the whole audit-to-audit exemption (§4.7). The final suite is mutation-tested against six mutants and all six are caught: pre-fix `exemption()→None` (8 cases), `root` not realpath'd (2), keyed on locator text (1), exempt citations dropped silently (3), **the rejected wider fix** (5), ledger match widened to `scripts/*.json` (1). A critic separately found three further cases killed by no mutant at all and they were removed or replaced.
- **Evidence:** command:a six-mutant matrix over scripts/check-audit-bindings.py reports CAUGHT for all six with the failing case names and exit 1 each; command:python3 scripts/check-audit-bindings.py --selftest exits 0 with 72 checks; file:scripts/check-audit-bindings.py
- **Version:** n/a — repository-local script and process.
- **Owner:** EHotwagner/rogue3 worker process; worth promoting into the FS-GG `fs-gg-testing` skill, which does not prescribe mutation-testing a new regression test against the unfixed code.
- **Recurrence:** new as a recorded practice. `feedback/2026-08-02-Rogue3-7.md` §4.5 recorded that the self-test "proves the mechanics, not the judgements"; this is the first cycle to record a method that separates them and to catch specific cases failing it.
- **Avoidable cost:** one rewritten assertion and three deleted ones. The counterfactual is the cost that matters: false coverage of the exact shape the issue was reopened to address.
- **Disposition:** skill fix — proposed in §11.6 against `fs-gg-testing`; applied here and carried in PR #42 as a mutation table.

#### §4.5 A gate that self-tests before it judges is what made rewriting the gate reviewable

- **Kind:** positive-pattern
- **Impact:** This cycle rewrote the core scan of the only thing standing between the repository and silent evidence rot — twice. There is no other test over that script, and `dotnet test`'s 228 cases do not touch it. The 53 embedded self-test cases were the entire regression suite, and the workflow ordering that runs them *before* the verdict is what makes a degraded checker fail loudly rather than pass everything.
- **Expected:** A gate a cycle intends to modify has a regression suite a reviewer can run in one command.
- **Observed:** `--selftest` runs 72 cases after this change (53 pre-existing, unchanged and passing, plus 19 new) in under a second, with no build step, no fixtures on disk and no network. It is what allowed a `collect_bindings` signature change and a second, larger rewrite to be made without hand-auditing 188 bindings. It did **not** catch the cycle's blocking bug, and §4.7 records why: it seeds its trees with `mkdtemp()`, which is a real path, so the one input class that broke the fix was the one class the suite could not generate.
- **Evidence:** command:python3 scripts/check-audit-bindings.py --selftest exits 0 with 72 checks reported ok; command:grep -rn "check-audit-bindings" tests/ build.fsx returns no matches, so the self-test is this script.s only regression suite; file:.github/workflows/audit-bindings.yml
- **Version:** n/a — repository-local script and workflow.
- **Owner:** EHotwagner/rogue3 `scripts/check-audit-bindings.py` and `.github/workflows/audit-bindings.yml`; the pattern belongs upstream in the FS-GG scaffold's other generated gates.
- **Recurrence:** seen again — `feedback/2026-08-02-Rogue3-7.md` §4.5 recorded this pattern from a cycle that *broke* bindings. New here is the view from a cycle that changed the *checker*, where the self-test is not a convenience but the precondition for the change, and the observation that its fixture generator bounded what it could ever catch.
- **Avoidable cost:** none — a saving. Contrast issue:EHotwagner/rogue3#34, where three of `Verify`'s six merge-gate steps write a success log and check nothing.
- **Disposition:** accepted; recorded so the contrast with #34 and the fixture-generator limit both stay visible.

#### §4.6 The worktree-isolation guard rejects compound shell commands, forcing scratch scripts for every multi-step reproduction

- **Kind:** friction
- **Impact:** This cycle's central evidence is a loop — copy a tree, edit a file, run a command four times, capture a digest and a true exit code each pass. Every attempt to express that as one shell invocation was refused with "this command is too complex to verify that it stays inside the worktree; break it into plain, separate commands". Work was not blocked, but each reproduction had to be materialised as a scratch `.sh` or `.py` file first, which is more tool calls and puts the evidence-producing code where the PR does not show it.
- **Expected:** A worker isolated to a git worktree is restricted from git operations that escape it. A `for` loop, a heredoc, or a redirect into a scratch directory is not a git operation.
- **Observed:** Six refusals across the cycle, on: the `eval "$(…)"` of the identity command the item brief prescribes as step 1; two `python3 - <<'EOF'` heredocs that only read JSON; a loop running the checker four times; a `git add … && cat > …` sequence; and a `… | tail -40` with `${PIPESTATUS[0]}` — the exact form the item brief mandates for capturing true exit codes. That last is the sharp one: the brief requires a construct the harness refuses, so the prescribed technique had to be moved into a file to be used at all.
- **Evidence:** command:eval "$(scripts/fsgg-coord whoami --mint)" refused with "this command is too complex to verify that it stays inside the worktree"; command:a --selftest invocation piped into tail with a PIPESTATUS exit-code capture was refused with the same message; file:scripts/check-audit-bindings.py
- **Version:** worker harness; version not surfaced to the agent and not re-verifiable against a newer release from this workspace.
- **Owner:** FS-GG worker orchestration / the Claude Code worktree-isolation command classifier — not this repository.
- **Recurrence:** new. No prior report records the isolation guard's shape; earlier reports record `Paths:`/touch-set friction, a different mechanism.
- **Avoidable cost:** six refused commands and four scratch files that exist only because a loop could not be typed inline. No rework of the fix, no lifecycle reruns.
- **Disposition:** accepted and recorded. The guard fails safe, which is the right direction for a misclassification, and nothing in this repository can change it.

#### §4.7 A green, self-tested, mutation-tested candidate was wrong in two independent ways, and only fresh-context critics found it

- **Kind:** quality-gap
- **Impact:** The first candidate passed both CI checks, `verify-paths`, a 66-case self-test and a two-mutant matrix, and carried a PR body with digests and a mutation table. It was wrong twice over, and each critic found a different one. Had either been skipped, the merge would have shipped a defect: one critic's finding was a **silent reintroduction of the very defect the item exists to fix**.
- **Expected:** A change that is green on every gate the repository owns, plus a self-test the change itself extends, plus mutation testing, is close to shippable.
- **Observed:** Two failures no gate could see. **(a) A false premise.** The candidate exempted every `*.audit.json` on the ground that such a binding "has no fixed point at all". The design critic disproved it by building a ledger-only variant and rebinding a merged audit that another audit cites — the exact case that stranded the `-6` audit in M13 — then running `--grandfather` three times: green after pass 1, one ledger digest across all three. Independently reproduced by the author before acting. The author had *already recorded the disconfirming fact in their own checkpoint* and did not connect it to the docstring they then wrote. The wider exemption also switched off three checks that were **fresh on `main`, not excused**, made a dangling reference to a non-existent audit silently green, and let an audit bypass its own report binding. **(b) A blocking bug.** `resolve_inside()` realpaths its result; `root` did not, so `_rel(root, resolved)` produced `../real/scripts/…` and matched no exemption — the entire fix silently inert under a symlinked root, with #38 returning in full: four passes, four digests, red. The self-test could not catch it because it seeds trees with `mkdtemp()`, a real path.
- **Evidence:** command:a ledger-only variant over a copy of HEAD, rebinding a merged audit that another audit cites, then --grandfather three times, gives check_exit=0 from pass 1 and one ledger digest 3dcea4d632d7 across all three; command:os.path.relpath over a realpath-resolved target and a non-realpath root yields "../real/scripts/audit-binding-exceptions.json", which matches no exemption; command:a six-mutant matrix catches a "root not realpath.d" mutant on 2 cases and the rejected wider fix on 5; file:scripts/check-audit-bindings.py
- **Version:** n/a — repository-local script and process.
- **Owner:** EHotwagner/rogue3 worker process — the review discipline, not the product. The generalisable half belongs to whoever owns the item-worker brief.
- **Recurrence:** seen again, and the brief predicted it. The item brief states that on every cycle in this repo so far a second independent fresh-context critic found something the first accepted, and that last cycle three critics each changed what shipped against a candidate already green with a `shipReady` verdict. This cycle is the fourth consecutive instance, and the first where a critic caught a change that would have silently disabled itself.
- **Avoidable cost:** a complete second implementation pass — one narrowed fix, one blocking bug fix, four output-honesty corrections, a rewritten docstring, a corrected ledger note, and a self-test section deleted and rebuilt. Roughly half the cycle's implementation effort, all of it after the candidate was green.
- **Disposition:** accepted, and recorded as the cycle's most transferable observation. Two critics with **different briefs** — one adversarial on code and licensed to mutate, one adversarial on design and policy — each found what the other did not, which is the argument for briefing them differently rather than running the same rubric twice. Concrete proposal in §11.7.

## §5 Did not exercise

- **Runtime and playtest.** No F# changed and no frame was rendered. `./fake.sh build -t Run` was not invoked. The `pnext-item` performance gate was correctly not invoked, nothing interactive having changed.
- **The FAKE `Verify` target.** Deliberately not run: the change touches no F#, and issue:EHotwagner/rogue3#26 records that a green `Verify` silently rewrites tracked `readiness/` artifacts — outside this item's touch-set. Not running it is why `git status` stayed at exactly the declared paths.
- **The `fsgg-sdd` lifecycle.** Not authored; a single-script defect with an executable acceptance criterion. Recorded in the phase-2 checkpoint rather than skipped silently.
- **Packaging and upgrade.** No pin moved.
- **macOS.** §4.7's blocking bug is stated for macOS by inference from `/var → /private/var`; it was reproduced on Linux with a symlinked `$TMPDIR`. No macOS runner was available.
- **The audit-immutability mechanism the docstring names.** "git history, not a digest the same commit can rewrite" is named as the right home for audit immutability and is not built here; §11.4.

## §6 Doc-versus-behavior contradictions

- **The checker's remedy text contradicted its behaviour.** At `d8d0024` it documented `--grandfather` as the remedy that leaves the tree clean, and its failure message told the reader to run exactly that "then commit the ledger". For a binding onto the ledger, running it produced a new violation every time. Rewritten. Owner: this repository.
- **The checker's remedy *order* contradicted the repository's own precedent.** "Remedies, in preference order: 1. Rebind the audit … 2. Excuse it explicitly" against `7e71d71` ("excuse rather than rebind"), issue #38, and #40, all of which treat rebinding a merged audit as the practice to eliminate. The ledger's own embedded note said the same thing ("Prefer rebinding the audit over adding an entry here"). This was not cosmetic: it was the premise the rejected wider exemption stood on. Both corrected. Owner: this repository.
- **The checker claims an alignment with the validator that no longer holds in full.** "This is byte-for-byte the rule the feedback tool applies … so a file this checker calls fresh is a file the validator calls fresh." True of the *hashing rule*, and the sentence is not false as written; but the two tools now disagree about which files are *bindable at all* (§4.3), and a reader will take it as a general agreement claim. Owner: split between this repository's docstring and the FS-GG `fs-gg-feedback-report` skill.

## §7 Workarounds still in the tree

- **`scripts/audit-binding-exceptions.json`, now 63 entries** (61 at `d8d0024`, plus two for this cycle's own edit to the checker source). Still the standing workaround recorded at 45 entries in `feedback/2026-08-02-Rogue3-3.md` §7 and 51 in `feedback/2026-08-02-Rogue3-7.md` §4.4. This cycle removed the *unsatisfiable* class of entry and did nothing about growth: excusing is still the only remedy with tooling, because no `rebind` command exists. **Removal condition:** a `rebind` subcommand in `feedback-tool.fsx` (§11.5). **Risk:** an ever-growing ledger normalises "excused" as the resting state of the evidence system, and the entry count structurally understates the number of distinct excusing events.
- **The two entries this cycle added**, excusing the `-3` and `-6` audits on `file:scripts/check-audit-bindings.py`. **Removal condition:** none — permanent by design under `7e71d71`, because those critics examined the pre-rewrite file. **Risk:** low and intended.
- **`feedback/2026-08-02-Rogue3-7.md` is un-revalidatable on `main` after this merge** (§4.3). A hole rather than a workaround. **Removal condition:** issue:EHotwagner/rogue3#40. **Risk:** the next worker who runs `validate` over the full `feedback/` set sees red and may "fix" it by hand-rebinding a merged audit — exactly what #38 was filed to end. Mitigated only by having said so on #38, #40, PR #42 and directly to the sibling worker who will hit it next.

## §8 Friction and avoidable cost

- **A full second implementation pass** after the candidate was green (§4.7): a narrowed fix, one blocking bug fix, four output-honesty corrections, a rewritten docstring, a corrected ledger note, and a rebuilt self-test section. The single largest cost of the cycle.
- **Refused shell commands: six**, each relocating work into a scratch file (§4.6). Four scratch scripts that would otherwise have been inline.
- **Reproductions run: three** — one partial-tree (misleading, discarded), one full-tree, one to verify a critic's disconfirming claim rather than take it on trust.
- **Self-test cases rewritten or deleted: four** — one caught by mutation, three by a critic noting no mutant killed them.
- **`--grandfather` runs on the real tree: 13**, of which three changed bytes; carry-forward made the convergence probes free.
- **Lifecycle reruns: none. Generated files replaced: none. Worker restarts: none. Reverts: none** — the second commit narrows rather than reverts.
- **Command duration**, kept separate from wall-clock: `dotnet test` 4s (228 tests, run twice); `--selftest` under 1s; a four-pass convergence proof under 3s; the six-mutant matrix a few seconds. The cycle's cost was reading, judgement and waiting on critics, not compute.
- **Board writes:** one `take`, three `widen`, one `add`, one `set-field --batch`, two `say`, several `heartbeat`.
- **Coordination:** four inbox messages from two sibling workers, both waiting on this item — `rook-f046` on #20 and `godwit-d5f5` on #34, which is hard-blocked on #38. Both collide on `scripts/audit-binding-exceptions.json`, and `take` admitted #38 into the wave anyway despite #20 declaring that path 29 seconds earlier. That scheduler defect is `godwit-d5f5`'s finding, filed at root cause as issue:EHotwagner/rogue3#41; recorded here as cost, not re-filed. Resolved by ordering: #38 lands first, both siblings rebase, and both skip the hand-prune-and-rebind route this fix removes.

## §9 Skill value and gaps

Inventory: `.agents/skills/skill-manifest.json`, 45 skills present.

- **Invoked:** `fs-gg-feedback-report`, in both modes — five checkpoints appended and validated (`validate-checkpoint-state` exit 0), and this report authored against its §1–§12 contract. Its `draft → cold critique → evidence verification → resolution` sequence is what produced §4.7; the draft was held outside `feedback/` until both critics had reported, which is the only reason the report describes the fix that shipped rather than the one that did not.
- **Relevant, not invoked, with reason:** the eleven `fs-gg-sdd-*` stage skills — a single-script defect with an executable acceptance criterion. `fs-gg-testing` — its guidance is written for F# tests over the generated product; this cycle's tests are Python cases embedded in the script under change, and nothing in it applies. `pnext-item` — fronts a performance gate for interactive work. The gameplay skills are out of scope for a CI-gate defect.
- **Wanted and absent:** a skill covering this repository's **evidence machinery as a system** — the audit gate, the ledger, the excuse-versus-rebind precedent, and the interaction between the Python gate and the F# validator. That knowledge currently lives in one script's docstring, one commit message (`7e71d71`), and scattered §4 entries across five prior reports. Reconstructing it took `git show 7e71d71`, three prior reports and 998 lines of Python before a one-function change could be judged safe. It is the largest onboarding cost of the cycle and no skill covers it. It is also what the first candidate got wrong.
- **Misleading guidance:** `fs-gg-feedback-report` prescribes `file:` locators with digests and never warns that citing a file the evidence system itself writes — the ledger, or another audit — produces a binding the system cannot keep valid. That omission is the direct cause of §4.3 and of `feedback/2026-08-02-Rogue3-7.md` §4.4. This report deliberately cites the ledger only through `command:` locators for that reason.
- **Overlap:** none observed.

## §10 Outcome markers

- **Time to first build:** not a scaffold measure here. `dotnet test` green at `d8d0024` on the first invocation, 228 passed, 4s.
- **Time to first reproduction:** the second attempt; the first was mis-scoped (§3).
- **First meaningful test:** the new `--selftest` cases, green on their first run after the fix, then mutation-tested — which is where the first false-coverage case surfaced.
- **First green verification:** `check` exit 0 and `--selftest` exit 0 after a single `--grandfather` pass; four further passes gave one ledger digest.
- **First green CI:** PR #42 at `71e3a18` — a candidate later rejected. **Green CI preceded correctness by a full implementation pass** (§4.7), which is the comparable outcome worth carrying forward.
- **Ship readiness:** PR #42 at `c6ceb60`, both checks green, `verify-paths` OK.
- **First render / playable state:** not exercised.
- **Critics:** two, both fresh-context subagents, run concurrently against the pushed branch with **different briefs** — one adversarial on code with explicit mutation licence, one adversarial on design and policy. Each found what the other did not. Both returned `do not merge as written`; both findings were verified independently by the author before being acted on, and one required disproving a claim the author had written.
- **Test counts** (supporting, not substitutes): 228 `dotnet test` cases unchanged and green throughout; `--selftest` 53 → 72; mutants 2 → 6, all caught.
- **Estimates:** all timings above are measured command durations. No wall-clock estimate is offered; the cycle's cost was judgement and critic latency, and no artifact records it.

## §11 Falsifiable improvements

1. **Give `feedback-tool.fsx` the same ledger exemption the gate now has.** Prevents §4.3. Owner: FS-GG `fs-gg-feedback-report`, `scripts/feedback-tool.fsx`. Acceptance: with a report whose audit cites the ledger, running `--grandfather` for an unrelated violation and then `validate` on that report exits 0, and the validator names the citation it did not check. Tracked as issue:EHotwagner/rogue3#40.
2. **Declare the non-bindable path set once and have both tools read it.** Prevents recurrence of §4.3 and the §6 contradiction. Owner: FS-GG `fs-gg-feedback-report`. Acceptance: one declared list that both `check-audit-bindings.py` and `feedback-tool.fsx` consume, such that adding an entry changes both tools with no second edit.
3. **Make `--grandfather` return the verdict, not the write.** Discharges §4.2 independently of §4.1, which it currently is not. Owner: EHotwagner/rogue3 `scripts/check-audit-bindings.py`. Acceptance: `grandfather()` re-evaluates after writing and returns non-zero when `check` would; a self-test seeds a violation the rewrite cannot clear and asserts a non-zero exit.
4. **Build the audit-immutability check the docstring names.** The docstring says audit immutability needs git history rather than a digest the same commit can rewrite, and nothing implements it. Owner: EHotwagner/rogue3 `.github/workflows/audit-bindings.yml`. Acceptance: with `fetch-depth: 0`, a PR that modifies an existing `feedback/audits/*.audit.json` fails, and — the part that matters — the failure has a documented, bounded route to green, so this does not become a second #38.
5. **Add a `rebind` subcommand to `feedback-tool.fsx`.** Attacks §7's growth at its root: excusing is the default only because the alternative has no tooling, a gap `feedback/2026-08-02-Rogue3-3.md` §4.2 recorded and this cycle's docstring still has to apologise for. Owner: FS-GG `fs-gg-feedback-report`. Acceptance: one command recomputes every `file:` digest in an audit, and a cycle that legitimately rebinds adds zero ledger entries.
6. **Prescribe mutation-testing new regression tests against the unfixed code.** Generalises §4.4. Owner: FS-GG `fs-gg-testing`. Acceptance: the skill states the method, and a defect-fix cycle records which of its new tests fail with the fix reverted. PR #42 carries the worked example.
7. **Brief concurrent critics differently, and require one to attack the *justification* rather than the code.** Generalises §4.7. Owner: the FS-GG item-worker brief. The brief already mandates at least two critics; it does not say they should be given different rubrics, and this cycle's two findings were each invisible to the other's brief. Acceptance: the brief names at least one code-level and one design/policy critic with distinct rubrics, and a cycle records which brief found which finding — this report's §4.7 being the first such record.

## §12 Development-surface coverage

| Surface | Status | Evidence and result |
|---|---|---|
| scaffolding | not-exercised | No scaffold or regeneration ran; `.fsgg/scaffold-provenance.json` read for §1 provenance only. |
| onboarding-guidance | exercised | Reconstructing the evidence system's rules from `git show 7e71d71`, three prior reports and 998 lines of Python was the precondition for a one-function change, and the first candidate got it wrong anyway (§4.7, §9). |
| skills | partial | `fs-gg-feedback-report` invoked in both modes and validated; eleven `fs-gg-sdd-*` and `fs-gg-testing` deliberately not invoked with reasons recorded (§9). |
| sdd-authoring | not-exercised | Lifecycle deliberately not authored for an S/M single-script defect; recorded in the phase-2 checkpoint rather than skipped silently (§5). |
| implementation-apis | not-exercised | No FS.GG API touched; the change is Python and JSON only. |
| dependencies-build | partial | `dotnet test` built and ran the full solution twice, 228 green each time; no pin moved and no package resolved differently. |
| testing | exercised | 53 pre-existing self-test cases carried two core rewrites unchanged; 19 added; six mutants, all caught; four cases rewritten or deleted after mutation and critic review exposed them as non-binding (§4.4). |
| evidence | exercised | Five checkpoints, `validate-checkpoint-state` exit 0, this report and its audit — and the cycle's central findings are *about* the evidence machinery (§4.1, §4.3, §7). |
| runtime-playtest | not-exercised | No F# changed, no frame rendered, the game never launched (§5). |
| performance | not-exercised | No performance surface touched; the `pnext-item` gate correctly not invoked and no timing artifact needed. |
| documentation | exercised | The module docstring rewritten twice — the second time to delete a claim a critic disproved — plus the ledger's embedded note corrected and the remedy preference order reversed to match `7e71d71` (§6). |
| packaging-upgrade | not-exercised | No package pin moved; no upgrade path exercised. |
| worker-git-pr | exercised | Claim at exit 0, three `widen` calls, isolated worktree, branch from `origin/main`, two commits, PR #42, two differently-briefed fresh-context critics that both returned "do not merge as written" (§4.7), one root-cause issue filed (#40) and one sibling's scheduler defect referenced (#41), four inbox messages resolved by ordering; six harness refusals recorded as friction (§4.6). |

---
feedbackSchema: 2
date: 2026-08-02
workspace: Rogue3
cycle: item-77-exempt-citation-existence-waiver
lane: none
toolVersion: n/a
commit: 14fd9eb
---

# Rogue3 cycle feedback — item-77-exempt-citation-existence-waiver

## §1 Provenance and confidence

- **activation:** active
- **phases:** onboarding-first-build, lifecycle-authoring-or-not-used, implementation-test-evidence, verify-ship-pr
- **material events:** 9
- **zero-event reason:** n/a
- Cycle boundary: item EHotwagner/rogue3#77. Work was written against `ccff119` and **rebased onto
  `14fd9eb`** before merge, after a sibling landed `#60`. The frontmatter `commit:` names the merge
  base; where a finding below quotes a figure it names the revision that figure was measured at, and
  `ccff119` appearing in a finding is deliberate rather than stale. §10's corpus figures and every
  gate figure are re-measured at `14fd9eb`, because two of this report's own wrong claims came from
  not doing that (§4.11 g, h). Work carried on branch
  `item-77-exempt-citation-existence-waiver` in an isolated worktree, never on `main`.
- Checkpoints: `feedback/checkpoints/item-77-exempt-citation-existence-waiver.jsonl`, 9 events
  spread across `onboarding-first-build`, `implementation-test-evidence` (×3) and
  `verify-ship-pr` (×5).
- Lane: `none`. Board `hardening` item worked directly against the `fs-gg-feedback-report` kit
  skill and `scripts/check-audit-bindings.py`. No `work/<id>/` tree exists or was authored, so no
  `fsgg-sdd` stage ran (§5).
- **Every quantity in the issue body and in this cycle's brief was stale and was re-derived against
  `ccff119` before anything was planned** (§4.1). Three separate figures were wrong in three
  different directions.
- Confidence limits, stated plainly. **First**, the mutation figure below is decided solely by
  `feedback-tool.fsx -- selftest` exiting non-zero; it exercises neither the F# tool's real CLI
  against a real repository report (that is asserted separately, §4.2) nor `check-audit-bindings.py`.
  **Second**, the two `.gitignore` declaration readers are now a hand port of one another across two
  languages, and what pins their agreement is a pair of comments and a differential check run by a
  critic — not a test either tool runs (§4.9, §11.2). **Third**, elapsed time was not instrumented;
  the proportions in §8 are explicit estimates.

## §2 What worked

- **The issue's acceptance criteria named the mutant, so the test suite could be written before the
  fix.** rogue3#77's third bullet asks for "a mutant that restores the existence check for an exempt
  path is killed by it". That is a sentence a worker can execute directly: write the case, apply the
  mutant, count the failures. It was applied literally and is killed by **13 of 88** cases on the
  shipped tree. Almost every acceptance criterion in this repository's backlog is a claim about
  behaviour; naming the mutant as well turns it into a claim about the *test*, which is the part
  that rots.
- **`feedback-tool.fsx -- selftest` carried the change end to end in under thirty seconds a run.**
  49 → 88 cases, still sub-second after the first `dotnet fsi` warm-up, each case building a
  throwaway workspace on disk. It was the inner loop, not a final gate: 13 mutants were applied and
  reverted against it across the cycle.
- **Two critics with different rubrics found disjoint defect sets, and the split was the point.**
  The code critic, given a mutation licence and its own worktree, found nine surviving mutants and
  a differential divergence between the two `.gitignore` readers. The doctrine critic, told
  explicitly *not* to mutate, found four false sentences, an un-guarded constant, and a measured
  regression across every merged report that no mutant could have surfaced. Neither found the
  other's list. A single critic asked to do both would have done one of them.
- **The one existing case that asserted the DEFECT was easy to find, because it said what it
  believed and why.** `"a citation onto a MISSING ledger is still an error -- only the digest has no
  fixed point, existence does"` is a test name that states the doctrine it encodes. Inverting the
  behaviour meant inverting one named case rather than hunting for which of 49 assertions was load
  bearing.
- **`git status --porcelain` completely empty after `Verify`, as promised by #56.** Verified at exit
  0 on the candidate. Every dirty file this cycle produced was one it wrote on purpose.

## §3 What did not

- **The two tools' exempt path SETS had silently diverged, and nothing reported it.** Both files
  carry paragraphs insisting they must agree — `FeedbackReportTool.digestExemption`'s doc comment
  says "The two must stay in agreement: a file the checker calls unbindable is a file this validator
  must not call stale, and vice versa" — and they had disagreed since rogue3#56 merged (§4.3). The
  agreement is asserted in prose in both files and checked by nothing.
- **Repinning one edited `SKILL.md` took two `TemplateDrift` / `KitPins` rounds** because the
  generated manifest pins it, `KitPins` does not touch the manifest, and `kit-pins.json` pins the
  manifest in turn (§4.10). Already filed as rogue3#78; this cycle is a second occurrence.
- **The bash environment refuses any command it cannot statically prove stays inside the worktree**,
  which includes every `cmd > file`, every heredoc and every pipeline whose exit code matters. Every
  such invocation had to be split into a written script plus a plain call (§4.12).

## §4 Findings

#### §4.1 Three of the item's own figures were stale against `main`, in three different directions

- **Kind:** documentation
- **Impact:** Any plan built on the issue body would have been built on wrong numbers; one of the
  three would have made a claim in this report false.
- **Expected:** An issue body and a cycle brief quoting tool output are usable as written, or say
  which revision they were measured at.
- **Observed:** Measured at `ccff119`. (a) The issue body names **four** merged audits citing
  `file:scripts/audit-binding-exceptions.json`; there are **five** — `2026-08-02-Rogue3-18.audit.json`
  was added after the body was written. (b) The second comment states
  `feedback/2026-08-02-Rogue3-17.md` "validates green today, exit 0"; it exits **1**, on three stale
  digests over `build.fsx` and `.github/workflows/verify.yml` that moved under #76/#79. Its
  ledger citations are still NOT BOUND, so the *point* the comment makes survives, but the figure
  does not. (c) This cycle's brief quotes `SelfTest` at 111/0 and 276 tests; `SelfTest` is
  **125** cases at `ccff119` and this change touches no `build.fsx`, so 125 is `main`'s number and
  111 was inherited from cycle #15. The tests figure, 276, was correct at `ccff119` — and is 277 at
  the `14fd9eb` this merges into, which is the same going-stale-under-you effect §4.11 (h) records
  against this report.
- **Evidence:** command:python3 scripts/check-audit-bindings.py --json reports 20 notBound citations at ccff119, of which 5 name scripts/audit-binding-exceptions.json across audits 2026-08-02-Rogue3-{7,8,11,17,18}; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- validate feedback/2026-08-02-Rogue3-17.md --audit feedback/audits/2026-08-02-Rogue3-17.audit.json exits 1 with three "digest is stale" errors; command:./fake.sh build -t SelfTest reports "SelfTest: 125 case(s), 0 failure(s)"; issue:EHotwagner/rogue3#81
- **Version:** ccff119
- **Owner:** EHotwagner/rogue3 issue authoring
- **Recurrence:** known — rogue3#81 is open for exactly this, filed by the #53 cycle whose own body quoted 22/216/72 against an actual 29/271/96. This is the next occurrence, and the first where a *comment* rather than a body carried the stale figure.
- **Avoidable cost:** ~20 minutes re-deriving three figures before any code was read.
- **Disposition:** already tracked as issue:EHotwagner/rogue3#81; not re-filed.

#### §4.2 The existence check ran upstream of the exemption, so an exempt path could never be deleted

- **Kind:** defect
- **Impact:** Every exemption acquired a permanent path fixture. rogue3#53 kept an emptied ledger
  file in the tree purely to satisfy it, and wrote an unconditional never-delete-a-ledger-file rule
  in a second tool as a workaround.
- **Expected:** An exemption says the validator has given up tracking a file, so neither its digest
  nor its presence is checked.
- **Observed:** At `ccff119`, `validateActionabilityAuditDetailed` tested `File.Exists` at
  `FeedbackReportTool.fs:657`, upstream of the `digestExemption` match at `:662`. The rule therefore
  forbade the change that cannot mislead a reader — deleting a file nothing is checking — and
  permitted the one that can, editing it. Fixed by deciding the exemption on the resolved
  workspace-relative path *before* existence is tested. `resolveEvidencePath` is unchanged, so a
  locator escaping the workspace is still an error, exempt or not. The pre-existing case that
  asserted the defect is inverted; applying the old ordering as a mutant fails **13 of 88** cases on
  the shipped tree, four of them driving the real CLI rather than the library. (An earlier draft of
  this finding quoted 10 of 68 — true of an intermediate commit and stale by the time the suite
  reached 88. A fresh-context critic caught it, and it is the same defect class §4.1 records against
  someone else, committed here by this report. Corrected, and named rather than quietly fixed.)
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs; file:.agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest reports 88/88 on the candidate and "13 of 88 cases FAILED" with the existence test restored upstream of the exemption; issue:EHotwagner/rogue3#77
- **Version:** ccff119 → candidate
- **Owner:** FS-GG `fs-gg-feedback-report` kit
- **Recurrence:** first fix; the defect itself was found and recorded by the #53 cycle (§4.2 of `feedback/2026-08-02-Rogue3-20.md`) and filed as rogue3#77.
- **Avoidable cost:** the fix is one reordering; the doctrine around it took longer to write than the code.
- **Disposition:** fixed in this cycle.

#### §4.3 The two tools' exempt path sets had diverged, and both files claim in prose that they cannot

- **Kind:** defect
- **Impact:** The same citation was reported `notBound` by the gate and `evidence file is missing` by
  the validator. rogue3#56 shipped knowing this and could not fix it, because the roll-ups are
  *absent* in a fresh checkout and the validator required an exempt path to exist — so #77's
  ordering fix was a precondition for closing it.
- **Expected:** A path one tool calls unbindable is a path the other does not bind.
- **Observed:** `DERIVED_ROLLUP_RELPATHS` in `scripts/check-audit-bindings.py` exempts
  `readiness/evidence-graph.md`, `readiness/performance-evidence.json` and
  `readiness/m7-ui-performance.json`; `digestExemption` knew nothing about them. All three are absent
  in a clean checkout of `ccff119`. Fixed by porting `gitignore_declarations` and
  `derived_exemptions` to F# as `gitignoreDeclarations` and `derivedExemptions`, keyed on the same
  two sources — the tool's own enumerated list **and** the workspace `.gitignore` — with the derived
  map passed to a new `digestExemptionWith`. `digestExemption` remains as a one-argument wrapper
  that passes an empty map, so a caller that forgets *under*-exempts, which is loud.
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs; file:scripts/check-audit-bindings.py; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- validate feedback/2026-08-01-Rogue3-8.md --audit feedback/audits/2026-08-01-Rogue3-8.audit.json reports its two readiness roll-up citations under NOT BOUND on the candidate, where ccff119 reported "evidence file is missing" for both; issue:EHotwagner/rogue3#56
- **Version:** ccff119 → candidate
- **Owner:** FS-GG `fs-gg-feedback-report` kit
- **Recurrence:** first fix.
- **Avoidable cost:** a second hand port of a `.gitignore` declaration reader; roughly 40 lines now exist twice in two languages.
- **Disposition:** fixed in this cycle; the residual — that nothing *tests* the two ports agree — is §11.2.

#### §4.4 A worker enumerating one seam's producers found exactly one call site, and that is what made the change safe

- **Kind:** positive-pattern
- **Impact:** The reorder touched one `match` arm and could not have touched more, which is why a
  270-line diff carries a one-line behavioural change.
- **Expected:** A change to an exemption rule has to be traced to every place that consults it.
- **Observed:** `grep -rn "digestExemption\|validateActionabilityAudit\|ledgerExemption"` over the
  whole tree returns exactly one call site of `digestExemption`
  (`FeedbackReportTool.fs:662`) and three of `validateActionabilityAuditDetailed`, all inside
  `feedback-tool.fsx`. A second grep for the *other* seam — every place that decides whether a cited
  evidence file must exist — returns `FeedbackReportTool.fs:657` and
  `check-audit-bindings.py:986` and nothing else;
  `.agents/skills/work-board/scripts/validate-feedback-state.py` touches no citation at all. Both
  greps ran before the first edit, not after.
- **Evidence:** command:grep -rn "digestExemption\|validateActionabilityAudit\|ledgerExemption\|ledgerRelativePath" over --include=*.fs,*.fsx,*.md,*.json,*.py,*.yml returns one call site of digestExemption; command:grep -rn "file is missing\|does not exist\|File.Exists\|os.path.isfile" over .agents/skills, .claude/skills and scripts returns two citation-existence seams; file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs
- **Version:** candidate
- **Owner:** worker practice
- **Recurrence:** the inverse was recorded by a worker earlier today — "I had placed three guards on the seam the issue named and none on the seam it did not. A `grep` for the symbol would have found it in seconds; three reading passes did not run it."
- **Avoidable cost:** none; two greps, under a minute, before any edit.
- **Disposition:** worth promoting into the item-worker brief as a required step rather than a lesson.

#### §4.5 The grep that found one call site also found a CI job whose "three statements that must agree" the change had quietly made four

- **Kind:** defect
- **Impact:** `.github/workflows/verify.yml`'s "Derived roll-ups stay out of the index" job asserts
  that its own hard-coded roll-up list matches `DERIVED_ROLLUP_RELPATHS`. This change introduced a
  third copy of that list in F# and the job could not see it, so the two constants could have
  diverged with the job green — the exact failure class §4.3 is an instance of, reintroduced by the
  fix for §4.3.
- **Expected:** A job that exists to prove N independent statements of one list agree covers all N.
- **Observed:** The job's own comment reads "The list above is duplicated from `.gitignore` and from
  the checker on purpose — three independent statements that must agree." After this change there
  are four. Found by grepping the *other* seam rather than the one the issue named:
  `grep -rn "feedback-tool\|check-audit-bindings" .github/workflows/*.yml`, run after the
  implementation was already committed and passing every gate. Closed by extending the job to read
  `derivedRollupRelativePaths` out of the F# source and compare it to the same expected list. The
  extraction is bounded to the binding's body and its failure mode is loud in the safe direction: a
  regex that stops matching yields an empty list, which is a mismatch. Verified both ways — against
  the candidate's source it matches and exits 0; against `ccff119`'s source, where the binding does
  not exist, it prints the diff and exits 1.
- **Evidence:** file:.github/workflows/verify.yml; file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs; command:the job's sed/grep extraction over the candidate's FeedbackReportTool.fs prints the three roll-up paths and exits 0, and over the ccff119 copy of the same file prints a three-line diff against the expected list and exits 1
- **Version:** candidate
- **Owner:** EHotwagner/rogue3 `.github/workflows/verify.yml`
- **Recurrence:** first record of this instance; the class is the same as §4.3 and §4.6 — two statements of one fact, required to agree, with the agreement written down and not run.
- **Avoidable cost:** none — one grep, after the fact, before review. Had it not been run, the cost would have landed on whichever cycle next edited either list.
- **Disposition:** fixed in this cycle.

#### §4.6 A test suite that only ever asks "is this ONE path exempt" cannot see a rule that changes the SET, and eight of this cycle's own new cases were that shape

- **Kind:** defect
- **Impact:** The `.gitignore` reader shipped with seven live mutants. The three dangerous ones —
  a negation form silently not honoured — are exactly the direction the reader's own docstring
  names as the one that loses evidence: the exemption standing over a file the repository has
  resumed treating as ordinary.
- **Expected:** Cases written for a ported parser exercise the parser.
- **Observed:** Every case this cycle first wrote for `gitignoreDeclarations` drove it through a
  citation, and a citation only ever asks whether ONE path is in the returned set. Junk in that set
  is inert, so dropping the comment filter, or letting `!` lines survive as literal declarations,
  changed the returned set and no case noticed. A code critic with a mutation licence found seven
  survivors this way: `TrimEnd('/')` on the negation rule, the `Trim()` after `!`, the fourth
  negation candidate form, the per-line `Trim()`, the comment filter, the `!`-exclusion, and CRLF/CR
  normalisation. Closed by adding five cases that call the public `gitignoreDeclarations` **directly**
  and assert the whole returned set, plus three more negation forms routed through citations. All
  seven now die. The five direct cases are what kills the last two: delete them from the shipped
  suite and it reports 83/83 green with both the comment-filter and `!`-exclusion mutants applied.
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest reports 88/88 on the candidate; command:with the comment filter removed it reports "gitignoreDeclarations: a COMMENT contributes nothing to the declared set" FAILED and with negations left in the positive set it reports two named failures, while removing the five direct gitignoreDeclarations cases leaves 83/83 green with both of those mutants applied
- **Version:** candidate
- **Owner:** this cycle
- **Recurrence:** first record of this shape. The general lesson is sharper than "test more": a case that reaches a function through a *narrower* interface than the function's contract can only ever pin that narrower thing, however many of them you write.
- **Avoidable cost:** none to the repository — the critic caught it before merge — but it is the second time in this cycle that a test I believed covered a claim covered something adjacent (see §4.5).
- **Disposition:** fixed in this cycle.

#### §4.7 The change made "containment is untouched" load-bearing and shipped a test that appeared to verify it and did not

- **Kind:** defect
- **Impact:** `resolveEvidencePath` tests containment twice — once lexically, once after
  canonicalization — and NEITHER half was individually pinned. Removing the canonicalized check let
  a merged audit bind evidence anywhere on the filesystem through a symlink inside the workspace,
  and the suite stayed green — 68/68 at commit `be74e52`, which is the only tree that figure
  describes. (The critic measured it at `b689137`, the pre-rebase hash of that same commit; the
  rebase orphaned that hash and `be74e52` is its reachable equivalent — same subject, same
  `minimumCases = 66`, byte-identical kit sources. See §4.11 (k).)
- **Expected:** A claim three files repeat is pinned by a case that fails when it stops being true.
- **Observed:** The case this cycle wrote, `an ESCAPING locator onto an exempt path is still an
  error`, cites `file:../scripts/audit-binding-exceptions.json` — a *lexical* escape, which BOTH
  checks reject. So it passes with either one deleted. The pre-existing symlink cases symlink the
  workspace *root*, never a component inside it, which is the direction the canonicalized check
  exists for. Closed with two cases built so exactly one check answers each: a symlink inside the
  workspace pointing out (only canonicalization sees it), and a lexically escaping locator whose
  target symlinks back inside (only the lexical check sees it). Both mutants now die, one case each.
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx; file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs; command:replacing the canonicalized containment test with `if true then` fails "a symlink INSIDE the workspace pointing OUT is rejected" and disabling the lexical test fails "a LEXICALLY escaping locator that symlinks back inside is still rejected", where at commit be74e52 -- before these two cases existed -- both mutants passed 68/68
- **Version:** candidate
- **Owner:** this cycle
- **Recurrence:** first record. The check itself is pre-existing; what this cycle introduced is the claim, in three files, that it had been verified.
- **Avoidable cost:** none to the repository; the critic caught it.
- **Disposition:** fixed in this cycle.

#### §4.8 A minimum-case FLOOR tolerates exactly the slack it is meant to forbid

- **Kind:** defect
- **Impact:** With the floor at 66 against 68 actual cases, a critic deleted two of this cycle's own
  assertions verbatim and the suite printed `66/66 cases passed` and exited 0 — the failure the
  floor's own comment says it exists to prevent.
- **Expected:** A guard against silently-skipped cases notices a deleted case.
- **Observed:** The floor was `total < minimumCases`, and `minimumCases` was set to total minus the
  two conditional symlink cases, so it carried two cases of slack permanently. At `ccff119` it was
  worse: 38 against 49 actual, eleven cases of slack. Replaced with an EXACT total — every
  conditional group now calls `skip` with its own case count, and the assertion is
  `total + skipped = expectedCases`. A sandbox that cannot make symlinks reports the same number as
  one that can, with named skip lines on stderr, and deleting a case is a failure rather than a
  smaller green.
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest reports "88/88 cases passed" and, with expectedCases left at a stale value, "88 cases ran and 0 were skipped, expected 85 in total"
- **Version:** ccff119 → candidate
- **Owner:** FS-GG `fs-gg-feedback-report` kit
- **Recurrence:** first record. The floor was introduced by an earlier cycle for a good reason and was never wrong, only weak.
- **Avoidable cost:** none to the repository; the critic caught it.
- **Disposition:** fixed in this cycle.

#### §4.9 Two tools now hold the same `.gitignore` declaration reader in two languages, pinned by comments alone

- **Kind:** quality-gap
- **Impact:** The exempt set is now derived twice from the same file by two independently written
  parsers. Either can drift into declaring a path the other does not, and the failure is silent in
  one direction — the direction where the F# side over-exempts.
- **Expected:** Two implementations required to agree are held to it by something that runs.
- **Observed:** `gitignoreDeclarations` in `FeedbackReportTool.fs` and `gitignore_declarations` in
  `check-audit-bindings.py` implement the same four-form negation rule, the same comment and blank
  handling, the same whole-line matching and the same deliberate refusal to interpret globs. What
  holds them together is a doc comment on each side saying they must agree, plus
  `derivedRollupRelativePaths` and `DERIVED_ROLLUP_RELPATHS` being identical lists — those two are
  now compared by CI (§4.5), but their *readers* are not. The selftest pins the F# side's behaviour
  on 14 cases — 5 driving `gitignoreDeclarations` directly, 6 negation forms, 1 padded declaration
  and 2 line-ending fixtures — and nothing anywhere pins that the *Python* side answers the same on
  the same input. No fixture is shared between the two suites.
- **Evidence:** file:.agents/skills/fs-gg-feedback-report/scripts/FeedbackReportTool.fs; file:scripts/check-audit-bindings.py; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest covers the F# reader with 14 cases -- 5 named "gitignoreDeclarations:" driven directly, 6 negation forms, 1 padded declaration, 2 line-ending fixtures -- and no case in either suite feeds both readers the same input
- **Version:** candidate
- **Owner:** FS-GG `fs-gg-feedback-report` kit and EHotwagner/rogue3 `scripts/`
- **Recurrence:** third instance of one class recorded in this cycle — §4.3 the exempt sets, §4.5 the constants that name them, and this the readers that derive them: two statements required to agree, with the agreement written down and not run. The first two are closed; this one is not.
- **Avoidable cost:** none yet; the cost lands on the cycle that hits the drift.
- **Disposition:** recorded, not fixed; §11.2.

#### §4.10 `TemplateDrift` names the drifted file precisely and names no command that repins a manifest entry

- **Kind:** friction
- **Impact:** Two extra `TemplateDrift` / `KitPins` rounds for one edited `SKILL.md`.
- **Expected:** A violation message names the command that clears it.
- **Observed:** Editing `.agents/skills/fs-gg-feedback-report/SKILL.md` produced seven violations.
  `KitPins` cleared the two over `FeedbackReportTool.fs` and `feedback-tool.fsx` and their mirrors,
  but the `SKILL.md` pin lives in the *generated* `skill-manifest.json`, which `KitPins` does not
  write; its digest had to be computed with `sha256sum` and pasted into both manifest copies by
  hand. That edit then drifted `skill-manifest.json` itself, which `kit-pins.json` pins, so
  `KitPins` had to run a second time. Three rounds of a two-command loop to land one documentation
  edit.
- **Evidence:** command:./fake.sh build -t TemplateDrift on the first attempt reports "7 violation(s)" naming .agents/skills/fs-gg-feedback-report/SKILL.md pinned 52b6eab3cdcc found 4c9a4e9503a6, and after the manual manifest edit reports 2 violations naming .agents/skills/skill-manifest.json pinned d26f8898ec65 found 6b51adecb3ac; command:./fake.sh build -t KitPins; file:.agents/skills/skill-manifest.json; issue:EHotwagner/rogue3#78
- **Version:** ccff119
- **Owner:** EHotwagner/rogue3 `build.fsx`
- **Recurrence:** known — filed as rogue3#78 by the #53 cycle after the same experience; this is the second recorded occurrence, and it adds no new mechanism. An earlier draft claimed it confirmed a second `KitPins` round "that #78's body does not mention"; the body carries it verbatim as repair step 3 and says outright "the repair is three manual steps". A fresh-context critic marked the citation `contradictory` and was right. A correcting comment was posted on #78 withdrawing the claim, because the original comment was filed against someone else's issue and is now wrong on the record.
- **Avoidable cost:** ~6 minutes and two extra full gate runs.
- **Disposition:** already tracked as issue:EHotwagner/rogue3#78; a comment recording the second round was added rather than a new issue.

#### §4.11 This report shipped eleven wrong claims of its own, across twelve locations, and only five were in the draft it started from

- **Kind:** defect
- **Impact:** A report whose §4.1 documents someone else's stale figures carried eleven wrong claims
  of its own — one propagated into the shipped `SKILL.md` where the next worker would read it as
  fact, and one into a comment on another worker's issue. **Only five were in the report as first
  written. Five were introduced by the rounds that corrected the earlier ones, and one by the
  rebase** — which is the useful half of this finding: a round that fixes stale numbers is a round
  that writes new ones, and two of those five were created by the very sentences recording that fact.
- **Expected:** A cycle that files a finding about stale quantities does not commit the same defect
  in the document that files it.
- **Observed:** A fresh-context actionability critic re-derived every figure against the branch tip
  and returned four findings `incomplete` and one evidence locator `contradictory`. (a) The headline
  mutant figure said "10 of 68"; on the shipped tree it is **13 of 88**, four CLI-driving not three —
  10-of-68 was true of the intermediate commit now reachable as `be74e52` and named no revision. (b) the SAME figure had
  reached `SKILL.md`, which told the next reader "fails on ten cases" — one claim, two places, which
  is why the five claims in the report as first written occupy six lettered locations — that phrase
  is scoped to the first row of the table below, not to the total. (c) §4.6 quoted an "85/85" pre-state that no reachable state of
  the suite has; the real figure is 83/83. (d) §4.9 claimed "8 cases, three of them negation forms"
  where 14 cases touch the reader — 8-with-3 describes only what this cycle *added*. (e) §4.10
  claimed #78's body does not mention a second `KitPins` round; the body says "the repair is three
  manual steps" and lists it as step 3, so a comment this cycle posted on someone else's issue was
  wrong on the record and has been withdrawn. (f) §10's own split said "18 / 13 / 1" one sentence
  after saying "both losses" — 17 / 13 / 2 at the base it was measured at.
  **Then the correction rounds produced four more, all of the same class.** (g) One left §10 and §12
  quoting `check-audit-bindings.py` at "exit 0 … 178 fresh, 110 excused, 55 dormant" while that same
  correction had re-staled bindings over `SKILL.md` and both `skill-manifest.json` copies — the gate
  was at **exit 1** for the duration, and needed a second `--grandfather` pass. A correction round
  that moves bytes invalidates the excuses written for the previous round; rogue3#53 §4.8 predicted
  exactly this. (h) §10's headline "2 green merged reports
  to 0" was true at base `ccff119` and **false at `14fd9eb`**, the tree this actually merges into:
  `Rogue3-18` had already gone red there under a sibling's `#60` merge, so its loss is not this
  cycle's, and `Rogue3-21` — green at `14fd9eb`, absent from the `ccff119` sweep — survives. Measured
  on both trees over the 33 pre-existing pairs: 2 pass / 31 fail on `main`, 1 pass / 32 fail on the
  candidate. This cycle reds exactly one historical report.
  **(i) Then the commit that wrote (g) and (h) invalidated the audit binding, and I reported an
  `exit 0` recalled from before it.** Rewriting §4.11 changed the report bytes and added a `command:`
  locator the signed audit had no check for, so `validate` was at **exit 1** —
  `reportSha256 does not bind the current report bytes`, plus an unmatched locator and a stale
  self-citation digest — at the moment I reported it green. Caught by the host's own sweep of my
  branch. The shape is identical to (g) and (h) and one turn tighter: **the sentence describing the
  problem is what re-staled the binding that proved the sentence.**
  **(j) And the same commit said the gate took THREE `--grandfather` passes; it took two.** Counted
  from the recorded invocations, not recalled. Reported to the host as "three passes, not one" before
  being checked. Caught by me before the final signature — the only item on this list that was — but
  it is the same defect and is listed rather than dropped for being self-caught.
  **(k) The rebase orphaned the commit hash three of these findings cite.** §4.7 twice and (a) once
  named `b689137`; after the rebase and force-push no ref contains it — `git branch -a --contains`
  is empty and it appears in no remote — so a reader in a fresh clone cannot resolve it, and it
  survives only in the object stores of agents that fetched before the push. Corrected to `be74e52`,
  its reachable equivalent: same subject, same `minimumCases = 66`, byte-identical kit sources
  (`68dab1bff6f26748…`, `cfdedd7411b17583…`). The observations were true; only the locator rotted.
  **This one was produced by neither the original draft nor a correction round but by the REBASE**,
  which is a fourth mechanism and the reason a provenance hash is not automatically safer than a
  bare number: naming the revision fixes staleness only while the revision stays reachable.

  **(l) And §10's corpus figure ASSUMED this report's own audit verdict.** It read "34 pairs,
  pass=2", counting `Rogue3-23` as green. That is not a stale measurement and re-running it cannot
  fix it: whether this report validates is the critic's verdict, not a property of the tree, so the
  figure asserted its own conclusion. As pushed it was in fact 34 pairs / pass=1 / fail=33. Corrected
  by measuring over the **33 pre-existing pairs**, which is audit-independent and carries the entire
  point about `Rogue3-18` and `Rogue3-21` unchanged, and reporting this report's own status
  separately. **This is the one item on the list that is not a staleness defect at all** — it is a
  category error, and it is the reason §11.6 needs a rule about *what may be asserted* and not only
  about *when to measure*.

  **The arithmetic, recomputed from the letters rather than from memory** — it has been wrong three
  revisions running, so it is shown as a sum:

  | source | claims | letters |
  |---|---|---|
  | in the report as first written | 5 | (a)+(b) one claim in two files, (c), (d), (e), (f) — 6 letters |
  | the round that corrected those | 3 | (g), (h), (l) |
  | the round that recorded (g), (h) | 2 | (i), (j) |
  | the rebase | 1 | (k) |
  | **total** | **11** | **12** |

  So **eleven distinct wrong claims across twelve lettered locations**, of which the correction
  rounds produced five, the rebase one, and the original draft five. The "five claims occupy six
  locations" phrasing inside (b) is scoped to the first row of that table and says so, rather than
  reading as the total — a leftover of that phrasing surviving into a later revision was itself
  flagged by an audit as the finding contradicting its own heading.
- **Evidence:** file:feedback/2026-08-02-Rogue3-23.md; file:.agents/skills/fs-gg-feedback-report/SKILL.md; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest reports 88/88 and 13 of 88 FAILED with the pre-#77 ordering restored, against the "10 of 68" an earlier draft quoted; command:a corpus sweep over the 33 pre-existing report/audit pairs, excluding this cycle's own, reports pass=2 fail=31 on main at 14fd9eb (green: Rogue3-19, Rogue3-21) and pass=1 fail=32 on the candidate (green: Rogue3-21), against the "2 green to 0" an earlier draft quoted at base ccff119 and the "34 pairs, pass=2" a later one asserted; command:git branch -a --contains b689137 is empty after the rebase while be74e52 is contained by this branch and carries the same subject, the same minimumCases = 66 and byte-identical kit sources 68dab1bff6f26748 and cfdedd7411b17583; issue:EHotwagner/rogue3#81
- **Version:** candidate
- **Owner:** this cycle
- **Recurrence:** §4.1 is the same class in someone else's document; this is the class turning up in mine. Both are instances of rogue3#81 — a quantity quoted without the revision it was measured at. Items (g) through (k) sharpen it four ways: **the correction round is itself a recurrence site**; (h) shows the figure did not have to be wrong when written, since it went stale because a sibling merged during review; (i) shows the tightest loop of all, where the sentence recording the defect invalidated the evidence proving it; and (k) shows that **naming the revision — rogue3#81's own remedy — fixes staleness only while that revision stays reachable**, which a rebase ends. The count itself was wrong in three consecutive revisions, which is why it is now shown as a sum over the letters rather than asserted.
- **Avoidable cost:** none to the repository — every one was caught before merge, which is what the audit and the host's independent sweeps are for. But the shape is worth the cost: five figures were written between one critic pass and the next, and four more between a correction and the merge, i.e. entirely inside the windows in which the tree was moving under them. Two full extra review rounds were spent on figures rather than on the change.
- **Disposition:** fixed in this cycle; the general remedy is rogue3#81, already open. Two specific lessons, both in §11.6: re-measure every quantity against the **merge base** rather than the base you branched from, and make the **last command before reporting a validation result be that validation**, at the tree that will merge, with its exit code read directly rather than recalled.

#### §4.12 The worktree-isolation guard rejects redirection, heredocs and pipelines, so every command that captures output must be split

- **Kind:** friction
- **Impact:** Every gate invocation in this cycle is two calls instead of one, and every ad-hoc
  analysis script had to be written to a file first.
- **Expected:** A worker in an isolated worktree can run `tool > log 2>&1; echo $?` and
  `python3 - <<EOF`.
- **Observed:** Both are refused with "this command is too complex to verify that it stays inside
  the worktree; break it into plain, separate commands". The workaround is benign — write the script
  to the scratchpad, invoke it by absolute path — and it interacts badly with the standing
  instruction to capture true exit codes rather than `$?` after a `| tail`, because the
  compliant form (`cmd > file; echo $?; tail file`) is itself a compound command. The form that
  survives is `cmd > file 2>&1; echo "EXIT=$?"` as one call and `tail file` as another.
- **Evidence:** command:eval "$(scripts/fsgg-coord whoami --mint)" is refused as too complex to verify; command:python3 - <<EOF heredoc is refused with the same message; command:dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest > FILE 2>&1 followed by a separate read is the form that works
- **Version:** harness at 2026-08-02
- **Owner:** worker orchestration harness
- **Recurrence:** first record in this repository's reports.
- **Avoidable cost:** ~10 minutes across the cycle, and one scratchpad script per analysis.
- **Disposition:** recorded; §11.3.

## §5 Did not exercise

- **No SDD stage ran.** This is a board `hardening` item with no `work/<id>/` tree; `fsgg-sdd
  charter`/`specify`/`plan`/`tasks`/`analyze`/`evidence`/`verify`/`ship` were not invoked and
  nothing in `work/` or `readiness/<id>/` changed. The lifecycle was not evaluated by this cycle and
  nothing here should be read as evidence about it.
- **The product was never launched.** No window was opened, no frame rendered, no offscreen readback
  taken. The change is confined to repository tooling and the feedback kit, and touches no
  `src/Rogue3/` file.
- **No performance workload was authored or measured.** `Verify`'s performance steps ran as part of
  the gate; nothing in this cycle reads or asserts their output.
- **No packaging, scaffold or dependency pin changed.** `.fsgg/scaffold-provenance.json`,
  `Directory.Packages.props` and `global.json` are untouched.
- **No `dotnet test` case was added.** The change is in tooling whose tests are its own self-tests;
  the suite is unchanged by this cycle at 277 and its greenness is a regression signal here, not coverage of this
  change.

## §6 Doc-versus-behavior contradictions

- **`.agents/skills/fs-gg-feedback-report/SKILL.md` stated the defect as doctrine.** "The locator
  still has to resolve inside the workspace and the file still has to exist — only the digest is
  unbindable" was true of the code and wrong as a rule. Rewritten with the code, in both kit copies,
  and the new text states the ordering explicitly and names the ten-case failure that follows from
  re-adding the check.
- **Both tools claimed agreement they did not have.** `digestExemption`'s doc comment said "The two
  must stay in agreement: a file the checker calls unbindable is a file this validator must not call
  stale, and vice versa" while the two exempt sets had differed by three paths since rogue3#56.
  Corrected by making the statement true rather than by weakening it.
- **Neither file cited the other on the existence rule, which is why the disagreement was invisible
  from both sides.** Both now carry a paragraph naming the other file, the other file's mechanism,
  and the instruction to change both together — `check-audit-bindings.py`'s module docstring gained
  an "An exempt citation is NOT required to EXIST" section, and `exemption`'s docstring names
  `digestExemptionWith` by path.
- **`scripts/check-audit-bindings.py`'s `grandfather` docstring contradicted a paragraph the same
  commit added 850 lines above it.** It justified the unconditional deletion ban partly with "one
  that could delete a file a merged audit cites would strand that audit for good" — false the
  moment this change landed, and false in the same file as the new section stating why. Rewritten to
  record the reason as *withdrawn* rather than to delete it silently, so a reader can see which of
  the two justifications survived.
- **`scripts/audit-binding-exceptions.json`'s own `note` gave the wrong reason for its existence.**
  It said the entries stay because merged audits cite the path "and an exempt citation must still
  resolve to a file that exists" — now false, and it is the sentence that would have told the next
  worker retirement was still blocked by #77. Rewritten to name the real blocker (most entries are
  live excuses) and to point at the command that measures it.
- **`.github/workflows/verify.yml` described the validator as carrying "a deliberate one-path
  exemption" and proving the exemption is "still exactly that one path".** Already loose after #53
  widened it to a directory; definitively wrong once #56's roll-ups reached this validator.
  Rewritten to describe both exemption classes and the existence waiver.
- **`feedback/2026-08-02-Rogue3-20.md` §4.2 and §11.1, and `feedback/2026-08-02-Rogue3-19.md`,
  describe the defect this cycle fixed and quote the SKILL.md sentence it deleted.** Merged reports
  are immutable and were not edited; the pointer is recorded here.

## §7 Workarounds still in the tree

- **`scripts/audit-binding-exceptions.json` is still a frozen archive — and this issue was only ONE
  of its two blockers, which the issue body does not say.** The citation blocker is gone: five
  merged audits cite it and those citations are now exempt from existence as well as digest. The
  blocker that remains is arithmetic, and a critic measured it: with the file present the gate
  reports 89 explicitly excused and 19 stale; with it deleted, 25 and 83. **Most of its 93 entries
  are live excuses**, so retiring it is a *migration* of those entries into
  `scripts/audit-binding-exceptions/`, not a deletion, and it is its own item with its own risk.
  The issue's "once this lands, the frozen archive stops being a workaround" is therefore half
  true, and the file's own `note` said the wrong thing about why it had to stay — corrected in this
  cycle rather than left (§6).
- **rogue3#53's unconditional "never delete a ledger file" rule is still unconditional, and that is
  now the right answer for a different reason.** It was written partly as a workaround for this
  issue living in a second tool. That reason is withdrawn, and the docstring saying so is corrected;
  what remains is the concurrency reason — a remedy with even a rare cross-cycle write is a path no
  item can declare — which #77 does not touch and which is sufficient on its own.
- **`derivedRollupRelativePaths` and `DERIVED_ROLLUP_RELPATHS` are two hand-maintained copies of one
  list.** They are now compared by CI (§4.5), but their *readers* still are not (§4.9; §11.2).

## §8 Friction and avoidable cost

- Re-deriving the item's three stale figures before planning: ~20 minutes, ~15% of the cycle
  (§4.1).
- Repinning the kit after one `SKILL.md` edit: ~6 minutes and two extra full gate runs (§4.10).
- Splitting every output-capturing command into two calls, and writing four scratchpad scripts to
  work around the heredoc and pipeline refusals: ~10 minutes (§4.12).
- Reading the two tools closely enough to be sure the exempt sets differed, rather than trusting
  either file's assertion that they could not: ~25 minutes, and it was the finding (§4.3).
- Closing the two critics' findings: ~45 minutes, the single largest block in the cycle, and the
  only one that produced defects rather than paperwork. Nine mutants had to be re-derived, applied
  and killed; four sentences in three files had to be corrected; one CI job extended.
- Everything else — the implementation itself and 39 new selftest cases — was under the friction
  total. The fix the issue asked for is one `match` arm.

## §9 Skill value and gaps

- **`fs-gg-feedback-report` was used and modified.** Its `SKILL.md` is the only place the exemption
  rule is written for a reader who is not already in the source, and it was wrong (§6). It is now
  the place the *new* rule is written, and it names the mutant and the failure count, so the next
  worker to consider re-adding an existence check can check the claim in one command.
- **Relevant, not invoked:** `intra-repo-parallel-work`. Two siblings were live, including one on a
  `Model` reshape with a wide touch-set; no overlap check was run because this cycle's touch-set is
  the feedback kit and `scripts/`, which no sibling item names.
- **Gap, unchanged since the #53 cycle recorded it:** no skill documents the audit-binding gate.
  Its rules live only in the module docstring of `scripts/check-audit-bindings.py`, which is found
  only by someone already looking at the file. This cycle added a section to that docstring, which
  deepens the same gap rather than closing it.

## §10 Outcome markers

- Merge base `14fd9eb`, all figures below re-measured there after the rebase. `./fake.sh build -t
  Verify` exit 0 on the candidate, **277** tests passed, 0 failed, and `git status --porcelain`
  completely empty afterwards. (It was 276 at `ccff119`; the extra case is `#60`'s, not this
  cycle's — `git diff origin/main -- tests/ src/` is empty.)
- Gate state on the candidate: `./fake.sh build -t SelfTest` **125** cases, 0 failures;
  `./fake.sh build -t TemplateDrift` **190 of 190** kit files pinned, 0 uncovered;
  `python3 scripts/check-audit-bindings.py --selftest` exit 0;
  `dotnet fsi .agents/skills/fs-gg-feedback-report/scripts/feedback-tool.fsx -- selftest`
  **88/88**, up from 49/49 at `ccff119`; `diff -rq .agents/skills .claude/skills` exit 0.
- The selftest's minimum-case FLOOR is replaced by an EXACT total (§4.8). At `ccff119` the floor was
  38 against an actual 49, so eleven cases could have been deleted and it would still have printed a
  green suite.
- Test count unchanged **by this cycle**: 277 on both `main` at `14fd9eb` and the candidate, and
  `git diff origin/main -- tests/ src/` is empty. This cycle adds no `dotnet test` case,
  deliberately (§5).
- Mutation: **13** targeted mutants over `FeedbackReportTool.fs`, 0 survivors. Four applied before
  the first critic pass — the pre-#77 ordering (13 cases on the shipped suite), a blanket existence waiver (suite
  aborts), `derivedExemptions` ignoring `.gitignore` (4 cases), `derivedExemptions` returning every
  ignored line (1 case). Nine more re-run after a code critic proved they survived the suite as it
  then stood: both halves of the containment check, the derived half's case sensitivity, the
  one-argument wrapper's contract, and five in the `.gitignore` reader (§4.6, §4.7). §1 records what
  the figure does and does not cover.
- One CI job extended rather than added: `.github/workflows/verify.yml`'s "Derived roll-ups stay out
  of the index" now compares the F# constant as well as the Python one (§4.5). Verified both ways —
  exit 0 against the candidate's source, exit 1 with a printed diff against `ccff119`'s.
- **Cost, measured against the tree this actually merges into (`14fd9eb`), not the tree it was
  written on.** Full corpus sweep, every `feedback/2026-08-0*.md` with a matching audit, true exit
  codes:

  Measured over the **33 pre-existing pairs** — every report/audit pair that exists on `main`,
  excluding this cycle's own, because whether *this* report validates is the critic's verdict and not
  a property of the tree that can be measured in advance:

  | tree | pre-existing pairs | pass | fail | the green ones |
  |---|---|---|---|---|
  | `main` @ `14fd9eb` | 33 | **2** | 31 | `Rogue3-19`, `Rogue3-21` |
  | candidate | 33 | **1** | 32 | `Rogue3-21` |

  So this cycle reds **exactly one** historical report — `feedback/2026-08-02-Rogue3-19.md`, on the
  `check-audit-bindings.py` docstring edits acceptance bullet 4 required — and leaves `Rogue3-21`
  untouched. Because `validate` has no excuse ledger (#40/#49), `Rogue3-19` is red permanently; 31 of
  those 33 were already red for the same reason before this branch existed. §11.4 is the fix.
- **This report's own status is reported separately, and deliberately not folded into the figure
  above.** An earlier draft wrote "34 pairs, pass=2, this report validates at exit 0" — which
  *assumes the audit's verdict*. That is a different error from a stale measurement: it is a claim
  that cannot be measured before the audit returns, so no amount of re-running makes it true. §4.11
  records the class; the operational half is in §11.6. Whatever `validate` returns for this pair at
  the merged tree is the answer, and it is the critic's to give.
- **The two earlier versions of that figure were both wrong, and the second was wrong only because
  the tree moved (§4.11 f, h).** A first sweep said "18 / 13 / 1"; a second, on the shipped branch at
  base `ccff119`, said 2 green → **0**, both lost to comment edits. That was true at `ccff119`. It is
  false at `14fd9eb`: `Rogue3-18` had already gone red there under a sibling's `#60` merge, so its
  loss is not this cycle's, and `Rogue3-21` — green at `14fd9eb` and not present in the `ccff119`
  sweep — survives. The 17 / 13 / 2 split remains accurate **for the `ccff119` base it was measured
  at**, which is exactly the qualification the earlier draft omitted.
- `python3 scripts/check-audit-bindings.py` exit 0 on the rebased candidate: 34 audits, 301
  bindings, 174 fresh, 127 excused, 21 not bound, 112 dormant over 6 ledger files. It took **two**
  `--grandfather` passes, not one, and that is the finding rather than the footnote (§4.11 g): the
  correction round moved `SKILL.md` and both `skill-manifest.json` copies again, and this cycle's own
  ledger had excused them at the *previous* digests, so the gate went back to exit 1 in between.
  Both passes wrote only
  `scripts/audit-binding-exceptions/item-77-exempt-citation-existence-waiver.json` and deleted
  nothing. This is rogue3#53 §4.8's predicted re-staling, observed inside a single cycle.
- Two critics under different rubrics, in their own worktrees, one with a mutation licence. **Both
  returned do-not-ship on a candidate that was green on every gate**, between them naming four false
  sentences, nine surviving mutants and one un-guarded constant. Every finding of both was closed or
  answered in writing; none was waved through.
- Ship readiness and merge: recorded in the pull request that carries this report.

## §11 Falsifiable improvements

1. **MIGRATE `scripts/audit-binding-exceptions.json`'s live entries into the ledger directory, then
   delete it.** The citation blocker is gone (§7); the remaining one is that most of its 93 entries
   are live excuses, so this is a migration and not a deletion. Owner: EHotwagner/rogue3
   `scripts/check-audit-bindings.py`. Acceptance: the legacy path is gone, the excused count is
   unchanged from before the migration (measured, not assumed — `--json` before and after), the gate
   exits 0, and each of the five merged audits citing the old path still validates with the citation
   reported under NOT BOUND rather than as missing.
2. **Make the two `.gitignore` declaration readers prove they agree, instead of asserting it.**
   Prevents §4.9 and would have prevented §4.3. Owner: FS-GG `fs-gg-feedback-report` kit and
   EHotwagner/rogue3 `scripts/`. Acceptance: one fixture of adversarial `.gitignore` text — comments,
   trailing slashes, all four negation forms, CRLF, a BOM, blank and whitespace-only lines — is fed
   to both readers by a check that runs in `Verify`, and a one-line divergence in either
   implementation fails it. The same check should assert `derivedRollupRelativePaths` and
   `DERIVED_ROLLUP_RELPATHS` are the same list.
3. **Let a worktree-isolated worker capture a command's output and true exit code in one call.**
   Prevents §4.12. Owner: worker orchestration harness. Acceptance: `cmd > file 2>&1; echo $?` and a
   heredoc writing inside the worktree are accepted, or the refusal message names the two-call form
   that works instead of only saying the command is too complex.
4. **Give `validate` an excuse ledger, so correcting a comment stops destroying merged evidence.**
   Prevents the cost measured in §10 and the general form of it: at `14fd9eb` **31 of 33** merged
   reports were already red, essentially all on source digests that moved for reasons unrelated to
   the finding citing them, and this cycle takes one more. The severity is not the count but the
   mechanism: a merged report's validity decays on every unrelated edit to any file it cites, so the
   steady state is a corpus that is entirely red and therefore says nothing. Owner: FS-GG
   `fs-gg-feedback-report`. Acceptance: a stale citation onto a file whose *content relevant to the
   finding* did not change can be excused by a diffable, reasoned entry, exactly as
   `check-audit-bindings.py --grandfather` allows; a report so excused validates green; and the
   excuse names the digest it was written against, so the next change to the same file fails again.
   Tracked upstream as the #40/#49 gap, which is named in `SKILL.md` as a known gap and has no issue.
5. **Make "two implementations that must agree" a named, gated relationship rather than a comment.**
   Generalises §4.9, §4.5 and §4.3, and would have caught the divergence rogue3#56 shipped. Owner: FS-GG.
   Acceptance: a file can declare `agrees-with: <path>#<symbol>`, and some gate fails when the two
   symbols' behaviour diverges on a shared fixture — so the next pair cannot drift for two cycles
   with both sides insisting in prose that they cannot.
6. **Make the last command before reporting a validation result BE that validation, at the tree that
   will merge; never report a result you are recalling; and never state a figure that assumes your
   own audit's verdict.** Prevents §4.11 (g) through (l) — six of the eleven. Re-measuring at the
   merge base is necessary and was *not* sufficient: (i) happened after a correct re-measurement,
   because the edit recording it re-staled the audit binding; (k) happened because a rebase orphaned
   a provenance hash, so naming the revision protects a figure only while that revision stays
   reachable; and (l) is not a measurement error at all but a category error, a figure that asserts
   the conclusion the audit exists to reach. Owner: EHotwagner/rogue3 `work-board` / `pnext-item`,
   and any worker reporting a gate result. Acceptance, in order: (1) rebase onto the current default
   branch; (2) make every prose edit; (3) re-run every command the report's §10 quotes and write
   those numbers; (4) obtain the critic's signature at that exact tree; (5) run `validate` and read
   its exit code directly; (6) merge. **After (4), an edit is permitted only if it falsifies no
   sentence in the report** — an audit pins the report's bytes, so a change that would make any
   sentence untrue requires a fresh signature, while one that cannot (a ledger file the exemption
   already covers, say) does not. A blanket freeze after signature is a notch too strong and would
   forbid settling the gate. Two testable rejection rules fall out: reject a report whose §10 quotes
   a validation result whose command did not run at the merged tree — the audit's `reportSha256`
   pins exactly that — and reject a corpus figure that counts the report's own pair, because that
   number is unknowable until the audit returns.

## §12 Development-surface coverage

| Surface | Status | Evidence and result |
|---|---|---|
| scaffolding | not-exercised | No scaffold operation ran; `.fsgg/scaffold-provenance.json` unchanged. |
| onboarding-guidance | partial | `CLAUDE.md` is a one-paragraph SDD pointer and carries no account of the audit-binding gate or the feedback validator; both were learned from source docstrings (§9). |
| skills | exercised | `fs-gg-feedback-report` used and modified — `SKILL.md`, `FeedbackReportTool.fs` and `feedback-tool.fsx`; both kit mirrors held byte-identical (`diff -rq .agents/skills .claude/skills` exit 0) and repinned (§4.10). |
| sdd-authoring | not-exercised | No `work/<id>/` tree; no `fsgg-sdd` stage ran (§5). |
| implementation-apis | not-exercised | No product API was called; no `src/Rogue3/` file is touched. |
| dependencies-build | exercised | `./fake.sh build -t KitPins`, `-t TemplateDrift` (190 of 190), `-t SelfTest` (125 cases, 0 failures) and `-t Verify` (exit 0, tree clean) all run; friction at §4.10. |
| testing | exercised | The feedback-tool selftest 49 → 88 cases, its minimum-case floor replaced by an exact total (§4.8); 13 mutants applied and all killed, the headline one by 13 of 88 cases, nine of the mutants re-run only after a code critic proved the first suite left them alive (§4.6, §4.7, §10); one CI job extended to cover a third statement of a list it already guarded (§4.5). |
| evidence | exercised | This cycle's subject. `python3 scripts/check-audit-bindings.py` at `ccff119`: 32 audits, 282 bindings, 177 fresh, 105 excused, 20 not bound, exit 0. On the rebased candidate: 34 audits, 301 bindings, 174 fresh, 127 excused, 21 not bound, 112 dormant over 6 ledger files, exit 0 — after **two** `--grandfather --cycle item-77-exempt-citation-existence-waiver` passes, the first re-staled by the correction round that followed it (§4.11 g). Both passes wrote only this cycle's own ledger file and deleted nothing. Corpus sweep over the 33 pre-existing pairs, excluding this cycle's own: `main` @ `14fd9eb` 2 pass / 31 fail; candidate 1 pass / 32 fail (§10). |
| runtime-playtest | not-exercised | The product was never launched; no rendered frame was produced or asserted (§5). |
| performance | not-exercised | No performance workload was authored or measured; `Verify`'s performance steps ran as part of the gate and nothing here reads them. |
| documentation | exercised | `SKILL.md` in both kit copies and the module docstring of `scripts/check-audit-bindings.py` rewritten with the code; three doc-versus-behaviour contradictions found and corrected (§6). |
| packaging-upgrade | not-exercised | No package pin, scaffold version or kit release changed (§5). |
| worker-git-pr | exercised | Identity minted via `scripts/fsgg-coord whoami --mint` with no harness-session warning; `take --repo EHotwagner/rogue3 --json` claimed rogue3#77 at exit 0; work carried in an isolated worktree on a branch, never on `main`; two critics run in their own worktrees. |

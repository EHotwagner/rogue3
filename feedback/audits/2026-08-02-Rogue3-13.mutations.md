# Mutation record — cycle `item-47-shop-grants-nothing`

Supporting artifact for `feedback/2026-08-02-Rogue3-13.md` §4.2. Committed because the previous
cycle's equivalent (`readiness/015-unify-world-state-generations/critic-history.md`) is the only
reason its mutation claims were checkable, and this cycle's first draft was graded `incomplete` by
the actionability critic for lacking one.

Method for every row: copy the target file aside, apply the single textual substitution, **rebuild**,
run `dotnet test tests/Rogue3.Tests/Rogue3.Tests.fsproj -m:1 --disable-build-servers`, record the
verdict, restore the file. A run whose reported test TOTAL does not match the expected count ran a
stale assembly and its verdict is void — this happened once (row A4 below) and was re-run.

`KILLED` = at least one test failed. `SURVIVED` = the suite stayed green with the product broken.

## Sweep A — author, against the pre-review change (expected total 246)

| # | Mutation | Verdict |
|---|---|---|
| A1 | `grantItem` drops the `PlayerItems` append | KILLED (7 failed) |
| A2 | `grantItem` drops `recomputePlayerStats` | KILLED (6 failed) |
| A3 | `grantItem` drops the `ItemsFound` increment | KILLED (8 failed) |
| A4 | shop stops granting the item (`ShopOffer.Item _ -> paid`) | KILLED — **first run VOID** (reported total 235, a stale assembly); re-run with a forced rebuild: 3 failed at total 246 |
| A5 | shop stops granting the consumable | KILLED (1 failed) |
| A6 | `loadM5Room` discards the pedestal again (`if isBoss then reward else None`) | KILLED (3 failed) |
| A7 | the fixed step stops calling `collectRoomReward` | KILLED (3 failed) |
| A8 | the reward is not removed from durable floor state | KILLED (1 failed) |
| A9 | the boss guard is dropped from `roomRewardCollectable` | KILLED (1 failed) |
| A10 | the grant emits no `AudioEvent.ItemGranted` | KILLED |
| A11 | an unauthored item's fallback becomes `[]` | KILLED (1 failed) |
| A12 | `roomRewardPosition` index drifts from the drawn slot | KILLED (1 failed) |
| A13 | `cracked-lens` loses its multishot modifier | KILLED (2 failed) |

**13 / 13 killed.** This is the sweep that made the *derivation* trustworthy, and it is also the
sweep that hid the problem: every mutation targets the fix itself, none targets the surface around
it. The independent critic below is what found that gap.

## Sweep B — independent fresh-context critic, against the same pre-review change

Ran 57 mutations at 246 passing tests. **31 killed, 26 survived (54%).** Every run rebuilt and
reported total 246; no void runs.

The critic's own summary of the split: the fix could not be silently reverted — `Reward=(if isBoss
…)`, `Reward=None`, dropping `|> collectRoomReward` and all eight `grantItem` field/order/recompute
mutations died, as did `ShopOffer.Item item -> paid`, the literal #47 defect. The damage was one ring
out. Of the 26 survivors it judged 4 equivalent, leaving **22 actionable**:

| Area | Survivors | Examples |
|---|---|---|
| `itemModifiers` | 11 | four of six authored arms deletable outright; both tear-delay SIGN FLIPS; `coal-heart` losing knockback; `maggot-crown` `PierceStat → BounceStat`; fallback `max 0` dropped, `0.5 * → 5.0 *`, `DamageStat → KnockbackStat` |
| `roomRewardRadius` / overlap | 4 | `20.0 → 1.0`, `→ 60.0`, `→ 400.0`, and `circlesOverlap … → 1.0e6` |
| `recomputePlayerStats` clamps | 3 | `tearDelay max 1.0 → 0.001`; `Pierce max 0` dropped; `FireRate` clamps removed |
| new audio cue | 2 | `itemPickup → pickupCoin`; volume `0.85 → 0.0` |
| durability | 1 | `withoutRewardFixture` filtering `ItemPedestal` only — the boss reward re-grants on every re-entry, unbounded |
| `purchaseM5ShopSlot` | 1 | `\| None -> model` replaced with a grant |

Root causes in the tests, as diagnosed: assertions of *shape* (`Expect.isNonEmpty`, `notEqual
basePlayerStats`) rather than *value*; a `walkUntil` helper that dropped the step count
`M11PlayabilityLegibilityTests` returns for exactly this reason, so no test asserted the walk
happened; and no negative case anywhere.

## Sweep C — author, re-running the critic's named survivors after hardening (expected total 253)

| # | Mutation | Verdict |
|---|---|---|
| S1 | `iron-teeth` arm deleted (falls through to the generic fallback) | KILLED |
| S2 | `void-map` arm deleted | KILLED |
| S3 | `maggot-crown` arm deleted | KILLED |
| S4 | `choir-bell` arm deleted | KILLED |
| S5 | `iron-teeth` tear-delay SIGN FLIP (the slow item becomes fast) | KILLED |
| S6 | `choir-bell` tear-delay SIGN FLIP (the fast item becomes slow) | KILLED |
| S7 | `coal-heart` loses its knockback modifier | KILLED |
| S8 | `maggot-crown` pierces the wrong stat | KILLED |
| S9 | the fallback stops clamping negative quality | KILLED |
| S10 | the fallback scale inflated 10x | KILLED |
| S11 | `roomRewardRadius` widened to 400 (grabs most of the room) | KILLED |
| S12 | durability filters `ItemPedestal` only (boss reward re-grants forever) | KILLED |
| S13 | an unknown slot id grants an item | KILLED |
| S14 | the boss guard disjunct is dropped | KILLED |
| S15 | `ItemGranted` cues the wrong sound | KILLED |
| S16 | `ItemGranted` is silent | KILLED |

**16 / 16 killed.**

## Sweep D — author, on the two fixes the critics prompted (expected total 247 then 253)

| # | Mutation | Verdict |
|---|---|---|
| R1 | a bought coin credits `RunStats.CoinsCollected` again (restores the score regression) | KILLED (1 failed) |
| R2 | the consumable effect is dropped entirely | KILLED (2 failed) |
| R3 | `DescendFloor` leaves a stale collectable reward | KILLED (1 failed at total 253) |
| R4 | the fixture sweep reverts to a per-floor fresh pool | KILLED (1 failed) |

**4 / 4 killed.** R3's first attempt was skipped by the harness because its anchor text was not
unique in the file; re-run with a unique anchor.

## Where this leaves the change

- **33 of 33** author mutations killed at the final state (sweeps A, C, D).
- Of the critic's **22 actionable** survivors: **16** re-run by name and killed (sweep C); **3**
  stat-clamp survivors now covered by a new clamp test in `M14ItemGrantTests.fs` but not individually
  re-run; **3** remain unpinned — loosenings of `roomRewardRadius` to values (`1.0`, `60.0`) that stay
  inside the walk helper's own 2-unit settling tolerance, so no walk-based test can distinguish them.
  The upper bound is pinned by the new standoff test; the lower bound is not, and that is a known
  limit rather than a claim.
- The **4** mutants the critic judged equivalent were not re-run.

Suite: 235 at `3913c26` → 253. Debug and Release both green, Release exit code 0.

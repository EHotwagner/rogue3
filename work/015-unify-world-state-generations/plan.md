---
schemaVersion: 1
workId: 015-unify-world-state-generations
title: Unify World State Generations
stage: plan
changeTier: tier1
status: planned
sourceSpec: work/015-unify-world-state-generations/spec.md
sourceClarifications: work/015-unify-world-state-generations/clarifications.md
sourceChecklist: work/015-unify-world-state-generations/checklist.md
publicOrToolFacingImpact: true
---

# Unify World State Generations Plan

Prose status: planned

## Source Snapshot
- spec: work/015-unify-world-state-generations/spec.md sha256:4e4dc2d9a03426b8330ab7f052b316239b37e3df23fb0ad5e19ec35f89297e03 schemaVersion:1
- clarifications: work/015-unify-world-state-generations/clarifications.md sha256:76893f389893cde1a0067f4f9c13e35257b80b0487a191881c7378cf14d2bb31 schemaVersion:1
- checklist: work/015-unify-world-state-generations/checklist.md sha256:66f6f0fed76de0f7b38b241f0f3970d6a4738e5b730c6cb14ab9566bb2f355b7 schemaVersion:1

## Plan Scope
- Work item 015-unify-world-state-generations is planned from the current specification, clarification, and checklist facts.
- Requirement count: 12.
- Clarification decision count: 9.
- Checklist result count: 12.
- Effective touch-set: `src/Rogue3/Model.fs`, `src/Rogue3/Entities.fs`, `src/Rogue3/PerformanceEvidence.fs`, `tests/Rogue3.Tests/`, `readiness/`, `work/015-unify-world-state-generations/`, this cycle's three `feedback/` files and `scripts/audit-binding-exceptions.json`. A whole-tree survey established that `Render.fs`, `EvidenceCommands.fs`, `GameplayVisualInventory.fs`, `AudioCues.fs`, `FloorGeneration.fs`, `Replay.fs`, `Program.fs`, `M7Ui.fs`, `LayoutEvidence.fs`, `View.fs`, `GameShell.fs`, `build.fsx` and every `scripts/*.fsx` reference none of the removed surface.

## Plan Decisions
- PD-001 [AC-001] [FR-001] [DEC-001] [DEC-002] complete: The removal set is exactly `Model.Enemy` (`Model.fs:114`), `Model.ShopCost` (`:138`), `Model.ShopSlot` (`:140`), the `Obstacles`, `Enemies` and `ShopSlots` fields (`:325`, `:327`, `:330`), their two initialisers (`:919-924`, `:2337-2342`), `Msg.InteractShop` (`:412`), `purchaseShopSlot` (`:1299`), its `update` arm (`:2258`) and `legacyEnemy` (`:1540`). Nothing is deprecated first: the three sets that could disagree are edited in one commit.
- PD-002 [AC-004] [AC-005] [FR-004] [FR-005] [DEC-001] complete: Add `Model.blockingObstacleRects : Rogue3.Entities.Obstacle list -> Rect list` — `List.filter (fun o -> Rogue3.Entities.blocksMovement Rogue3.Entities.MovementClass.Grounded o.Kind) >> List.map (fun o -> toSimRect o.Position 40.0 40.0)` — placed above its first use. The four sites that maintained the cache (`resolveBombs` `:1457-1459`, `loadM5Room` `:1581-1584`/`:1624`, `damageM5Obstacle` `:1689-1691`, and the boot initialiser) drop their `Obstacles=` assignment entirely; the `blocking` locals disappear with them. The three read sites call the function: the player sweep `:1884`, the `shotWalls` filter `:1912`, and the `TotalWallQueries` accounting `:1945`. The value is byte-identical to what the cache held, so no counter moves for this reason alone.
- PD-003 [AC-003] [FR-003] [DEC-002] [DEC-006] complete: `Rogue3.Entities.EnemyActor` gains `Velocity: Vec2`, `LastContactTick: int option` and `HitFlashTicks: int`; `Entities.spawn` initialises them to `zero`, `None` and `0`, so `grubSplit`, `stepEnemy` and every `{ actor with … }` in the product keep working unchanged. `Radius` and `ContactDamage` are read at the use sites through `Rogue3.Entities.definition actor.Kind`; a private `Model.actorRadius`/`actorContactDamage` pair keeps those reads short. A comment at `Velocity` records that shot knockback writes it and no integrator reads it, naming the follow-up issue.
- PD-004 [AC-002] [AC-008] [FR-002] [FR-008] [DEC-003] complete: `resolveShotCombat` stops rebuilding a list from a live filter. It builds its broadphase from `model.M5Enemies |> List.filter (fun a -> a.HitPoints > 0.0)` exactly as it did from `Enemies`, accumulates hits into a `Map<int, EnemyActor>` keyed by id, and writes back `M5Enemies = model.M5Enemies |> List.map (fun a -> Map.tryFind a.Id updated |> Option.defaultValue a)` — preserving list order and **keeping zero-hit-point actors**. `resolveBombs`, `resolveEnemyDamage` and `resolveCombat`'s `aliveBefore`/`aliveAfter` switch to `M5Enemies` with their existing `HitPoints > 0.0` filters intact. `stepM5Entities` loses the `hpById` re-sync from the legacy list (there is nothing left to sync from) and keeps its `§14.21` cleanup as the only remover of dead actors, which is what preserves the drop roll, the kill credit, the grub split and the room clear.
- PD-005 [AC-006] [FR-006] [DEC-009] complete: The legacy shop tests are re-pointed, not deleted. `M3CombatHealthCurrencyTests`' affordability/rejection pair moves to `purchaseM5ShopSlot` over an `Entities.ShopSlot` with an `Item` offer and a coin price; `M10AcceptanceDeterminismTests` scenario 11 dispatches `InteractM5Shop` through `update` and asserts the accepted and rejected paths on `M5ShopSlots`. `Entities.purchase` empties a slot by setting its `Offer` to `Empty` rather than removing it from the list, so the assertions become "the offer is consumed" and "the offer survives" rather than list-length checks — stated here because it is the one place the re-pointing is not a mechanical substitution.
- PD-006 [AC-007] [FR-007] [DEC-009] complete: The descent tests keep their seed-then-descend shape and their guard-the-guard assertion. `M4ProceduralFloorTests`' two seeds and `M10AcceptanceDeterminismTests` scenario 18 seed `M5Enemies`, `M5Obstacles` and `M5ShopSlots` after the boss-room staging, assert non-emptiness before the descent, and assert emptiness after — the in-file comments warning that entering a cleared room would make the assertion unable to fail are preserved and still apply.
- PD-007 [AC-004] [FR-004] [DEC-009] complete: The two fixtures that would become unfalsifiable are re-expressed rather than dropped. `M2MovementShotTests`' slide-along-AABB test replaces its hand-built `Rect` with an `Entities.obstacle Rock` placed so the derived 40x40 rect stands where the old collider stood, and keeps asserting the player's settled X. `M10AcceptanceDeterminismTests`' friendly-fire test seeds an actor co-located with an enemy bullet and keeps asserting the actor's hit points are unchanged.
- PD-008 [AC-009] [FR-009] [DEC-006] complete: Every remaining fixture that constructed a `Model.Enemy` becomes `{ Rogue3.Entities.spawn floorIndex id kind position with HitPoints = … }`. `Grub` is the default kind: radius 12 and contact damage 1, which keeps every fixture whose old `ContactDamage = 0` mattered outside the `13 + 12 = 25`-unit contact range at its existing position. Kinds are chosen per fixture where the old radius was load-bearing. The test-local `legacyOf` projection in `M10AcceptanceDeterminismTests` is deleted with the type it produced.
- PD-009 [AC-002] [FR-002] complete: A new regression test in `M10AcceptanceDeterminismTests` pins the §14.21 closure: one shot that kills an enemy through the production `Tick` route leaves no actor with that id in the model, credits exactly one kill in `RunStats.KillsByType`, and advances the drop stream exactly once. It is written so that reverting PD-004's "keep zero-hit-point actors" clause makes it fail.
- PD-010 [AC-010] [FR-010] [DEC-004] [DEC-006] complete: In `PerformanceEvidence.fs`, `maximumObstacles` and `maximumEnemies` are deleted; `maximumContentModel` drops its `Obstacles=` and `Enemies=` assignments and keeps `M5Enemies = maximumM5Enemies` and `M5Obstacles = maximumM5Obstacles`. The two measurement arms become `(blockingObstacleRects afterModel.M5Obstacles).Length` and `afterModel.M5Enemies |> List.filter (fun a -> a.HitPoints > 0.0) |> List.length`. `state.static-obstacles` and `state.live-enemies` keep their ids and get re-pointed `ScaleSource` text; every `MaximumExpected` that the measured population moves — including `collision.shot-wall-queries` and `collision.combat-candidates`, which change because the fixture's enemy radii become the real roster radii instead of a uniform 64 — is re-declared to the value the run emits. No driver is retired.
- PD-011 [AC-011] [FR-011] [DEC-008] complete: `readiness/performance-evidence.json`, `readiness/m7-ui-performance.json` and `readiness/performance-intent.yml` are regenerated at the exact candidate; each emitted `definitionDigest` is copied into its `Authored` declaration and into `workloadDefinitionDigests`; `maximumExpectedScale` is rewritten to describe the surviving populations. Regeneration is done through the evidence command, not by hand, and the diff is reviewed before staging — known defect #26 means a full FAKE `Verify` in a worktree silently rewrites tracked `readiness/` artifacts, so the two real CI commands are what gate the merge and any `readiness/` drift not intended by this plan is reverted.
- PD-012 [AC-012] [FR-012] complete: The merge gate is reproduced locally as its two real commands — `dotnet test -c Release` and `python3 scripts/check-audit-bindings.py --selftest` followed by `python3 scripts/check-audit-bindings.py` — with each exit code captured directly rather than after a pipe. The FAKE `Verify` target is not the gate (CI does not invoke it) and three of its six steps are known no-ops (#34), so a green `Verify` is not treated as evidence for anything.
- PD-013 [DEC-005] acceptedDeferral: The `M5` prefix is not dropped. Recorded with its reason — three audit-bound files outside the touch-set and a ledger contended with `EHotwagner/rogue3#38` — and filed as a follow-up issue rather than left implicit.
- PD-015 [DEC-003] complete: Two independent fresh-context critics reviewed the candidate and both changed what ships. The code critic's mutation run found 19 of 38 mutations passing green, including every mutation of `actorRadius` -- the derivation this whole change is built on, which could be pinned to a constant 64.0, the exact unspawnable shape the removal exists to make unrepresentable -- and the removal of either `HitPoints > 0.0` filter from the `EnemyDied` death count, which silences death audio entirely with the suite green. Four guard tests were added in response: the roster radius/contact-damage derivation with a behavioural Brute-versus-Fly overlap, the death-cue emission, the shot pass-through filter (whose existing assertion counted shots in a tick during which none reached the obstacle and so could not fail), and a bomb KILL, which no test covered at all. Three of the four previously-green mutations are now caught; the fourth -- bombs damaging a zero-hit-point corpse -- is accepted as a provable no-op, since `max 0.0` leaves a corpse at zero either way.
- PD-016 [DEC-003] complete: The same critic established that AC-008's preservation claim was false for bomb and burst kills. That damage reached the actor list through an `hpById` re-sync running after the cleanup fold, so such a kill resolved a step late; it now resolves in the step the damage lands, shifting the `DropRng` draw order. AC-008 and FR-008 were amended to declare it, CA-003 carries the measured before/after, and PD-015's bomb-kill test guards it.
- PD-017 [DEC-009] acceptedDeferral: The evidence critic established that two deleted shop assertions -- the granted `PlayerItems` contents and the recomputed `PlayerStats.Damage` -- could not be restated against `purchaseM5ShopSlot`, because that reducer grants no item. This is the one place CA-009's "rewrite, never delete" rule was not met. The cause is a pre-existing, player-facing gap the removed dead reducer concealed; it is filed at root cause as `EHotwagner/rogue3#47`, recorded as DEF-003, and pinned by a characterization assertion in both shop tests so it goes red the day it is fixed.
- PD-014 [DEC-002] acceptedDeferral: Shot knockback is still recorded and never applied, and the hit flash is still never drawn. Both are carried unchanged and filed as a follow-up issue at root cause.

## Contract Impact
- PC-001 [PD-001] product contract: `Rogue3.Model` loses the public fields `Obstacles`, `Enemies` and `ShopSlots`, the public types `Enemy`, `ShopSlot` and `ShopCost`, the public `Msg` case `InteractShop` and the public function `purchaseShopSlot`. The canonical determinism encoding of every model moves as a consequence.
- PC-002 [PD-003] product contract: `Rogue3.Entities.EnemyActor` gains `Velocity`, `LastContactTick` and `HitFlashTicks`. `Rogue3.Entities.spawn`, `grubSplit`, `stepEnemy` and `stepBoss` keep their signatures; every construction outside `spawn` is a `with` expression, so no caller outside the touch-set breaks.
- PC-003 [PD-002] product contract: `Rogue3.Model` gains the public function `blockingObstacleRects`. It is the sole description of the player's static collider set.
- PC-004 [PD-010] [PD-011] repository contract: every workload definition digest in `readiness/performance-evidence.json` and `readiness/performance-intent.yml` moves, three cost drivers' `ScaleSource` and several `MaximumExpected` values are re-declared, and `scripts/audit-binding-exceptions.json` gains one entry per audit binding this change invalidates.
- PC-005 [PD-002] product contract: `Rogue3.Collision.sweepCircle` is product-owned scaffold source (`src/Rogue3/Collision.fs`), not a framework member; it keeps its signature and simply receives a derived list where it received a stored one.
- framework: FS.GG.Game.Core#SpatialGrid.build
- framework: FS.GG.Game.Core#SpatialGrid.queryRadius

## Verification Obligations
- VO-001 [PD-001] [PC-001] semanticTest: A source-level assertion that no file under `src/` names `Model.Enemies`, `Model.Obstacles`, `Model.ShopSlots`, `Model.Enemy`, `Model.ShopSlot`, `Model.ShopCost`, `InteractShop` or `purchaseShopSlot`, plus a green build — the removal is only real if the compiler agrees.
- VO-002 [PD-004] [PD-009] [PC-001] semanticTest: The §14.21 regression test, written so that restoring the dropped-on-death behaviour makes it fail; plus the existing dead-turret and room-clear scenarios staying green.
- VO-003 [PD-003] [PD-008] [PC-002] semanticTest: Damage, knockback, hit flash, contact re-tick and pierce assertions holding at their existing numeric values against `EnemyActor`.
- VO-004 [PD-002] [PD-007] [PC-003] [PC-005] semanticTest: The player stopped by a grounded-blocking obstacle and passing a non-blocking one through the production input route, with the slide-along-AABB assertion still able to fail.
- VO-005 [PD-005] [PD-006] semanticTest: Affordability, rejection and descent-clearing assertions holding against the M5 shop and the surviving room-local collections, each seeded observably non-empty first.
- VO-006 [PD-010] [PD-011] [PC-004] semanticTest: A full Release evidence run at the exact candidate, every timing and structural budget green, every exact cost-driver equality holding at its re-declared value, and every moved definition digest copied from that run.
- VO-007 [PD-012] semanticTest: `dotnet test -c Release` exit 0 and both `check-audit-bindings.py` invocations exit 0 at the exact candidate, with the exit codes captured directly.
- VO-008 [PD-008] [FR-009] semanticTest: The test count at the candidate is at least the 228 present at `d8d0024`, and the code critic mutation-tests the change by reverting one edit at a time and confirming a test goes red.

## Performance Intent
- PI-001 PERF-PLAN: The producer-owned typed declaration `readiness/performance-intent.yml` (`PI-GENERATED-GAME`, disposition `active`, 60 FPS, p95 16.67 ms / p99 25.0 ms, `maxCatchUpFrames` 0, structural budgets `scene-nodes<=4096`, `shot-spawn-history<=40`, `combat-candidates<=2520`) is the single performance contract; this work item maintains no second one. This is a refactor with no intended cost change on any route, so the declared expectation is **identity on every timing budget and movement only where the `maximum-content` fixture's shape changed**, with each movement caused and stated.
- PI-002 PERF-PLAN: Focused skills loaded for their performance sections: `fs-gg-game-core` (the fixed step and `SpatialGrid`), `fs-gg-collision` (broad/narrow phase and the swept cast), `fs-gg-testing` and `fs-gg-playtest`. No new workload is authored and no workload route changes: the three routes this change touches — the fixed step, the player sweep and combat resolution — are already the substance of `maximum-content` and `firing`.
- PI-003 PERF-PLAN: Route analysis, stated before implementation. (a) The player sweep receives `blockingObstacleRects model.M5Obstacles` where it received `model.Obstacles`. Same elements, same order, computed once per step instead of once per obstacle mutation — an O(obstacles) map per step against a room that carries at most nine, on a path that already performs six swept casts. (b) Combat resolution builds its broadphase from `M5Enemies` filtered live instead of from a list that was already filtered live; the population is identical, so the grid build and every `queryRadius` are unchanged. (c) `resolveShotCombat` stops rebuilding a 30-element list and instead maps over it once — same order of work. (d) Nothing is added to the AI, bullet, homing, scene or HUD paths, and no scene node changes, so `scene-nodes` must not move at all.
- PI-004 PERF-SMOKE: The pre-implementation baseline is the committed `readiness/performance-evidence.json` at `d8d0024` together with a green build and a 228-test suite captured before any source change (`dotnet test Rogue3.slnx`, exit 0, 228 passed). The declared numbers each post-change value is read against: `collision.shot-wall-queries` 820, `simulation.homing-target-considerations` 2400, `state.static-obstacles` 8, `state.live-enemies` 30, `state.m5-enemies` 30, `state.m5-obstacles` 5, `combat-candidates` structural budget 2520, `scene-nodes<=4096`.
- PI-005 PERF-IMPLEMENT: No new counter is introduced, because no new cost is introduced. The existing exact-equality drivers are the instrument: `MaximumExpected` is compared for equality in `maximum-content` (`src/Rogue3/PerformanceEvidence.fs` line 1827), so any unintended change in the number of wall queries, combat candidates, live enemies or static obstacles reds the evidence run instead of passing under a ceiling. The two movements this plan *expects* are `collision.shot-wall-queries` and `collision.combat-candidates`, both caused by the `maximum-content` fixture's enemy population becoming the real roster (radii 8..22) instead of 30 uniform 64-unit discs that no floor can spawn; each is re-declared from the run and its cause recorded. A movement with any other cause is investigated, not copied.
- PI-006 PERF-RELEASE: Before the pull request is opened, the Release test route and the evidence run execute against the exact candidate and the typed evidence is retained. An exceeded timing budget, a non-zero catch-up count, a `scene-nodes` movement or a cost-driver inequality that this plan did not predict blocks the change. `liveCompositorRequired: false`, so no live-compositor claim is made and the bounded-headless results stay labelled as such. No independent Governance verdict capability is wired in this repository; that is recorded as a capability limit rather than claimed.
- PI-007 PERF-REPORT: The report records, per workload, the pre- and post-change p95/p99, scene nodes and catch-up frames; the re-declared values of every moved cost driver with its cause; and the reason each definition digest moved. The durable decision for the next worker: in this product a model **field removal** moves every workload digest for the same reason a field addition does — `definitionDigest` folds `Determinism.encode` of the initial model — so a refactor that changes no behaviour still owns a full re-declaration loop, and that loop belongs in the plan rather than being discovered at verify.

## Migration Posture
- PM-001 [PC-001] [PC-004] breaking-encoding: The canonical model encoding changes, so a determinism golden captured before this change no longer compares equal to one captured after. Nothing in the repository persists an encoded model across versions — `Replay` folds an input log through `update` at run time and `ProfileStore` persists `MetaProfile`, which is untouched — so there is no stored artefact to migrate. Every existing CLI form, the saved profile format and every audio cue id are unchanged.
- PM-002 [PC-001] removing: The legacy shop surface is removed outright rather than deprecated. `InteractShop` has zero production dispatch sites, so a deprecation window would protect no caller and would leave the product shipping two shop reducers for another cycle — which is the condition this work item exists to end.
- PM-003 [PC-002] additive: `EnemyActor` gains fields rather than changing any, and `spawn` supplies them, so every existing construction site keeps compiling.

## Generated View Impact
- GV-001 [PD-011] workModel: `readiness/015-unify-world-state-generations/` refreshes from current plan sources or reports staleGeneratedView; `readiness/performance-evidence.json`, `readiness/performance-intent.yml` and `readiness/m7-ui-performance.json` are regenerated at the exact candidate with each movement explained. Known defect #26 means a FAKE `Verify` run in a worktree silently rewrites tracked `readiness/` artifacts, so the diff is inspected before staging and any drift this plan did not intend is reverted.
- GV-002 [PD-012] repositoryGate: `python3 scripts/check-audit-bindings.py --selftest` and `python3 scripts/check-audit-bindings.py` must both pass at the candidate. `scripts/audit-binding-exceptions.json` gains one entry per binding this change invalidates, each carrying the digest the audit pins, the digest the file now has, and its reason — excused rather than rebound, following the `7e71d71` precedent, because repointing an earlier cycle's audit would claim its critic examined code that did not exist. That file is contended with `EHotwagner/rogue3#38`; the write is sequenced with its holder rather than raced.

## Accepted Deferrals
- DEF-001 acceptedDeferral: The `M5` prefix stays on the surviving fields. Condition to lift: an item whose touch-set includes `Render.fs`, `EvidenceCommands.fs` and `GameplayVisualInventory.fs`.
- DEF-002 acceptedDeferral: Shot knockback is recorded and never applied; the hit flash is recorded and never drawn. Both filed at root cause.

## Planning Findings
No blocking planning findings recorded.

## Advisory Notes
- Optional Governance pointers remain compatibility facts only.
- The lesson this item encodes: the product kept two representations of the same fact for eight milestones, and the gate that finally noticed was a player-visible bug, not a test. Nothing in the suite could fail merely because `Enemies` and `M5Enemies` disagreed, because no test asserted they agreed — and per DEC-001 that is why an invariant test was declined in favour of removal. A repository that wants this class of defect caught earlier needs a check that reports two model fields whose populations are maintained in lockstep by hand, which is the falsifiable improvement this cycle proposes.
- `fsgg-sdd plan` has been observed by four previous cycles to reclaim the `## Performance Intent` section back to its placeholder line. `plan.md` is copied to a scratch path before each `fsgg-sdd plan` run and restored if reclaimed; it is never accepted.

## Lifecycle Notes
- Next lifecycle action: `fsgg-sdd tasks --work 015-unify-world-state-generations`.

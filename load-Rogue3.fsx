// GENERATED — do not edit. Emitted with the scaffold and renamed with the Rogue3.
// Build first, then load the Rogue3 and its transitive dependencies in one step:
//   ./build.sh build
//   dotnet fsi load-Rogue3.fsx
//
// This script references and opens only the app; .NET resolves its dependencies from
// the same build output. It launches nothing, so a missing assembly is a normal load
// failure and host-warning classification is unaffected.
#r "src/Rogue3/bin/Debug/net10.0/Rogue3.dll"
open Rogue3
